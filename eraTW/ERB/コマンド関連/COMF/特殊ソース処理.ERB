@特殊ソース处理
;圧抑、抵抗持ちには、抑郁ソース追加
SIF TALENT:自己爱 < 0 || TALENT:抵抗
	SOURCE:郁闷 += 1000

;贞操重视持ちで恋慕、亲爱持ち以外には常に反抗のソースを追加する（性交系のみ）
SIF TALENT:贞操 > 0 && !TALENT:恋慕
	SOURCE:反感 += 1000

;处女だった场合は、苦痛のソースと反抗のソースを追加する
IF TALENT:处女 == 1
	SOURCE:苦痛 += 2000
	SOURCE:反感 += 3000 / ((TCVAR:TARGET:Counter行动 == 41 || TCVAR:TARGET:Counter行动 == 43) ? 10 # 1)
ENDIF

;再生处女だった场合は、苦痛のソースと反抗のソースを追加する
IF TALENT:处女 == 2
	SOURCE:苦痛 += 500
	SOURCE:反感 += 1000 / ((TCVAR:TARGET:Counter行动 == 41 || TCVAR:TARGET:Counter行动 == 43) ? 10 # 1)
ENDIF