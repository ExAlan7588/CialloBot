;-------------------------------------------------
;自慰
;日常系指令、レベル1
;-------------------------------------------------
@COM451
#DIM C_ID
#DIM AMT
IF TARGET
	PRINTFORMW 自慰的时候、必须是谁也不能打扰的 自由和救贖的 独自宁静丰富的……
	RETURN -1
ENDIF
PRINTFORML 脑海中想起了谁？
DRAWLINE
FOR LOCAL,1,CHARANUM
	IF !IS_NAMED(LOCAL) || !CFLAG:LOCAL:认识
		CONTINUE
	ENDIF
	PRINTFORMLC [{LOCAL, 3}] %NAME_OUTPUT_TRANSLATION("CALLNAME", LOCAL)%
NEXT
PRINTL
DRAWLINE
PRINTL [999] 谁也不去想 [1000] 放弃
INPUT
C_ID = RESULT
SELECTCASE C_ID
	CASE 1 TO CHARANUM
		CALL PRINT_FACE, C_ID, "发情", "裸", "_1"
		PRINTFORML %NAME_OUTPUT_TRANSLATION("CALLNAME", MASTER)%想象着%NAME_OUTPUT_TRANSLATION("CALLNAME", C_ID)%处理了性欲
	CASE 999
		PRINTFORML %NAME_OUTPUT_TRANSLATION("CALLNAME", MASTER)%思考着%TEXTR("天体的运行/山川草木/生老病死")%处理了性欲
	CASEELSE
		RETURN -1
ENDSELECT
AMT = 50 + RAND:50
IF TALENT:MASTER:浓厚精液
	AMT *= 2
	PRINTFORMW ……浓厚的{AMT}出来了
ELSE
	PRINTFORMW ……{AMT}出来了
ENDIF
BASE:MASTER:精力 -= AMT
DOWNBASE:MASTER:气力 = 100

TIME += 20
RETURN 1

;等待
@COM_ABLE451
;ショップ画面では不可
SIF !TFLAG:100
	RETURN 0
;批量管理
SIF GLOBAL_COMABLE(451)
	RETURN RESULT
;气力0では不可
SIF BASE:MASTER:气力 <= 0
	RETURN 0
;精力0では不可
SIF BASE:MASTER:精力 <= 0
	RETURN 0
;ウフフ中は不可
SIF CFLAG:诶嘿嘿
	RETURN 0
;停止中は不可
SIF FLAG:70 == 1
	RETURN 0
;私室以外では不可
SIF CFLAG:MASTER:当前位置 != CFLAG:MASTER:初始位置
	RETURN 0
RETURN 1