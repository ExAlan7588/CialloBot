;-------------------------------------------------
;和留琴对话
;日常系指令、レベル1
;-------------------------------------------------
@COM420
PRINTL 「你有什么事吗？」
PRINTL [0] - 想找人
PRINTL [1] - 想做扫除
PRINTL [2] - 关于git控制系统
PRINTL [100] - 返回
INPUT
IF RESULT == 0
	PRINTL 「要寻找谁呢？」
	CALL HITOSAGASI()
ELSEIF RESULT == 1
CALL OHEYA_CHECK
ELSEIF RESULT == 2
	PRINTDATA
		DATAFORM 「使用git控制系统的话可以将补丁一次性整合起来哦」
		DATAFORM 「有git控制系统就可以很简单的把握变更的地方」
		DATAFORM 「过去的代码也能很好的得到保存」
	ENDDATA
	PRINTW 好像很厉害的样子
	RETURN -1
ELSEIF RESULT == 100
	RETURN -1
ELSE
	RETURN -1
ENDIF
INPUTS
RESULT = TOINT(RESULTS)
IF RESULT == 0
	REPLACE RESULTS, CALLNAME:MASTER, "%CALLNAME:MASTER%"
	FOR LOCAL, 1, 19
		SIF STR:LOCAL == RESULTS
			RESULT = LOCAL
	NEXT
ENDIF
SIF RESULT == 0
	RETURN -1
SIF IN_HOME(RESULT) == 1
	CALL SKIP_MOVE(CFLAG:MASTER:当前位置, RESULT)
RETURN -1

@OHEYA_CHECK
VARSET LOCAL
FOR LOCAL, MINROOM(), MAXROOM
	LOCALS = %NAME_FROM_PLACE(LOCAL)%
	SIF LOCALS == ""
		CONTINUE
	SIF CFLAG:MASTER:当前位置 == LOCAL
		SETCOLOR C_GREEN
;	PRINTBUTTON "["+LOCALS+"]", LOCAL
;	RESETCOLOR
;	PRINTFORM %" "*(22-STRLENS(LOCALS))%
	PRINTFORM [{LOCAL, 3}]%LOCALS, 24, LEFT%
	LOCALS = %SEIKETU(LOCAL), 15, RIGHT%
	PRINTFORM %LOCALS%　　
	RESETCOLOR
	LOCAL:1 ++
	SIF LOCAL:1 % 2 == 0
		PRINTL
NEXT
PRINTFORML 
DRAWLINE
PRINTFORML [0] 返回
;PRINTBUTTON "[确认结束]", 0

@SEIKETU(ARG)
#FUNCTIONS
IF OUTROOF(ARG) && DAY:2 == 4
	SELECTCASE YOGORE:ARG
		CASE IS < 100
			SETCOLOR C_GREEN
			RETURNF "积雪量:干净"
		CASE IS < 500
			SETCOLOR C_M_GREEN
			RETURNF "积雪量:小"
		CASE IS < 1000
			SETCOLOR C_P_GREEN
			RETURNF "积雪量:较小"
		CASE IS < 2000
			SETCOLOR 0xFFFFFF
			RETURNF "积雪量:中"
		CASE IS < 3500
			SETCOLOR 0xFFFFFF
			RETURNF "积雪量:较大"
		CASE IS < 5000
			SETCOLOR 0xFFFFFF
			RETURNF "积雪量:大"
		CASEELSE
			SETCOLOR 0xFFFFFF
			RETURNF "积雪量:雪山"
	ENDSELECT
ELSE
	SELECTCASE YOGORE:ARG
		CASE IS < 100
			SETCOLOR C_GREEN
			RETURNF "清洁度:最高"
		CASE IS < 500
			SETCOLOR C_M_GREEN
			RETURNF "清洁度:高"
		CASE IS < 1000
			SETCOLOR C_P_GREEN
			RETURNF "清洁度:较高"
		CASE IS < 2000
			SETCOLOR C_YELLOW
			RETURNF "清洁度:中"
		CASE IS < 3500
			SETCOLOR C_V_PINK
			RETURNF "清洁度:较低"
		CASE IS < 5000
			SETCOLOR C_RED
			RETURNF "清洁度:低"
		CASEELSE
			SETCOLOR C_RED
			RETURNF "清洁度:垃圾山"
	ENDSELECT
ENDIF

@HITOSAGASI(ARG)
CALL HITOSAGASI_2(1)
CALL HITOSAGASI_2(2)
PRINTFORML [0] 返回
;PRINTBUTTON "[确认结束]", 0

;ARG=何回目の表示か(1or2)
@HITOSAGASI_2(ARG)
SIF FLAG:排序
	CALL CHARASORT_EX
FOR LOCAL:1, 1, EX_CHARANUM
	IF FLAG:排序
		LOCAL = RESULT:(LOCAL:1)
		SIF LOCAL < 0
			CONTINUE
	ELSE
		LOCAL = LOCAL:1
		SIF LOCAL >= CHARANUM
			BREAK
	ENDIF
	SIF AT_HOME(LOCAL) == 0
		CONTINUE
	SIF CFLAG:LOCAL:当前位置 < MINROOM()
		CONTINUE
	SIF CFLAG:LOCAL:当前位置 == MAXROOM && CFLAG:LOCAL:睡眠
		CONTINUE
	IF ARG == 1
		SIF !CFLAG:LOCAL:神社在住
			CONTINUE
		SETCOLOR C_S_BLUE
	ELSEIF ARG == 2
		SIF CFLAG:LOCAL:神社在住
			CONTINUE
	ENDIF
	PRINTFORM %NAME_OUTPUT_TRANSLATION("CALLNAME", LOCAL),11,Left%
	RESETCOLOR
;xxx
	;名字对齐 主要是 舞 105
	;PRINTFORM 一般{STRLENS(CALLNAME:LOCAL)}
	;PRINTFORM U{STRLENSU(CALLNAME:LOCAL)}
	;PRINTFORM %" "*(12-STRLENS(CALLNAME:LOCAL))%
	;老版本存档 名字 修改成新版 842
	;SIF CALLNAME:105 != CSVCALLNAME(105)
	;	CALLNAME:105 = %CSVCALLNAME(105)%
	;881 修改
	CALLNAME:105 = 舞
	    NAME:105 = 舞
	;SIF LOCAL == 105
	;	PRINTFORM 　
;依赖 CFLAG:403
;   -1　依赖发生直后/ 1　依赖未受托/ 2　依赖中/ 3　依赖达成/ 4　期限切れ/ 5　报告忘れ/ 6　依赖失败/
;    103 依赖结束（成功）/104 依赖结束（期限切れ）/105 依赖结束（报告忘れ）/106 依赖结束（失败）

	;逆援交 - 提示 可能做成任务提示？！

IF CFLAG:LOCAL:404 % 100 == 15
	IF CFLAG:LOCAL:403 == 1
		CALL COLORMESSAGE(@"！",C_V_PINK, 0)
	ELSEIF CFLAG:LOCAL:403 == 2
		CALL COLORMESSAGE(@"？",C_V_PINK, 0)
	ELSE
		PRINT 　
	ENDIF
ELSE
	IF CFLAG:LOCAL:403 == 1
		;任务不满 且 能接任务
		IF SEARCH_IRAI_EMPTY() && INT_DATA_IRAI(CFLAG:LOCAL:依赖内容, "依赖受托可能条件", LOCAL)
			CALL COLORMESSAGE(@"！",C_YELLOW, 0)
		ELSEIF INT_DATA_IRAI(CFLAG:LOCAL:依赖内容, "依赖受托可能条件", LOCAL)
			IF !SEARCH_IRAI_EMPTY()
				CALL COLORMESSAGE(@"！",C_GRAY, 0)
			ELSE
				PRINT 　
			ENDIF
		ELSE
			PRINT 　
		ENDIF
	ELSEIF CFLAG:LOCAL:403 == 2
		CALL COLORMESSAGE(@"？",C_GRAY, 0)
	ELSEIF CFLAG:LOCAL:403 == 3
		CALL COLORMESSAGE(@"？",C_YELLOW, 0)
	ELSE
		PRINT 　
	ENDIF
ENDIF

;交互角色
TCVAR:LOCAL:356 = 0
FOR LOCAL:11, 1, COUNT_IRAI_SLOT() + 1

SELECTCASE IRAI_SLOT_TO_TASKNAME(LOCAL:11)
CASE "调达"
	;PRINTFORML 　　要求品：%ITEMNAME:(IRAI_SLOT_TO_ITEM(LOCAL:11))%×{IRAI_SUB:LOCAL:11}
	
CASE "传令（人物）"
	;PRINTFORML 　目标人物：%NAME_OUTPUT_TRANSLATION("NAME", (IRAI_SLOT_TO_PERSON(LOCAL:11)))%
	SIF IRAI_SLOT_TO_PERSON(LOCAL:11) == LOCAL
		TCVAR:LOCAL:356 ++

CASE "捜索（人物）"
	IF IRAI_EX:(LOCAL:11) == LOCAL
		;PRINTFORML 　参考人物：%NAME_OUTPUT_TRANSLATION("CALLNAME", (IRAI_EX:(LOCAL:11)))%
		TCVAR:LOCAL:356 ++
	ENDIF

CASE "讨伐"
	;PRINTFORML 　讨伐对象：%NAME_OUTPUT_TRANSLATION("NAME", (IRAI_SLOT_TO_PERSON(LOCAL:11)))%
	SIF IRAI_SLOT_TO_PERSON(LOCAL:11) == LOCAL
		TCVAR:LOCAL:356 ++
ENDSELECT
NEXT
IF TCVAR:LOCAL:356 > 0 && CFLAG:LOCAL:403 == 103
	CALL COLORMESSAGE(@"*",C_GRAY, 0)
ELSEIF TCVAR:LOCAL:356 > 0
	CALL COLORMESSAGE(@"*",C_GREEN, 0)
ELSE
	PRINT  
ENDIF

	IF CFLAG:LOCAL:允许无套 == 2
		SETCOLOR 0xEE60EE
		PRINT ❤
	ELSEIF CFLAG:LOCAL:允许无套 == 1
		SETCOLOR 0xFFFF00
		PRINT ❤
	ELSEIF GETBIT(CFLAG:LOCAL:既成事实,1)
		PRINT ❤
	ELSE
		PRINT 　
	ENDIF
	RESETCOLOR
		IF TALENT:LOCAL:恋人
			SETCOLOR C_RED
			PRINTFORM [恋人]
		ELSEIF TALENT:LOCAL:恋慕 == 1
			SETCOLOR 0xEE60EE
			PRINTFORM [恋慕]
		ELSEIF TALENT:LOCAL:爱欲 == 1
			SETCOLOR 0x2E8B57
			PRINTFORM [爱欲]
		ELSEIF TALENT:LOCAL:炮友 == 2
			SETCOLOR 0xFF6347
			PRINTFORM [爱人]
		ELSEIF TALENT:LOCAL:思慕 == 1
			SETCOLOR 0xFFFF00
			PRINTFORM [思慕]
		ELSEIF TALENT:LOCAL:炮友 == 1
			SETCOLOR 0x7CFC00
			PRINTFORM [炮友]
		ELSE
			PRINTFORM       
		ENDIF
		RESETCOLOR


		IF ESTRUS_CYCLE(LOCAL) == 150
			SETCOLOR C_RED
			PRINTFORM [危险]
		ELSEIF ESTRUS_CYCLE(LOCAL) == 15
			SETCOLOR C_L_GRAY
			PRINTFORM [安全]
		ELSE
			PRINTFORM       
		ENDIF
		RESETCOLOR

;;;(MAXBASE:LOCAL:酒气 / 10) * 4 

		IF TCVAR:LOCAL:烂醉
			SELECTCASE BASE:LOCAL:酒气
				CASE IS >= (MAXBASE:LOCAL:酒气 / 10) * 4
					SETCOLOR C_RED
				CASE IS < (MAXBASE:LOCAL:酒气 / 10) * 4
					SETCOLOR 0x7CFC00
			ENDSELECT
			PRINT 醉
			RESETCOLOR
		ELSE
			SELECTCASE BASE:LOCAL:酒气

				CASE IS >= (MAXBASE:LOCAL:酒气 / 3) * 2
					SETCOLOR 0xFFFF00
					PRINTFORM 醉
				CASE IS < (MAXBASE:LOCAL:酒气 / 3) * 2
					PRINTFORM 　

			ENDSELECT
			RESETCOLOR
		endif
;xxx
	LOCALS = %NAME_FROM_PLACE(CFLAG:LOCAL:当前位置)%

	;交互对象---
	;SETCOLOR C_GRAY
	;FOR LOCAL:11, 1, 10
	;	SIF IRAI_SLOT_TO_PERSON(LOCAL:11) == LOCAL
	;		SETCOLOR C_GREEN
	;NEXT
	;PRINTFORM [{CFLAG:LOCAL:当前位置, 3}]
	;RESETCOLOR
	;交互对象---

	CALL COLORMESSAGE(@"[{CFLAG:LOCAL:当前位置, 3}]",C_GRAY, 0)
IF CFLAG:LOCAL:睡眠
	SETCOLOR 0,120,180
ELSEIF CFLAG:Master:当前位置 == CFLAG:LOCAL:当前位置
	SETCOLOR C_GRAY
ENDIF
	PRINTFORM %LOCALS%
RESETCOLOR
	;舞的房间修正
	SIF CFLAG:LOCAL:当前位置 == 235
		PRINTFORM 　
	IF STRCOUNT(LOCALS,"・")
		PRINTFORM %" "*(24-STRLENS(LOCALS))%
	ELSE
		PRINTFORM %" "*(25-STRLENS(LOCALS))%
	ENDIF
	IF LOCALS == ""
		PRINTFORM 位于不明位置({CFLAG:LOCAL:当前位置})
	ELSEIF CFLAG:LOCAL:睡眠
		SIF EXP:LOCAL:睡眠奸经验 > 100 || (TCVAR:LOCAL:睡眠深度 > 2000 && Palam:LOCAL:眠奸 < 1000)
			SETCOLOR 0xEE60EE
		CALL COLORMESSAGE(@"就寝中:",C_GRAY, 0)
		PRINTFORM {TCVAR:LOCAL:睡眠深度,5,right}　起{CFLAG:LOCAL:起床时间 / 60,2,RIGHT}时 
		SETCOLOR C_GRAY
		IF CFLAG:LOCAL:起床时间 > TIME
			SIF CFLAG:LOCAL:起床时间 - TIME < 60
				SETCOLOR 255,255,255
			PRINTFORM {CFLAG:LOCAL:起床时间 - TIME,4}
		ELSE
			SIF CFLAG:LOCAL:起床时间 - TIME < 60
				SETCOLOR 255,255,255
			PRINTFORM {CFLAG:LOCAL:起床时间 - TIME + 1440,4}
		ENDIF
		CALL COLORMESSAGE(@"分后",C_GRAY, 0)
		RESETCOLOR			 
	ELSEIF CFLAG:LOCAL:行动 == 5
		IF CFLAG:LOCAL:当前位置 == TFLAG:宴会场 && TIME_PROGRESS(TIME:1) && (CFLAG:LOCAL:职业种类 == 49 || CFLAG:LOCAL:职业种类 == 51)
			;PRINTL 宴会中
			CALL COLORMESSAGE(@"宴会中　　　",C_GRAY, 0)
		ELSE
			;PRINTL 仕事中
			CALL COLORMESSAGE(@"仕事中　　　",C_GRAY, 0)
		ENDIF
		IF ARG == 1
			CALL COLORMESSAGE(@"　睡",0x7070C0, 0)
			PRINTFORM {CFLAG:LOCAL:就寝时间 / 60,2,RIGHT}
		ELSEIF ARG == 2
			CALL COLORMESSAGE(@"　回",0x66FFFF, 0)
			PRINTFORM {CFLAG:LOCAL:回家时间 / 60,2,RIGHT}
		ENDIF
		CALL COLORMESSAGE(@"时",C_GRAY, 0)
	ELSE
		CALL COLORMESSAGE(@"积攒度:",C_GRAY, 0)
			SIF !TCVAR:LOCAL:302
				SETCOLOR C_GREEN
			SIF TCVAR:LOCAL:发情 == 1
				SETCOLOR 0xFF00FF
			PRINTFORM {CFLAG:LOCAL:积攒度,5,right}
			RESETCOLOR
			IF ARG == 1
				CALL COLORMESSAGE(@"　睡",0x7070C0, 0)
				PRINTFORM {CFLAG:LOCAL:就寝时间 / 60,2,RIGHT}
			CALL COLORMESSAGE(@"时",C_GRAY, 0)
		SETCOLOR C_GRAY
		IF CFLAG:LOCAL:就寝时间 > TIME
			SIF CFLAG:LOCAL:就寝时间 - TIME < 60
				SETCOLOR 255,255,255
			PRINTFORM  {CFLAG:LOCAL:就寝时间 - TIME,4}
		ELSE
			SIF (CFLAG:LOCAL:就寝时间 - TIME + 1440) < 60
				SETCOLOR 255,255,255
			PRINTFORM  {CFLAG:LOCAL:就寝时间 - TIME + 1440,4}
		ENDIF
		CALL COLORMESSAGE(@"分后",C_GRAY, 0)
		RESETCOLOR
			ELSEIF ARG == 2
				CALL COLORMESSAGE(@"　回",0x66FFFF, 0)
				PRINTFORM {CFLAG:LOCAL:回家时间 / 60,2,RIGHT}
			CALL COLORMESSAGE(@"时",C_GRAY, 0)
		SETCOLOR C_GRAY
		IF CFLAG:LOCAL:回家时间 > TIME
			SIF CFLAG:LOCAL:回家时间 - TIME < 60
				SETCOLOR 255,255,255
			PRINTFORM  {CFLAG:LOCAL:回家时间 - TIME,4}
		ELSE
			SIF (CFLAG:LOCAL:回家时间 - TIME + 1440) < 60
				SETCOLOR 255,255,255
			PRINTFORM  {CFLAG:LOCAL:回家时间 - TIME + 1440,4}
		ENDIF
		CALL COLORMESSAGE(@"分后",C_GRAY, 0)
		RESETCOLOR
			ENDIF
	ENDIF
		IF Activity_Duration:LOCAL > 0
			SETCOLOR C_GRAY
			PRINTFORM  自由行动
			SETCOLOR 255,255,255
			PRINTFORM {Activity_Duration:LOCAL,3,RIGHT}
			SETCOLOR C_GRAY
			PRINTFORM 分 
		ENDIF
	RESETCOLOR
	PRINTL
	;
	;IF ARG == 1
	;	PRINTFORML ❤睡{CFLAG:LOCAL:就寝时间 / 60,2,RIGHT}时
	;ELSEIF ARG == 2
	;	PRINTFORML ❤回{CFLAG:LOCAL:回家时间 / 60,2,RIGHT}时
	;ENDIF
NEXT

;-------------------------------------------------
;实行判定
;-------------------------------------------------
;和留琴对话
@COM_ABLE420
;自宅外は不可
SIF AT_HOME(MASTER) == 0
	RETURN 0
;停止中は不可
SIF FLAG:70 == 1
	RETURN 0
;移动实行判定
SIF !TFLAG:100
	RETURN 0
;批量管理
SIF GLOBAL_COMABLE(420)
	RETURN RESULT
SELECTCASE MAIN_MAP
	CASE 0
		;留琴が寝ていたらダメ
		SIF CFLAG:2:睡眠
			RETURN 0
		;留琴の居る所
		SIF CFLAG:MASTER:当前位置 != CFLAG:2:当前位置
			RETURN 0
	CASE 3
		SIF CFLAG:MASTER:当前位置 != 323
			RETURN 0
	CASE 9
		SIF CFLAG:MASTER:当前位置 != 923
			RETURN 0
	CASEELSE
		RETURN 0
ENDSELECT

SIF CFLAG:诶嘿嘿 == 2
	RETURN 0
RETURN 1

