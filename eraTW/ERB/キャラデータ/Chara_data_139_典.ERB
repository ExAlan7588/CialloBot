@CHARAMOVE_DATA_139(ARG, ARG:2)
LOCAL = 139
SELECTCASE ARG
	;连动设定（RETURN=依存する角色, RESULT:1～3=对等に连动する角色）
	CASE 1
		;たまに连动
		SIF CFLAG:LOCAL:今日的运势 % 8 == 0
			RETURN [[龙]]
	;特殊生活时间设定（CFLAG:LOCAL:生活时间补正を设定する）
	CASE 3
	;移动倾向（±～10=行きやすい/行きにくい, -100=行かない）
	CASE 4
		SELECTCASE ARG:2
			;天界には行かない
			CASE P830玄云海 TO P842比那名居邸库房
				RETURN -100
		ENDSELECT
	;出现场所
	CASE 7
		SELECTCASE ARG:2
			CASE 樱花道的鸟居, 神社境内, 神社走廊
				RETURN 1
			CASE 广场, 商业街, 吞屋小道
				RETURN 1
			CASE 森林的入口, 香霖堂, 古木的大树, 雾雨魔法店
				RETURN 1
			CASE 山麓树海, 玄武之泽, 大蛤蟆之池, 间歇泉地下中枢
				RETURN 1
			CASE 九天瀑布, 绝景之丘, 山之湖, 守矢神社境内, 虹龙洞
				RETURN 1
		ENDSELECT
	;引力设定（CFLAG:LOCAL:引力点を设定する）
	CASE 8
ENDSELECT

@EXIST_キャラデータ139
#LOCALSIZE 1
#LOCALSSIZE 1

@キャラデータ139(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "称呼"
	CALLF MAKE_STR(V_NAME, "典")
;こっから衣装设定
CASE "今天的睡衣"
	CALLF MAKE_STR(V_NAME, "睡衣（裤子+兜帽）套")

CASE "便装：连衣裙"
	CALLF MAKE_INT(V_NAME, CLOTHES_ID("连衣裙", "旗袍（裤子）"))
CASE "便装：袜子"
	CALLF MAKE_INT(V_NAME, CLOTHES_ID("袜子", "短布袜"))
	
CASE "固有内衣形容词"
	CALLF MAKE_STR(V_NAME, "蛊惑的な")
CASE "固有下半身内衣名"
	CALLF MAKE_STR(V_NAME, "纯白胖次")
CASE "固有上半身内衣"
	CALLF MAKE_INT(V_NAME, 1)
CASE "固有上半身内衣名"
	CALLF MAKE_STR(V_NAME, "番の管狐ブラ")
CASE "固有内衣着用条件"
	SIF !RAND:(3 - (2 * 胜负内衣判定([[典]])))
		CALLF MAKE_INT(V_NAME, 1)
CASE "固有内衣半透明(前)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "固有内衣半透明(后)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "固有内衣布料厚度"
	CALLF MAKE_INT(V_NAME, 1)

;料理关联
CASE "料理：喜欢的味道"
	CALLF MAKE_STR(V_NAME, "肉料理/肉料理/肉料理/卵/卵/卵/油炸食品/甜/甜/辣/辣/简易/清淡/豪华/上品/新鲜/粘稠/")
CASE "料理：讨厌的味道"
	CALLF MAKE_STR(V_NAME, "酸/酸/浓厚/朴素/朴素/野趣/野趣/")
CASE "料理：助手效果"
	CALLF MAKE_STR(V_NAME, "隐し味/")
;赠り物关联
CASE "感性：好き"
	CALLF MAKE_STR(V_NAME, "高级/白/混沌/可爱的/朴素/别致/知的/" + "纯白の/狐の/药瓶/" * 5)
CASE "感性：嫌い"
	CALLF MAKE_STR(V_NAME, "黑/秩序/夸张/威严/" + "安物/护符/" * 3 + "狸の/护臂/" * 10)
ENDSELECT

;-------------------------------------------------
;固有料理设定
;DISH_NAME          料理名
;RESULTS            料理の种类（轻食/主食/甜点）
;CALL 材料设定("")  材料の指定。"泛用"を入れると自动で泛用食材になる
;DISH_TASTE         味の设定
;DISH_COMMENT       （任意）一行コメント
;DISH_TIME          （任意）调理にかかる时间
;DISH_MAXLIMIT      （任意）料理の持续时间
;-------------------------------------------------
@SPECIAL_MENU_139
DISH_NAME = 狐狸米饭
RESULTS = 主食
CALL 材料设定("胡萝卜")
DISH_TASTE = 和食/米/野菜/咸/简易/清淡/温暖/朴素/家庭的/
DISH_COMMENT = 煮好的油豆腐与胡萝卜搭配米饭而制成的料理。有种令人怀念的味道。
RETURN 1

;IMAGE_I139_特殊服装(CHARA)-------------------------------------------------------------------
;角色特有の服装表示条件
;エフェクトを共有する服装差分はこちらで指定する
;使用时注意点:
;	RESULTSに特殊服装を格纳すること
;	书式は %服装%%特殊服装% 适宜"_"等入れること
;-------------------------------------------------------------------------------------------
@IMAGE_I139_特殊服装(CHARA)
#DIM CHARA
VARSET RESULTS

IF CFLAG:CHARA:应用差分替换 && FLAG:立绘种类
	IF CFLAG:CHARA:衣物暂时更换 == 55 || (CFLAG:CHARA:基本服装SET == 55 && CFLAG:CHARA:衣物暂时更换==0)
		SIF !CHECK_NUDE(CHARA)
			RESULTS = _バニー
		ENDIF
	ELSE
	;通常画像
	IF CHECK_NUDE(CHARA)
		;裸
		IF FLAG:立绘种类
			;颜绘
		ELSE
			;立绘
		ENDIF
	ELSE
		;服
		IF CFLAG:CHARA:衣物暂时更换 == 55 || (CFLAG:CHARA:基本服装SET == 55 && CFLAG:CHARA:衣物暂时更换==0)
			IF FLAG:立绘种类
				;颜绘
				RESULTS = _バニー
			ELSE
				;立绘
				RESULTS = _バニー
			ENDIF
		ENDIF
	ENDIF
ENDIF

;IMAGE_I139_体形(CHARA)-----------------------------------------------------------------------
;角色特有の体形表示条件
;エフェクトを共有する体形差分はこちら
;服装を指定しても自动で表示されるので注意
;使用时注意点:
;	RESULTSに体形を格纳すること
;	书式は %体形%%服装% 适宜"_"等入れること
;	"巨乳"は本编で实装済み %体形%巨乳%服装% で追记可能
;-------------------------------------------------------------------------------------------
@IMAGE_I139_体形(CHARA)
#DIM CHARA
VARSET RESULTS

IF CFLAG:CHARA:应用差分替换
	;替换用画像
	IF FLAG:立绘种类
		;颜绘
	ELSE
		;立绘
	ENDIF
ELSE
	;通常画像
	IF FLAG:立绘种类
		;颜绘
	ELSE
		;立绘
		SIF TALENT:CHARA:妊娠 && CFLAG:CHARA:妊娠经过日数 >= 41
		RESULTS = 妊娠
	ENDIF
ENDIF

;IMAGE_I139_特殊表情(CHARA)--------------------------------------------------------------------
;角色特有の表情表示条件
;时间停止中でも维持する表情はこちら
;使用时注意点:
;	RESULTS:0に表情を格纳
;	RESULTS:1にマウスオーバー时の表情を格纳すること
;	"通常","笑颜","愤怒","发情","睡眠"は本编で实装済み
;-------------------------------------------------------------------------------------------
@IMAGE_I139_特殊表情(CHARA)
#DIM CHARA

IF CFLAG:CHARA:应用差分替换
	;替换用画像
	IF FLAG:立绘种类
		;颜绘
		IF RESULTS:0 == "通常"
			IF CFLAG:CHARA:诶嘿嘿 || CFLAG:CHARA:积攒度 >= 500
				RESULTS:0 = 半目
				RESULTS:1 = 半目
			ELSEIF TALENT:CHARA:心情 > 0
				RESULTS:0 = 恶い颜
				RESULTS:1 = 恶い颜
			ENDIF
		ENDIF
	ELSE
		;立绘
	ENDIF
ELSE
	;通常画像
	IF FLAG:立绘种类
		;颜绘
	ELSE
		;立绘
	ENDIF
ENDIF

;IMAGE_I139_特殊表情COM(CHARA, 指令)-------------------------------------------------------
;直前の指令で变化する角色特有の表情表示条件
;时间停止中は变化しない表情はこちら
;使用时注意点:
;	RESULTS:0に表情を格纳
;	RESULTS:1にマウスオーバー时の表情を格纳すること
;	"自慰","爱抚","性交"は本编で实装済み
;-------------------------------------------------------------------------------------------
@IMAGE_I139_特殊表情COM(CHARA, 指令)
#DIM CHARA
#DIM 指令

IF CFLAG:CHARA:应用差分替换
	;替换用画像
	IF FLAG:立绘种类
		;颜绘
	ELSE
		;立绘
	ENDIF
ELSE
	;通常画像
	IF FLAG:立绘种类
		;颜绘
	ELSE
		;立绘
	ENDIF
ENDIF

;IMAGE_I139_差分(CHARA)-----------------------------------------------------------------------
;角色特有の差分表示条件
;裸やエフェクトを共有しない画像はこちら
;使用时注意点:
;	RESULTSに差分を格纳すること
;	书式は {CHARA}%差分% 适宜"_"等入れること
;-------------------------------------------------------------------------------------------
@IMAGE_I139_差分(CHARA)
#DIM CHARA
VARSET RESULTS

IF CFLAG:CHARA:应用差分替换
	;替换用画像
	IF FLAG:立绘种类
		;颜绘
	ELSE
		;立绘
	ENDIF
ELSE
	;通常画像
	IF FLAG:立绘种类
		;颜绘
	ELSE
		;立绘
	ENDIF
ENDIF

;IMAGE_I139_特殊照れ(CHARA)--------------------------------------------------------------------
;角色特有の照れエフェクト表示条件
;使用时注意点:
;	RESULTSに特殊照れを格纳すること
;	书式は %特殊照れ%照れ
;	"裸"は本编で实装済み %特殊照れ%裸照れ で追记可能
;-------------------------------------------------------------------------------------------
@IMAGE_I139_特殊照れ(CHARA)
#DIM CHARA
VARSET RESULTS

IF CFLAG:CHARA:应用差分替换
	;替换用画像
	IF FLAG:立绘种类
		;颜绘
	ELSE
		;立绘
	ENDIF
ELSE
	;通常画像
	IF FLAG:立绘种类
		;颜绘
	ELSE
		;立绘
	ENDIF
ENDIF

;IMAGE_I139_特殊吐息(CHARA)--------------------------------------------------------------------
;角色特有の吐息エフェクト表示条件
;使用时注意点:
;	RESULTSに特殊吐息を格纳すること
;	书式は %特殊吐息%吐息
;	"裸"は本编で实装済み %特殊吐息%裸吐息 で追记可能
;-------------------------------------------------------------------------------------------
@IMAGE_I139_特殊吐息(CHARA)
#DIM CHARA
VARSET RESULTS

IF CFLAG:CHARA:应用差分替换
	;替换用画像
	IF FLAG:立绘种类
		;颜绘
	ELSE
		;立绘
	ENDIF
ELSE
	;通常画像
	IF FLAG:立绘种类
		;颜绘
	ELSE
		;立绘
	ENDIF
ENDIF

;IMAGE_I139_特殊汗(CHARA)--------------------------------------------------------------------
;角色特有の汗エフェクト表示条件
;使用时注意点:
;	RESULTSに特殊汗を格纳すること
;	书式は %特殊汗%汗
;	"裸"は本编で实装済み %特殊汗%裸汗 で追记可能
;-------------------------------------------------------------------------------------------
@IMAGE_I139_特殊汗(CHARA)
#DIM CHARA
VARSET RESULTS

IF CFLAG:CHARA:应用差分替换
	;替换用画像
	IF FLAG:立绘种类
		;颜绘
	ELSE
		;立绘
	ENDIF
ELSE
	;通常画像
	IF FLAG:立绘种类
		;颜绘
	ELSE
		;立绘
	ENDIF
ENDIF

;-------------------------------------------------------------------------------------------
;IMAGE_I139_特殊エフェクトチェック(CHAR, エフェクト)--------------------------------------------
;角色特有のエフェクト判定  特殊エフェクトを直接指定したい场合に参照
;参数 "エフェクト" に固有のエフェクトが含まれるかを判定する
;例の街道、STRFINDを使用してください
;-------------------------------------------------------------------------------------------
@IMAGE_I139_特殊エフェクトチェック(CHARA, エフェクト)
#DIM CHARA
#DIMS エフェクト

IF CFLAG:CHARA:应用差分替换
	;替换用画像
ELSE
	;通常画像
	SIF STRFIND(エフェクト, "ぶっかけ") != -1
		RETURN 1
	SIF STRFIND(エフェクト, "お口") != -1
		RETURN 1
ENDIF

RETURN 0

;IMAGE_I139_特殊エフェクト(CHARA)--------------------------------------------------------------------
;角色特有のエフェクト表示条件
;使用时注意点:
;	RESULTS配列に特殊エフェクトを格纳
;	RETURNで追加するエフェクト数を返还すること
;-------------------------------------------------------------------------------------------
@IMAGE_I139_特殊エフェクト(CHARA)
#DIM CHARA
#DIM DYNAMIC エフェクト数 = 0
VARSET RESULTS

IF CFLAG:CHARA:应用差分替换
	;替换用画像
	IF FLAG:立绘种类
		;颜绘
	ELSE
		;立绘
	ENDIF
ELSE
	;通常画像
	IF FLAG:立绘种类
		;颜绘
			IF CFLAG:CHARA:脸上精液
				;胸ぶっかけ
				RESULTS:エフェクト数 = ぶっかけ
				エフェクト数 += 1
			ENDIF
			IF CFLAG:CHARA:口内精液
				;口交
				RESULTS:エフェクト数 = お口
				エフェクト数 += 1
			ENDIF
	ELSE
		;立绘
	ENDIF
ENDIF

RETURN エフェクト数

;********************************************************************************************
@IMAGE_EX_I139_PREリソースチェック(CHARA, 表示形式, 服装, 表情, 表情_マウスオーバー, 差分, 乳量)
#DIM CHARA
#DIMS 表示形式
#DIMS 服装
#DIMS 表情
#DIMS 表情_マウスオーバー
#DIMS 差分
#DIMS 乳量

SIF CFLAG:CHARA:应用差分替换 == 1
{
CALL COMPOSE_SPRITE_PRESET([[典]],"别颜_裸_/别颜_服_/别颜_服_バニー_",
"通常/笑颜/愤怒/发情/トロ颜/イキ颜/半目/恶い颜")
}