@CHARAMOVE_DATA_37(ARG, ARG:2)
LOCAL = 37
SELECTCASE ARG
	;连动设定（RETURN=依存する角色, RESULT:1～3=对等に连动する角色）
	CASE 1
		;土日は阿燐と一绪
		IF YOUBI_MATCH(DAY, "土日")
			RESULT:1 = [[阿燐]]
			RESULT:2 = [[阿空]]
		ENDIF
	;特殊生活时间设定（CFLAG:LOCAL:生活时间补正を设定する）
	CASE 3
	;移动倾向（±～10=行きやすい/行きにくい, -100=行かない）
	CASE 4
		SELECTCASE ARG:2
			;后院が好き
			CASE P923后院
				RETURN 5
			;入らない房间
			CASE P904大会堂, P916库房, P918恋私室, P919觉私室, P920书斋, P925薔薇园, P930旧血池地狱
				RETURN -100
			CASE P936勇仪的家, P937空屋, P938鬼的聚集场所, P940帕露西的家, P941山女的家, P942琪斯美的家, P943茅屋, P947血枪的栖息处
				RETURN -100
		ENDSELECT
	;出现场所
	CASE 7
		SELECTCASE ARG:2
			CASE 间歇泉地下中枢
				RETURN 1
			CASE 幻想风穴, 地狱的深道, 旧地狱街道, 地灵殿, 灼热地狱遗迹
				RETURN 1
		ENDSELECT
	;引力设定
	CASE 8
		;仕事终わりに出步行の,概率6分の4,6分の1で阿燐にくっつく
		IF YOUBI_MATCH(DAY, "月火水木金") && INRANGE(TIME, 780, 1140)
			SELECTCASE CFLAG:LOCAL:今日的运势 % 6
			CASE 1
				TCVAR:LOCAL:引力点 = P923后院
			CASE 2
				TCVAR:LOCAL:引力点 = P933酒场
			CASE 3
				TCVAR:LOCAL:引力点 = P945河岸
			CASE 4
				TCVAR:LOCAL:引力点 = TCVAR:[[阿燐]]:引力点
			CASEELSE
				TCVAR:LOCAL:引力点 = 0
			ENDSELECT
		;休日はまれに觉样にくっつく
		ELSEIF CFLAG:LOCAL:今日的运势 % 10 == 0 && !CFLAG:[[觉]]:诶嘿嘿 && SHIRAHU([[觉]])
			TCVAR:LOCAL:引力点 = CFLAG:[[觉]]:当前位置
		ENDIF
ENDSELECT

@EXIST_キャラデータ37
#LOCALSIZE 1
#LOCALSSIZE 1

@キャラデータ37(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "称呼"
	CALLF MAKE_STR(V_NAME, "阿空")
;こっから衣装设定
CASE "今天的睡衣"
	CALLF MAKE_STR(V_NAME, "睡衣（裤子+兜帽）套")

CASE "便装：外衣"
	CALLF MAKE_INT(V_NAME, CLOTHES_ID("外衣", "斗篷"))
CASE "便装：帽子"
	CALLF MAKE_INT(V_NAME, CLOTHES_ID("帽子", "蝴蝶结"))
CASE "便装：靴"
	CALLF MAKE_INT(V_NAME, CLOTHES_ID("靴", "靴"))
CASE "便装：袜子"
	CALLF MAKE_INT(V_NAME, CLOTHES_ID("袜子", "袜子"))
CASE "便装：裙子"
	CALLF MAKE_INT(V_NAME, CLOTHES_ID("裙子", "裙子"))
CASE "便装：上身衣服２"
	CALLF MAKE_INT(V_NAME, CLOTHES_ID("上身衣服２", "女式衬衫"))

CASE "固有内衣形容词"
	CALLF MAKE_STR(V_NAME, "描绘了【CAUTION！】")
CASE "固有下半身内衣名"
	CALLF MAKE_STR(V_NAME, "印花内裤(核)")
CASE "固有上半身内衣"
	CALLF MAKE_INT(V_NAME, 1)
CASE "固有上半身内衣名"
	CALLF MAKE_STR(V_NAME, "印花胸罩")
CASE "固有内衣着用条件"
	SIF !RAND:(1 + (2 * 胜负内衣判定([[阿空]])))
		CALLF MAKE_INT(V_NAME, 1)
CASE "固有内衣半透明(前)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "固有内衣半透明(后)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "固有内衣布料厚度"
	CALLF MAKE_INT(V_NAME, 2)

;料理关联
CASE "料理：喜欢的味道"
	CALLF MAKE_STR(V_NAME, "卵/香料/锅物/丰盛/浓厚/温暖/野趣/家庭的/豪快/松脆/地底风/甜/甜/辣/辣/煮鸡蛋/煮鸡蛋/煮鸡蛋/")
CASE "料理：讨厌的味道"
	CALLF MAKE_STR(V_NAME, "清淡/冷/酸/酸/禽肉/禽肉/禽肉/")
CASE "料理：助手效果"
	CALLF MAKE_STR(V_NAME, "地狱风/地底风/大火力/")
;赠り物关联
CASE "感性：好き"
	CALLF MAKE_STR(V_NAME, "朱/混沌/帅气的/充满力量/壮大/" + "火/日/鸟/弹/" * 3 + "地灵の/业火の/核热の/地底の/灵知の/" * 5)
CASE "感性：嫌い"
	CALLF MAKE_STR(V_NAME, "纤细/知的/" + "暗/书物/" * 3)

;农民关联
;アホの子角色から「ウドの大木」という言叶を连想
;さらに地底在住という立地は白独活の栽培に适している
;贬す意图はありませんし阿空ちゃんがキライなわけでもありません
;地狱迹へ续く穴に横穴でも掘って栽培？
CASE "农民：说明"
	CALLF MAKE_STR(V_NAME, "独活农家")
CASE "农民：作物"
	CALLF MAKE_STR(V_NAME, "白独活")
CASE "农民：田的种类"
	CALLF MAKE_STR(V_NAME, "独活畑")
CASE "农民：畑の所在地"
	CALLF MAKE_STR(V_NAME, "地底")

CASE "农民：MapID"
	CALLF MAKE_INT(V_NAME, 9)
CASE "农民：场所ID"
	CALLF MAKE_INT(V_NAME, 927)
CASE "农民：报酬"
	CALLF MAKE_INT(V_NAME, 400)
CASE "农民：金利"
	CALLF MAKE_INT(V_NAME, 3)
ENDSELECT

;-------------------------------------------------
;固有料理设定
;DISH_NAME          料理名
;RESULTS            料理の种类（轻食/主食/甜点）
;CALL 材料设定("")  材料の指定。"泛用"を入れると自动で泛用食材になる
;DISH_TASTE         味の设定
;DISH_COMMENT       （任意）一行コメント
;DISH_TIME          （任意）调理にかかる时间
;DISH_MAXLIMIT      （任意）料理の持续时间
;-------------------------------------------------
@SPECIAL_MENU_37
DISH_NAME = 核热烤牛排
RESULTS = 主食
CALL 材料设定("野味")
DISH_TASTE = 洋食/牛扒/肉料理/野味/香料/辣/丰盛/浓厚/温暖/豪华/野趣/豪快/豪快/豪快/大火力/地底风/
DISH_COMMENT = 用核聚变烹调的牛排。小心烫伤。
RETURN 1

;IMAGE_I37_特殊服装(CHARA)-------------------------------------------------------------------
;キャラクター特有の服装表示条件
;エフェクトを共有する服装差分はこちらで指定する
;使用时注意点:
;	RESULTSに特殊服装を格纳すること
;	书式は %服装%%特殊服装% 适宜"_"等入れること
;-------------------------------------------------------------------------------------------
@IMAGE_I37_特殊服装(CHARA)
#DIM CHARA
VARSET RESULTS
IF IS_BUNNY(CHARA) && FLAG:立绘种类 && CFLAG:CHARA:应用差分替换
	RESULTS = _バニー
ENDIF

;********************************************************************************************
@IMAGE_EX_I37_PREリソースチェック(CHARA, 表示形式, 服装, 表情, 表情_マウスオーバー, 差分, 乳量)
#DIM CHARA
#DIMS 表示形式
#DIMS 服装
#DIMS 表情
#DIMS 表情_マウスオーバー
#DIMS 差分
#DIMS 乳量

SIF CFLAG:CHARA:应用差分替换 == 1
	CALL COMPOSE_SPRITE_PRESET([[阿空]],"别颜_裸_/别颜_服_/别颜_服_バニー_")