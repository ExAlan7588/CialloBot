@CHARAMOVE_DATA_70(ARG, ARG:2)
LOCAL = 70
SELECTCASE ARG
	;连动设定（RETURN=依存する角色, RESULT:1～3=对等に连动する角色）
	CASE 1
		;日曜日は连动
		IF YOUBI_MATCH(DAY, "日")
			RESULT:1 = [[阿求]]
			RESULT:2 = [[小铃]]
		ENDIF
	;特殊生活时间设定（CFLAG:LOCAL:生活时间补正を设定する）
	CASE 3
	;移动倾向（±～10=行きやすい/行きにくい, -100=行かない）
	CASE 4
		SELECTCASE ARG:2
			;稗田邸,慧音的家,铃奈庵,自室,广场
			CASE P221稗田邸, P224慧音宅, P213铃奈庵, P226小铃私室, P202中央广场
				RETURN 5
			;私室には行かない
			CASE P208雷鼓的房间, P209八桥的房间, P210弁弁的房间
				RETURN -100
			CASE P231蛮奇的房间, P232莲子的房间, P233梅莉的房间, P234雪的房间, P235​舞​的房间, P239美宵的房间
				RETURN -100
		ENDSELECT
	;出现场所
	CASE 7
		SELECTCASE ARG:2
			CASE 樱花道的鸟居, 神社境内, 神社走廊
				RETURN 1
			CASE 广场, 商业街, 吞屋小道, 长屋街
				RETURN 1
		ENDSELECT
	;引力设定（CFLAG:LOCAL:引力点を设定する）
	CASE 8
ENDSELECT

@EXIST_キャラデータ70
#LOCALSIZE 1
#LOCALSSIZE 1

@キャラデータ70(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "称呼"
	CALLF MAKE_STR(V_NAME, "小铃")
;こっから衣装设定
CASE "今天的睡衣"
	CALLF MAKE_STR(V_NAME, "睡衣（襦袢）套")

CASE "便装：帽子"
	CALLF MAKE_INT(V_NAME, CLOTHES_ID("帽子", "发饰"))
CASE "便装：裙子"
	CALLF MAKE_INT(V_NAME, CLOTHES_ID("裙子", "行灯袴"))
CASE "便装：和服"
	CALLF MAKE_INT(V_NAME, CLOTHES_ID("和服", "和服"))
CASE "便装：连衣裙"
	CALLF MAKE_INT(V_NAME, CLOTHES_ID("连衣裙", "肌襦袢"))

CASE "固有内衣形容词"
	CALLF MAKE_STR(V_NAME, "英文报纸的")
CASE "固有下半身内衣名"
	CALLF MAKE_STR(V_NAME, "单调内裤")
CASE "固有上半身内衣"
	CALLF MAKE_INT(V_NAME, 1)
CASE "固有上半身内衣名"
	CALLF MAKE_STR(V_NAME, "单调胸罩")
CASE "固有内衣着用条件"
	SIF !RAND:(1 + (2 * 胜负内衣判定([[小铃]])))
		CALLF MAKE_INT(V_NAME, 1)
CASE "固有内衣半透明(前)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "固有内衣半透明(后)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "固有内衣布料厚度"
	CALLF MAKE_INT(V_NAME, 2)

;料理关联
CASE "料理：喜欢的味道"
	CALLF MAKE_STR(V_NAME, "米/牛奶/和食/面类/酸/简易/清淡/豪华/上品/家庭的/软糯的/外界风/" + "甜/" * 3)
CASE "料理：讨厌的味道"
	CALLF MAKE_STR(V_NAME, "野趣/辣/辣/" + "青蛙/香料/地狱风/" * 3 + "虫肉/怪诞/谜料理/" * 5)
CASE "料理：助手效果"
	CALLF MAKE_STR(V_NAME, "薄味/")
;赠り物关联
CASE "感性：好き"
	CALLF MAKE_STR(V_NAME, "绿/秩序/可爱的/奇妙/活泼/知的/不思议/狂热/" + "眼镜/书物/" * 3 + "妖怪の/妖魔の/外界の/妖魔图鉴/铃/" * 5)
CASE "感性：嫌い"
	CALLF MAKE_STR(V_NAME, "混沌/可怕的/" + "火/幽灵/刃物/弹/生体品/护臂/烟袋/鼠の/" * 3)
ENDSELECT

;-------------------------------------------------
;固有料理设定
;DISH_NAME          料理名
;RESULTS            料理の种类（轻食/主食/甜点）
;CALL 材料设定("")  材料の指定。"泛用"を入れると自动で泛用食材になる
;DISH_TASTE         味の设定
;DISH_COMMENT       （任意）一行コメント
;DISH_TIME          （任意）调理にかかる时间
;DISH_MAXLIMIT      （任意）料理の持续时间
;-------------------------------------------------
@SPECIAL_MENU_70
DISH_NAME = 铃铛蛋糕
RESULTS = 甜点
CALL 材料设定("甜味料")
DISH_TASTE = 和食/卵/甜/艺术的/家庭的/软糯的/
DISH_COMMENT = 一口大小的圆形蛋糕。半球上有焦斑、形状像铃铛。
RETURN 1


;IMAGE_I70_特殊表情(CHARA)--------------------------------------------------------------------
;角色特有の表情表示条件
;时间停止中でも维持する表情はこちら
;使用时注意点:
;	RESULTS:0に表情を格纳
;	RESULTS:1にマウスオーバー时の表情を格纳すること
;	"通常","笑颜","愤怒","发情","睡眠"は本编で实装済み
;-------------------------------------------------------------------------------------------
@IMAGE_I70_特殊表情(CHARA)
#DIM CHARA

IF CFLAG:CHARA:应用差分替换
	;替换用画像
	IF FLAG:立绘种类
		;颜绘
	ELSE
		;立绘
	ENDIF
ELSE
	;通常画像
	IF FLAG:立绘种类
		;颜绘
		IF !CHECK_NUDE(CHARA)
			;just finished a battle with master and won
			IF PREVCOM == 480 && TFLAG:103 == CHARA && TCVAR:CHARA:弹幕胜负结果 <= 0
				RESULTS:0 = YAN
				RESULTS:1 = 笑颜
			ENDIF
		ENDIF
	ELSE
		;立绘
	ENDIF
ENDIF
;IMAGE_I70_特殊服装(CHARA)-------------------------------------------------------------------
;キャラクター特有の服装表示条件
;エフェクトを共有する服装差分はこちらで指定する
;使用时注意点:
;	RESULTSに特殊服装を格纳すること
;	书式は %服装%%特殊服装% 适宜"_"等入れること
;-------------------------------------------------------------------------------------------
@IMAGE_I70_特殊服装(CHARA)
#DIM CHARA
VARSET RESULTS

IF CFLAG:CHARA:应用差分替换 == 1
	;差し替え画像
	IF FLAG:立绘种类
		;颜绘
	ELSE
		;立绘
	ENDIF
ELSE
	;通常画像
	IF FLAG:立绘种类
		IF !CHECK_NUDE(CHARA)
			;just finished a battle with master and won
			IF PREVCOM == 480 && TFLAG:103 == CHARA && TCVAR:CHARA:弹幕胜负结果 <= 0 ; working set doesn't have a yandere expression
			ELSEIF WORKING(CHARA) ;at work, not nude, facial, default pic
				RESULTS = _メガネ
			ENDIF
		ENDIF
	ELSE
		;立绘
	ENDIF
ENDIF

IF WORKING(CHARA)
	CFLAG:CHARA:201 = 1
	EQUIP:CHARA:饰品 = 1
ELSE
	CFLAG:CHARA:201 = 0
	EQUIP:CHARA:饰品 = 0
ENDIF
;********************************************************************************************
@IMAGE_EX_I70_PREリソースチェック(CHARA, 表示形式, 服装, 表情, 表情_マウスオーバー, 差分, 乳量)
#DIM CHARA
#DIMS 表示形式
#DIMS 服装
#DIMS 表情
#DIMS 表情_マウスオーバー
#DIMS 差分
#DIMS 乳量

SIF CFLAG:CHARA:应用差分替换 == 1
	CALL COMPOSE_SPRITE_PRESET(CHARA)