@DAILY_CONDITION_EV13
#DIM 对象

SIF RAND:(10 - TALENT:MASTER:色欲察觉)> 0
	RETURN -1
对象 = 0
CALL FISHER_YATES_SHAFFLE(CHARANUM - 1)
FOR LOCAL:10, 0, CHARANUM - 1
	LOCAL = SHAFFLE_ARRAY:(LOCAL:10) + 1
	SIF !CFLAG:LOCAL:神社在住
		CONTINUE
	IF CFLAG:LOCAL:积攒度 >  700
		对象 = LOCAL
		BREAK
	ENDIF
NEXT
SIF !对象
	RETURN -1

CALL DAILY_EV13(对象)
RETURN 1


@DAILY_EV13(ARG)
PRINTFORMW 在半夜醒了过来
PRINTFORMW 想继续睡觉但是怎么也睡不着了……
CALL ASK_YN("散步","待在房间")
IF RESULT
	PRINTFORM %NAME_OUTPUT_TRANSLATION("CALLNAME", MASTER)%直到重新发困为止
	PRINTDATA
		DATAFORM 整理・鉴赏着自己的收藏
		DATAFORM 看着美丽的胖次陷入了沉思
		DATAFORM 看着墙上的斑点发愣
	ENDDATA
	PRINTFORML ……
	CALL COLORMESSAGE("心灵受到了洗礼、气力和TSP暂时的增加了",C_YELLOW,2,1)
	CALL BUFF_BASE(MASTER,BASE_气力,100)
	CALL BUFF_BASE(MASTER,BASE_TSP,100)
ELSEIF !RAND:4 || (!RAND:4 && ABL:ARG:亲密 >= 7)
	PRINTFORMW 随意散步了一会、\@ CFLAG:MASTER:311 == CFLAG:ARG:311 ? 在打算回自己房间的时候、听到了什么声音# 想着差不多该回去的时候、听到从%NAME_OUTPUT_TRANSLATION("CALLNAME", ARG)%的房间传来了什么声音于是打算去看看\@
	CALL ONABARE(ARG)
	CFLAG:MASTER:彻夜 = 1
ELSEIF ITEM:兔符「开运大纹」
		PRINTL 咦、开运大纹的样子…？
		CALL ASK_YN("取出","置之不理")
		IF !RESULT
			ITEM:兔符「开运大纹」 --
			SETCOLOR COLOR ("YELLOW")
			PRINTFORMW 兔符「开运大纹」发动！
			RESETCOLOR
			PRINTFORMW 随意散步了一会、\@ CFLAG:MASTER:311 == CFLAG:ARG:311 ? 在打算回自己房间的时候、听到了什么声音# 想着差不多该回去的时候、听到从%NAME_OUTPUT_TRANSLATION("CALLNAME", ARG)%的房间传来了什么声音于是打算去看看\@
			CALL ONABARE(ARG)
			CFLAG:MASTER:彻夜 = 1
		ELSEIF !RAND:2
			GOTO OYATU
		ELSE
			GOTO NANIMONAI
		ENDIF
ELSEIF !RAND:2
	$OYATU
	PRINTFORML 肚子饿了。而且如果吃了什么可能会犯困。
	PRINTFORMW %NAME_OUTPUT_TRANSLATION("CALLNAME", MASTER)%到厨房的柜子、找到一个好吃的东西。
	PRINTFORMW 不是%NAME_OUTPUT_TRANSLATION("CALLNAME", MASTER)%买的东西、但是%NAME_OUTPUT_TRANSLATION("CALLNAME", MASTER)%自己买了忘了也不是不可能
	PRINTFORML 
	CALL COLORMESSAGE("怎么办，保质期马上就要到了！有没有人快点来吃掉我啊！",C_YELLOW,2,2)
	PRINTFORML 
	PRINTFORML 虽然只是小小的点心、还是请你收下把。
	CALL BUFF_BASE(MASTER,BASE_气力,300)
	CALL COLORMESSAGE("吃了好吃的点心、一段时间内气力增加",C_YELLOW,2,1)
	PRINTFORMW %NAME_OUTPUT_TRANSLATION("CALLNAME", MASTER)%重新刷牙、高兴地睡着了。
	CALL COLORMESSAGE(@"不知道为什么、%NAME_OUTPUT_TRANSLATION("CALLNAME", ARG)%的心情变差了……",C_RED,2,1)
	TALENT:ARG:心情 = -1
ELSE
	$NANIMONAI
	PRINTFORMW 出去闲逛后发现好像没有什么有趣的事、于是决定老老实实的回去睡觉了
	CFLAG:MASTER:就寝时间 += 60
	CALL COLORMESSAGE("睡眠时间减少了1小时",C_YELLOW,2,1)
ENDIF
