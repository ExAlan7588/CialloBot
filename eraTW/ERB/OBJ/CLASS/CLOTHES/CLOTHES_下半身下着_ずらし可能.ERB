;要素名→ID变换用函数の存否确认用函数
;变换可能（IDと要素名の关系が常に变化しない）なら设置
;状况によって名前が变わるようなのは不可能
;WEAPONIDは变换可能
@EXIST_下半身内衣_ずらし可能_ID
#LOCALSIZE 1
#LOCALSSIZE 1

;要素名→ID变换用函数
;必要なら各オブジェクトで同样に作成のこと
@下半身内衣_ずらし可能_ID(ARG:0, ARGS:0, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS V_NAME                  ;作成する变量名
#DIMS C_NAME, OBJ_ID_LAST + 1 ;データ保持
#DIM  O_ID                    ;参照用
IF ARGS:0 == "RESET"
	VARSET C_NAME
	FOR O_ID, 1, OBJ_ID_LAST + 1
		C_NAME:O_ID = %GET_STR(, "下半身内衣_ずらし可能", O_ID, "名前")%
	NEXT
ELSE
	O_ID = FINDELEMENT(C_NAME, ESCAPE(ARGS:0), , , 1)
	CALLF MAKE_INT(V_NAME, O_ID)
ENDIF



;存否函数
;F函数はTRYCALL系が使えないので通常函数で存否配列を作っちゃう
@EXIST_下半身内衣_ずらし可能0
#LOCALSIZE 1
#LOCALSSIZE 1

;オブジェクト本体
@下半身内衣_ずらし可能0(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	;STR型の一时变量を定义する命令
	CALLF MAKE_STR(V_NAME, "没穿内裤")
CASE "能穿"
;没穿内裤を履くとは
	SIF (TALENT:ARG:风骚 || TALENT:ARG:小恶魔 || TALENT:ARG:炮友 || ABL:ARG:露出癖 >= 8) && !RAND:5
		CALLF MAKE_INT(V_NAME, 1)
CASE "关联内衣"
	CALLF MAKE_INT(V_NAME, 0)
ENDSELECT



@EXIST_下半身内衣_ずらし可能1
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身内衣_ずらし可能1(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "四角裤")
CASE "描写"
	CALLF MAKE_STR(V_NAME, "男式")
CASE "半透明"
	CALLF MAKE_INT(V_NAME, 1)
CASE "换装可能"
	CALLF MAKE_INT(V_NAME, 1)
CASE "关联内衣"
	CALLF MAKE_INT(V_NAME, CLOTHES_ID("上半身内衣１", "Ｔ恤衬衫"))
ENDSELECT



@EXIST_下半身内衣_ずらし可能2

@下半身内衣_ずらし可能2(ARG, O_DATA, V_NAME)
#FUNCTION
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "灯笼裤")
CASE "描写"
	CALLF MAKE_STR(V_NAME, "构造宽松的")
CASE "半透明(前)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "半透明(后)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "能穿"
	SIF !RAND:(1 + (2 * 胜负内衣判定(ARG)))
		CALLF MAKE_INT(V_NAME, 1)
CASE "关联内衣"
	CALLF MAKE_INT(V_NAME, 0)

CASE "收藏表示可能"
	CALLF MAKE_INT(V_NAME, 1)
ENDSELECT



@EXIST_下半身内衣_ずらし可能3
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身内衣_ずらし可能3(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "小熊胖次")
CASE "描写"
	CALLF MAKE_STR(V_NAME, "印着吉祥物的可爱的")
CASE "半透明(前)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "半透明(后)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "能穿"
	IF TALENT:ARG:小恶魔 || TALENT:ARG:炮友
	ELSEIF TALENT:ARG:幼稚 || TALENT:ARG:幼儿／幼儿退行 || TALENT:ARG:无知
		CALLF MAKE_INT(V_NAME, 1)
	ENDIF
CASE "关联内衣"
	CALLF MAKE_INT(V_NAME, 0)

CASE "收藏表示可能"
	IF TALENT:ARG:小恶魔 || TALENT:ARG:炮友
	ELSEIF TALENT:ARG:幼稚 || TALENT:ARG:幼儿／幼儿退行 || TALENT:ARG:无知
		CALLF MAKE_INT(V_NAME, 1)
	ENDIF
ENDSELECT



@EXIST_下半身内衣_ずらし可能4
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身内衣_ずらし可能4(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "波点胖次")
CASE "描写"
	CALLF MAKE_STR(V_NAME, "孩子气的")
CASE "半透明(前)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "半透明(后)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "能穿"
	IF TALENT:ARG:小恶魔 || TALENT:ARG:炮友
	ELSEIF TALENT:ARG:幼稚 || TALENT:ARG:幼儿／幼儿退行 || TALENT:ARG:体型 < 0 || TALENT:ARG:无知
		CALLF MAKE_INT(V_NAME, 1)
	ENDIF
CASE "关联内衣"
	CALLF MAKE_INT(V_NAME, 0)

CASE "收藏表示可能"
	IF TALENT:ARG:小恶魔 || TALENT:ARG:炮友
	ELSEIF TALENT:ARG:幼稚 || TALENT:ARG:幼儿／幼儿退行 || TALENT:ARG:体型 < 0 || TALENT:ARG:无知
		CALLF MAKE_INT(V_NAME, 1)
	ENDIF
ENDSELECT



@EXIST_下半身内衣_ずらし可能5
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身内衣_ずらし可能5(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "条纹胖次")
CASE "描写"
	CALLF MAKE_STR(V_NAME, "凸显出臀部曲线的")
CASE "半透明(前)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "半透明(后)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "能穿"
	SIF !RAND:(1 + (2 * 胜负内衣判定(ARG)))
		CALLF MAKE_INT(V_NAME, 1)
CASE "关联内衣"
	CALLF MAKE_INT(V_NAME, 0)

CASE "收藏表示可能"
	CALLF MAKE_INT(V_NAME, 1)
ENDSELECT



@EXIST_下半身内衣_ずらし可能6
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身内衣_ずらし可能6(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "褶边胖次")
CASE "描写"
	CALLF MAKE_STR(V_NAME, "装饰着可爱褶边的")
CASE "半透明(前)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "半透明(后)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "能穿"
	IF TALENT:ARG:小恶魔 || TALENT:ARG:炮友
	ELSEIF TALENT:ARG:幼稚 || TALENT:ARG:幼儿／幼儿退行 || TALENT:ARG:体型 < 0 || TALENT:ARG:无知
		CALLF MAKE_INT(V_NAME, 1)
	ENDIF
CASE "关联内衣"
	CALLF MAKE_INT(V_NAME, 0)

CASE "收藏表示可能"
	IF TALENT:ARG:小恶魔 || TALENT:ARG:炮友
	ELSEIF TALENT:ARG:幼稚 || TALENT:ARG:幼儿／幼儿退行 || TALENT:ARG:体型 < 0 || TALENT:ARG:无知
		CALLF MAKE_INT(V_NAME, 1)
	ENDIF
ENDSELECT



@EXIST_下半身内衣_ずらし可能7
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身内衣_ずらし可能7(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "纯色胖次")
CASE "描写"
	CALLF MAKE_STR(V_NAME, "毫不做作的")
CASE "半透明(前)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "半透明(后)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "能穿"
	IF TALENT:ARG:小恶魔 || TALENT:ARG:炮友
	ELSEIF !RAND:(1 + (2 * 胜负内衣判定(ARG)))
		CALLF MAKE_INT(V_NAME, 1)
	ENDIF
CASE "关联内衣"
	CALLF MAKE_INT(V_NAME, 0)

CASE "收藏表示可能"
	IF TALENT:ARG:小恶魔 || TALENT:ARG:炮友
	ELSE
		CALLF MAKE_INT(V_NAME, 1)
	ENDIF
ENDSELECT



@EXIST_下半身内衣_ずらし可能10
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身内衣_ずらし可能10(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "低腰胖次")
CASE "描写"
	CALLF MAKE_STR(V_NAME, "设计质朴的")
CASE "半透明(前)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "半透明(后)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "能穿"
	IF !RAND:(1 + (2 * 胜负内衣判定(ARG)))
		CALLF MAKE_INT(V_NAME, 1)
	ENDIF
CASE "关联内衣"
	CALLF MAKE_INT(V_NAME, 0)

CASE "收藏表示可能"
	CALLF MAKE_INT(V_NAME, 1)
ENDSELECT



@EXIST_下半身内衣_ずらし可能11
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身内衣_ずらし可能11(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "低腰紧身胖次")
CASE "描写"
	CALLF MAKE_STR(V_NAME, "仿佛能看到股沟的")
CASE "半透明(前)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "半透明(后)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "能穿"
	SIF !RAND:(2 - TALENT:ARG:恋慕)
		CALLF MAKE_INT(V_NAME, 1)
CASE "关联内衣"
	CALLF MAKE_INT(V_NAME, 0)

CASE "收藏表示可能"
	CALLF MAKE_INT(V_NAME, 1)
ENDSELECT



@EXIST_下半身内衣_ずらし可能12
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身内衣_ずらし可能12(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "高开叉胖次")
CASE "描写"
	CALLF MAKE_STR(V_NAME, "格外修长的")
CASE "半透明(前)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "半透明(后)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "能穿"
	SIF !RAND:(2 - TALENT:ARG:恋慕)
		CALLF MAKE_INT(V_NAME, 1)
CASE "关联内衣"
	SELECTCASE RAND:(1 + 胜负内衣判定(ARG))
		CASE 1
			CALLF MAKE_INT(V_NAME, CLOTHES_ID("上半身内衣１", "背带裙衣"))
		CASEELSE
			CALLF MAKE_INT(V_NAME, 0)
	ENDSELECT

CASE "收藏表示可能"
	CALLF MAKE_INT(V_NAME, 1)
ENDSELECT



@EXIST_下半身内衣_ずらし可能13
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身内衣_ずらし可能13(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "蕾丝胖次（白）")
CASE "描写"
	CALLF MAKE_STR(V_NAME, "整洁并有些透明的")
CASE "半透明(前)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "半透明(后)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "能穿"
	IF !(TALENT:ARG:小恶魔 || TALENT:ARG:恋慕) && (TALENT:ARG:幼稚 || TALENT:ARG:幼儿／幼儿退行 || TALENT:ARG:体型 < 0 || TALENT:ARG:无知)
		CALLF MAKE_INT(V_NAME, 0)
	ELSEIF !RAND:(3 - TALENT:ARG:恋慕 - 胜负内衣判定(ARG))
		CALLF MAKE_INT(V_NAME, 1)
	ENDIF
CASE "关联内衣"
	SELECTCASE (RAND:2 + 胜负内衣判定(ARG))
		CASE 2
			CALLF MAKE_INT(V_NAME, CLOTHES_ID("上半身内衣１", "背带裙衣"))
		CASE 1
			CALLF MAKE_INT(V_NAME, CLOTHES_ID("上半身内衣１", "蕾丝胸罩（白）"))
		CASEELSE
			CALLF MAKE_INT(V_NAME, 0)
	ENDSELECT

CASE "收藏表示可能"
	IF !(TALENT:ARG:小恶魔 || TALENT:ARG:恋慕) && (TALENT:ARG:幼稚 || TALENT:ARG:幼儿／幼儿退行 || TALENT:ARG:体型 < 0 || TALENT:ARG:无知)
	ELSE
		CALLF MAKE_INT(V_NAME, 1)
	ENDIF
CASE "收藏解禁条件"
	CALLF MAKE_STR(V_NAME, "解禁：恋慕")
ENDSELECT



@EXIST_下半身内衣_ずらし可能14
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身内衣_ずらし可能14(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	;STR型の一时变量を定义する命令
	CALLF MAKE_STR(V_NAME, "蕾丝胖次（黑）")
CASE "描写"
	CALLF MAKE_STR(V_NAME, "充满成熟女人韵味的")
CASE "半透明(前)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "半透明(后)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "能穿"
	IF !(TALENT:ARG:小恶魔 || TALENT:ARG:恋慕) && (TALENT:ARG:幼稚 || TALENT:ARG:幼儿／幼儿退行 || TALENT:ARG:体型 < 0 || TALENT:ARG:无知)
		CALLF MAKE_INT(V_NAME, 0)
	ELSEIF !RAND:(3 - TALENT:ARG:恋慕 - 胜负内衣判定(ARG))
		CALLF MAKE_INT(V_NAME, 1)
	ENDIF
CASE "关联内衣"
	SELECTCASE (RAND:2 + 胜负内衣判定(ARG))
		CASE 2
			CALLF MAKE_INT(V_NAME, CLOTHES_ID("上半身内衣１", "背带裙衣"))
		CASE 1
			CALLF MAKE_INT(V_NAME, CLOTHES_ID("上半身内衣１", "蕾丝胸罩（黑）"))
		CASEELSE
			CALLF MAKE_INT(V_NAME, 0)
	ENDSELECT

CASE "收藏表示可能"
	IF !(TALENT:ARG:小恶魔 || TALENT:ARG:恋慕) && (TALENT:ARG:幼稚 || TALENT:ARG:幼儿／幼儿退行 || TALENT:ARG:体型 < 0 || TALENT:ARG:无知)
	ELSE
		CALLF MAKE_INT(V_NAME, 1)
	ENDIF
CASE "收藏解禁条件"
	CALLF MAKE_STR(V_NAME, "解禁：恋慕")
ENDSELECT



@EXIST_下半身内衣_ずらし可能15
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身内衣_ずらし可能15(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "Ｔ字裤")
CASE "描写"
	CALLF MAKE_STR(V_NAME, "裸露大片臀部的大胆的")
CASE "半透明(前)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "半透明(后)"
	CALLF MAKE_INT(V_NAME, 0)
CASE "能穿"
	SIF (TALENT:ARG:风骚 || TALENT:ARG:小恶魔 || TALENT:ARG:恋慕) && !RAND:(3 -  TALENT:ARG:恋慕 - 胜负内衣判定(ARG))
		CALLF MAKE_INT(V_NAME, 1)
CASE "关联内衣"
	SELECTCASE RAND:(1 + 胜负内衣判定(ARG))
		CASE 1
			CALLF MAKE_INT(V_NAME, CLOTHES_ID("上半身内衣１", "背带裙衣"))
		CASEELSE
			CALLF MAKE_INT(V_NAME, 0)
	ENDSELECT

CASE "收藏表示可能"
	SIF TALENT:ARG:风骚 || TALENT:ARG:小恶魔 || TALENT:ARG:恋慕
		CALLF MAKE_INT(V_NAME, 1)
CASE "收藏解禁条件"
	CALLF MAKE_STR(V_NAME, "解禁：恋慕")
ENDSELECT



@EXIST_下半身内衣_ずらし可能16
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身内衣_ずらし可能16(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "绳系胖次")
CASE "描写"
	CALLF MAKE_STR(V_NAME, "用细绳系在腰上的")
CASE "半透明(前)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "半透明(后)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "能穿"
	SIF (TALENT:ARG:风骚 || TALENT:ARG:小恶魔 || TALENT:ARG:恋慕) && !RAND:(2 -  TALENT:ARG:恋慕)
		CALLF MAKE_INT(V_NAME, 1)
CASE "关联内衣"
	SELECTCASE RAND:(1 + 胜负内衣判定(ARG))
		CASE 1
			CALLF MAKE_INT(V_NAME, CLOTHES_ID("上半身内衣１", "背带裙衣"))
		CASEELSE
			CALLF MAKE_INT(V_NAME, 0)
	ENDSELECT

CASE "收藏表示可能"
	SIF TALENT:ARG:风骚 || TALENT:ARG:小恶魔 || TALENT:ARG:恋慕
		CALLF MAKE_INT(V_NAME, 1)
CASE "收藏解禁条件"
	CALLF MAKE_STR(V_NAME, "解禁：恋慕")
ENDSELECT



@EXIST_下半身内衣_ずらし可能17
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身内衣_ずらし可能17(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "皮革胖次")
CASE "描写"
	CALLF MAKE_STR(V_NAME, "缝着竖向拉链的")
CASE "半透明(前)"
	CALLF MAKE_INT(V_NAME, 0)
CASE "半透明(后)"
	CALLF MAKE_INT(V_NAME, 0)
CASE "能穿"
	SIF (TALENT:ARG:风骚 || TALENT:ARG:小恶魔 || TALENT:ARG:恋慕 || TALENT:ARG:炮友) && ABL:ARG:施虐属性 > 4 && !RAND:3
		CALLF MAKE_INT(V_NAME, 1)
CASE "关联内衣"
	IF !RAND:2
		CALLF MAKE_INT(V_NAME, CLOTHES_ID("上半身内衣１", "吊带式文胸"))
	ELSE
		CALLF MAKE_INT(V_NAME, 0)
	ENDIF

CASE "收藏表示可能"
	SIF TALENT:ARG:风骚 || TALENT:ARG:小恶魔 || TALENT:ARG:恋慕 || TALENT:ARG:炮友
		CALLF MAKE_INT(V_NAME, 1)
CASE "收藏解禁条件"
	IF (TALENT:ARG:风骚 || TALENT:ARG:小恶魔 || TALENT:ARG:恋慕 || TALENT:ARG:炮友) && ABL:ARG:施虐属性 <= 4
		CALLF MAKE_STR(V_NAME, "解禁：施虐属性5")
	ELSE
		CALLF MAKE_STR(V_NAME, "解禁：恋慕")
	ENDIF
ENDSELECT



@EXIST_下半身内衣_ずらし可能18
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身内衣_ずらし可能18(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, @"%UNIQUE_UNDERWARE_BOTTOM(ARG)%")
CASE "描写"
	CALLF MAKE_STR(V_NAME, @"%UNIQUE_UNDERWARE_ADJ(ARG)%")
CASE "半透明(前)"
	CALLF MAKE_INT(V_NAME, UNIQUE_UNDERWARE_TRANSPARENCY_FRONT(ARG))
CASE "半透明(后)"
	CALLF MAKE_INT(V_NAME, UNIQUE_UNDERWARE_TRANSPARENCY_BACK(ARG))
CASE "能穿"
	CALLF MAKE_INT(V_NAME, UNIQUE_UNDERWARE_ABLE(ARG))
CASE "关联内衣"
	;固有ブラ
	IF GET_INT(ARG, "キャラデータ", ARG, "固有上半身内衣")
		CALLF MAKE_INT(V_NAME, 13)
	ELSE
		CALLF MAKE_INT(V_NAME, 0)
	ENDIF
;とりあえず今回は无条件で投げるけど本来はキャラデータ函数に渡して返回值を见るべき
;そうすると着用条件の有无まで设定できるが、再び全角色分の设定が必要になる
;そこまでするの大变でしょ
CASE "收藏表示可能"
	CALLF MAKE_INT(V_NAME, 1)
CASE "收藏解禁条件"
ENDSELECT



@EXIST_下半身内衣_ずらし可能19
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身内衣_ずらし可能19(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "三角胖次")
CASE "描写"
	CALLF MAKE_STR(V_NAME, "充满少女味的可爱的")
CASE "半透明(前)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "半透明(后)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "能穿"
	CALLF MAKE_INT(V_NAME, 1)
CASE "关联内衣"
	CALLF MAKE_INT(V_NAME, 0)

CASE "收藏表示可能"
	CALLF MAKE_INT(V_NAME, 1)
CASE "收藏解禁条件"
ENDSELECT



@EXIST_下半身内衣_ずらし可能20
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身内衣_ずらし可能20(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "超短胖次")
CASE "描写"
	CALLF MAKE_STR(V_NAME, "极为短小的")
CASE "半透明(前)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "半透明(后)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "能穿"
	SIF FLAG:性感胖次追加 && !RAND:(2 - TALENT:ARG:恋慕)
		CALLF MAKE_INT(V_NAME, 1)
CASE "关联内衣"
	SELECTCASE RAND:(1 + 胜负内衣判定(ARG))
		CASE 1
			CALLF MAKE_INT(V_NAME, CLOTHES_ID("上半身内衣１", "背带裙衣"))
		CASEELSE
			CALLF MAKE_INT(V_NAME, 0)
	ENDSELECT

CASE "收藏表示可能"
	SIF FLAG:性感胖次追加
		CALLF MAKE_INT(V_NAME, 1)
CASE "收藏解禁条件"
ENDSELECT



@EXIST_下半身内衣_ずらし可能21
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身内衣_ずらし可能21(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "兜裆布")
CASE "描写"
	CALLF MAKE_STR(V_NAME, "紧紧抱住屁股的")
CASE "半透明(前)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "半透明(后)"
	CALLF MAKE_INT(V_NAME, 0)
CASE "能穿"
	SIF FLAG:性感胖次追加 && !RAND:(3 + 胜负内衣判定(ARG))
		CALLF MAKE_INT(V_NAME, 1)
CASE "关联内衣"
	CALLF MAKE_INT(V_NAME, CLOTHES_ID("上半身内衣１", "裹胸"))

CASE "收藏表示可能"
	SIF FLAG:性感胖次追加
		CALLF MAKE_INT(V_NAME, 1)
CASE "收藏解禁条件"
ENDSELECT



@EXIST_下半身内衣_ずらし可能22
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身内衣_ずらし可能22(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "丁字裤")
CASE "描写"
	CALLF MAKE_STR(V_NAME, "面积极小简直跟绳一样的")
CASE "半透明(前)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "半透明(后)"
	CALLF MAKE_INT(V_NAME, 0)
CASE "能穿"
	IF !(FLAG:性感胖次追加 && ABL:ARG:露出癖 >= 4)
		CALLF MAKE_INT(V_NAME, 0)
	ELSEIF !(TALENT:ARG:风骚 || TALENT:ARG:小恶魔 || TALENT:ARG:恋慕 || TALENT:ARG:炮友) && (TALENT:ARG:幼稚 || TALENT:ARG:幼儿／幼儿退行 || TALENT:ARG:体型 < 0 || TALENT:ARG:无知)
		CALLF MAKE_INT(V_NAME, 0)
	ELSEIF !RAND:(3 - TALENT:ARG:恋慕 - 胜负内衣判定(ARG))
		CALLF MAKE_INT(V_NAME, 1)
	ENDIF
CASE "关联内衣"
	SELECTCASE RAND:(1 + 胜负内衣判定(ARG))
		CASE 1
			CALLF MAKE_INT(V_NAME, CLOTHES_ID("上半身内衣１", "背带裙衣"))
		CASEELSE
			CALLF MAKE_INT(V_NAME, 0)
	ENDSELECT

CASE "收藏表示可能"
	IF !(FLAG:性感胖次追加 && ABL:ARG:露出癖 >= 4)
		CALLF MAKE_INT(V_NAME, 0)
	ELSEIF !(TALENT:ARG:风骚 || TALENT:ARG:小恶魔 || TALENT:ARG:恋慕 || TALENT:ARG:炮友) && (TALENT:ARG:幼稚 || TALENT:ARG:幼儿／幼儿退行 || TALENT:ARG:体型 < 0 || TALENT:ARG:无知)
		CALLF MAKE_INT(V_NAME, 0)
	ELSE
		CALLF MAKE_INT(V_NAME, 1)
	ENDIF
CASE "收藏解禁条件"
	IF (TALENT:ARG:幼稚 || TALENT:ARG:幼儿／幼儿退行 || TALENT:ARG:体型 < 0 || TALENT:ARG:无知) && ABL:ARG:露出癖 >= 4
		CALLF MAKE_STR(V_NAME, "解禁：恋慕")
	ELSEIF ABL:ARG:露出癖 < 4
		CALLF MAKE_STR(V_NAME, "解禁：露出癖4")
	ENDIF
ENDSELECT



@EXIST_下半身内衣_ずらし可能23
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身内衣_ずらし可能23(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "透明胖次")
CASE "描写"
	CALLF MAKE_STR(V_NAME, "重要的部位能直接看见的")
CASE "半透明(前)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "半透明(后)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "能穿"
	IF !(FLAG:性感胖次追加 && ABL:ARG:露出癖 >= 4)
		CALLF MAKE_INT(V_NAME, 0)
	ELSEIF !(TALENT:ARG:风骚 || TALENT:ARG:小恶魔 || TALENT:ARG:恋慕 || TALENT:ARG:炮友) && (TALENT:ARG:幼稚 || TALENT:ARG:幼儿／幼儿退行 || TALENT:ARG:体型 < 0 || TALENT:ARG:无知)
		CALLF MAKE_INT(V_NAME, 0)
	ELSEIF !RAND:(3 - TALENT:ARG:恋慕 - 胜负内衣判定(ARG))
		CALLF MAKE_INT(V_NAME, 1)
	ENDIF
CASE "关联内衣"
	SELECTCASE (RAND:2 + 胜负内衣判定(ARG))
		CASE 2
			CALLF MAKE_INT(V_NAME, CLOTHES_ID("上半身内衣１", "背带裙衣"))
		CASE 1
			CALLF MAKE_INT(V_NAME, CLOTHES_ID("上半身内衣１", "透明胸罩"))
		CASEELSE
			CALLF MAKE_INT(V_NAME, 0)
	ENDSELECT

CASE "收藏表示可能"
	IF !(FLAG:性感胖次追加 && ABL:ARG:露出癖 >= 4)
		CALLF MAKE_INT(V_NAME, 0)
	ELSEIF !(TALENT:ARG:风骚 || TALENT:ARG:小恶魔 || TALENT:ARG:恋慕 || TALENT:ARG:炮友) && (TALENT:ARG:幼稚 || TALENT:ARG:幼儿／幼儿退行 || TALENT:ARG:体型 < 0 || TALENT:ARG:无知)
		CALLF MAKE_INT(V_NAME, 0)
	ELSE
		CALLF MAKE_INT(V_NAME, 1)
	ENDIF
CASE "收藏解禁条件"
	IF (TALENT:ARG:幼稚 || TALENT:ARG:幼儿／幼儿退行 || TALENT:ARG:体型 < 0 || TALENT:ARG:无知) && ABL:ARG:露出癖 >= 4
		CALLF MAKE_STR(V_NAME, "解禁：恋慕")
	ELSEIF ABL:ARG:露出癖 < 4
		CALLF MAKE_STR(V_NAME, "解禁：露出癖4")
	ENDIF
ENDSELECT



@EXIST_下半身内衣_ずらし可能24
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身内衣_ずらし可能24(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "开放式胖次")
CASE "描写"
	CALLF MAKE_STR(V_NAME, "清楚的暴露出耻丘的")
CASE "半透明(前)"
	CALLF MAKE_INT(V_NAME, 0)
CASE "半透明(后)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "能穿"
	IF !(FLAG:性感胖次追加 && ABL:ARG:露出癖 >= 4)
		CALLF MAKE_INT(V_NAME, 0)
	ELSEIF !(TALENT:ARG:风骚 || TALENT:ARG:小恶魔 || TALENT:ARG:恋慕 || TALENT:ARG:炮友) && (TALENT:ARG:幼稚 || TALENT:ARG:幼儿／幼儿退行 || TALENT:ARG:体型 < 0 || TALENT:ARG:无知)
		CALLF MAKE_INT(V_NAME, 0)
	ELSEIF !RAND:(4 - TALENT:ARG:恋慕 - (2 * 胜负内衣判定(ARG)))
		CALLF MAKE_INT(V_NAME, 1)
	ENDIF
CASE "关联内衣"
	SELECTCASE (RAND:2 + 胜负内衣判定(ARG))
		CASE 2
			CALLF MAKE_INT(V_NAME, CLOTHES_ID("上半身内衣１", "背带裙衣"))
		CASE 1
			CALLF MAKE_INT(V_NAME, CLOTHES_ID("上半身内衣１", "露两点胸罩"))
		CASEELSE
			CALLF MAKE_INT(V_NAME, 0)
	ENDSELECT

CASE "收藏表示可能"
	IF !(FLAG:性感胖次追加 && ABL:ARG:露出癖 >= 4)
		CALLF MAKE_INT(V_NAME, 0)
	ELSEIF !(TALENT:ARG:风骚 || TALENT:ARG:小恶魔 || TALENT:ARG:恋慕 || TALENT:ARG:炮友) && (TALENT:ARG:幼稚 || TALENT:ARG:幼儿／幼儿退行 || TALENT:ARG:体型 < 0 || TALENT:ARG:无知)
		CALLF MAKE_INT(V_NAME, 0)
	ELSE
		CALLF MAKE_INT(V_NAME, 1)
	ENDIF
CASE "收藏解禁条件"
	IF (TALENT:ARG:幼稚 || TALENT:ARG:幼儿／幼儿退行 || TALENT:ARG:体型 < 0 || TALENT:ARG:无知) && ABL:ARG:露出癖 >= 4
		CALLF MAKE_STR(V_NAME, "解禁：恋慕")
	ELSEIF ABL:ARG:露出癖 < 4
		CALLF MAKE_STR(V_NAME, "解禁：露出癖4")
	ENDIF
ENDSELECT



@EXIST_下半身内衣_ずらし可能25
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身内衣_ずらし可能25(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "创口贴")
CASE "描写"
	CALLF MAKE_STR(V_NAME, "沿着裂缝轻轻贴上的")
CASE "半透明(前)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "半透明(后)"
	CALLF MAKE_INT(V_NAME, 0)
CASE "能穿"
	IF !(FLAG:性感胖次追加 && ABL:ARG:露出癖 >= 4)
		CALLF MAKE_INT(V_NAME, 0)
	ELSEIF (TALENT:ARG:幼稚 || TALENT:ARG:幼儿／幼儿退行 || TALENT:ARG:体型 < 0 || TALENT:ARG:无知) && !RAND:3
		CALLF MAKE_INT(V_NAME, 1)
	ELSE
		CALLF MAKE_INT(V_NAME, 0)
	ENDIF
CASE "关联内衣"
	CALLF MAKE_INT(V_NAME, CLOTHES_ID("上半身内衣１", "创口贴"))

CASE "收藏表示可能"
	IF !(FLAG:性感胖次追加 && ABL:ARG:露出癖 >= 4)
		CALLF MAKE_INT(V_NAME, 0)
	ELSEIF (TALENT:ARG:幼稚 || TALENT:ARG:幼儿／幼儿退行 || TALENT:ARG:体型 < 0 || TALENT:ARG:无知)
		CALLF MAKE_INT(V_NAME, 1)
	ELSE
		CALLF MAKE_INT(V_NAME, 0)
	ENDIF
CASE "收藏解禁条件"
	IF ABL:ARG:露出癖 < 4
		CALLF MAKE_STR(V_NAME, "解禁：露出癖4")
	ENDIF
ENDSELECT



@EXIST_下半身内衣_ずらし可能26
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身内衣_ずらし可能26(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "拉链（屁股处可开）")
CASE "描写"
	CALLF MAKE_STR(V_NAME, "在胯股间打了开大口的排便用")
CASE "半透明(前)"
	CALLF MAKE_INT(V_NAME, 0)
CASE "半透明(后)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "能穿"
	IF FLAG:性感胖次追加 && (TALENT:ARG:炮友 && ABL:ARG:露出癖 >= 5 || TALENT:ARG:无知) && !RAND:4
		CALLF MAKE_INT(V_NAME, 1)
	ELSE
		CALLF MAKE_INT(V_NAME, 0)
	ENDIF
CASE "关联内衣"
	CALLF MAKE_INT(V_NAME, 0)

CASE "收藏表示可能"
	IF !FLAG:性感胖次追加
		CALLF MAKE_INT(V_NAME, 0)
	ELSEIF (TALENT:ARG:炮友 && ABL:ARG:露出癖 >= 5 || TALENT:ARG:无知)
		CALLF MAKE_INT(V_NAME, 1)
	ELSE
		CALLF MAKE_INT(V_NAME, 0)
	ENDIF
CASE "收藏解禁条件"
	IF TALENT:ARG:无知
	ELSEIF TALENT:ARG:炮友 && ABL:ARG:露出癖 >= 5
	ELSEIF TALENT:ARG:炮友
		CALLF MAKE_STR(V_NAME, "解禁：露出癖5")
	ELSEIF ABL:ARG:露出癖 >= 5
		CALLF MAKE_STR(V_NAME, "解禁：炮友")
	ENDIF
ENDSELECT



@EXIST_下半身内衣_ずらし可能27
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身内衣_ずらし可能27(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "贞操带")
CASE "描写"
	CALLF MAKE_STR(V_NAME, "挂着牢固的锁的金属制的")
CASE "半透明(前)"
	CALLF MAKE_INT(V_NAME, 0)
CASE "半透明(后)"
	CALLF MAKE_INT(V_NAME, 0)
CASE "关联内衣"
	CALLF MAKE_INT(V_NAME, CLOTHES_ID("上半身内衣１", "贞操带胸罩"))
ENDSELECT



@EXIST_下半身内衣_ずらし可能28
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身内衣_ずらし可能28(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "触手胖次")
CASE "描写"
	CALLF MAKE_STR(V_NAME, "内侧有什么在蠢动着的")
CASE "半透明(前)"
	CALLF MAKE_INT(V_NAME, 0)
CASE "半透明(后)"
	CALLF MAKE_INT(V_NAME, 0)
CASE "关联内衣"
	CALLF MAKE_INT(V_NAME, CLOTHES_ID("上半身内衣１", "触手胸罩"))
ENDSELECT



@EXIST_下半身内衣_ずらし可能29
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身内衣_ずらし可能29(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "振动棒胖次")
CASE "描写"
	CALLF MAKE_STR(V_NAME, "放入円筒状异物的")
CASE "半透明(前)"
	CALLF MAKE_INT(V_NAME, 0)
CASE "半透明(后)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "关联内衣"
	CALLF MAKE_INT(V_NAME, 0)
ENDSELECT



@EXIST_下半身内衣_ずらし可能30
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身内衣_ずらし可能30(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "二穴振动棒胖次")
CASE "描写"
	CALLF MAKE_STR(V_NAME, "两根棒子在腿间嗡嗡作响的")
CASE "半透明(前)"
	CALLF MAKE_INT(V_NAME, 0)
CASE "半透明(后)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "关联内衣"
	CALLF MAKE_INT(V_NAME, 0)
ENDSELECT



@EXIST_下半身内衣_ずらし可能31
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身内衣_ずらし可能31(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "比基尼内袴")
;下半身内衣２の20
CASE "换装可能"
	CALLF MAKE_INT(V_NAME, 1)
CASE "半透明(前)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "半透明(后)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "关联内衣"
	CALLF MAKE_INT(V_NAME, CLOTHES_ID("上半身内衣１", "比基尼文胸"))
ENDSELECT



@EXIST_下半身内衣_ずらし可能32
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身内衣_ずらし可能32(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "微型内裤")
;下半身内衣２の21
CASE "换装可能"
	CALLF MAKE_INT(V_NAME, 1)
CASE "半透明(前)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "半透明(后)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "关联内衣"
	CALLF MAKE_INT(V_NAME, CLOTHES_ID("上半身内衣１", "微型管式胸罩"))
ENDSELECT

@胜负内衣判定(ARG)
#FUNCTION
IF !TALENT:ARG:无知
	SIF (CFLAG:ARG:既成事实 & 合意_诶嘿嘿 || TALENT:ARG:恋慕) && CFLAG:ARG:翌日来访
		RETURNF 1
	IF DAY:2 == 1
		SELECTCASE DAY:3
			CASE 14
				SIF TALENT:ARG:恋慕
					RETURNF 1
		ENDSELECT
	ELSEIF DAY:2 == 4
		SELECTCASE DAY:3
			CASE 14, 24, 25
				SIF TALENT:ARG:恋慕
					RETURNF 1
		ENDSELECT
	ENDIF
ENDIF
RETURNF 0

