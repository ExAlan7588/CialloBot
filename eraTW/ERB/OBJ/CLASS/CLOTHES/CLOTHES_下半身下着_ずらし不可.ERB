;下半身内衣_ずらし不可
;要素名→ID变换用函数の存否确认用函数
;变换可能（IDと要素名の关系が常に变化しない）なら设置
;状况によって名前が变わるようなのは不可能
@EXIST_下半身内衣_ずらし不可_ID
#LOCALSIZE 1
#LOCALSSIZE 1

;要素名→ID变换用函数
;必要なら各オブジェクトで同样に作成のこと
@下半身内衣_ずらし不可_ID(ARG:0, ARGS:0, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS V_NAME                  ;作成する变量名
#DIMS C_NAME, OBJ_ID_LAST + 1 ;データ保持
#DIM  O_ID                    ;参照用
IF ARGS:0 == "RESET"
	VARSET C_NAME
	FOR O_ID, 1, OBJ_ID_LAST + 1
		C_NAME:O_ID = %GET_STR(, "下半身内衣_ずらし不可", O_ID, "名前")%
	NEXT
ELSE
	O_ID = FINDELEMENT(C_NAME, ESCAPE(ARGS:0), , , 1)
	CALLF MAKE_INT(V_NAME, O_ID)
ENDIF



@EXIST_下半身内衣_ずらし不可1
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身内衣_ずらし不可1(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "兜裆布")
CASE "装备部位"
	CALLF MAKE_STR(V_NAME, "「下半身内衣１」")
;旧装备处理との照会用、下半身内衣１の1
CASE "半透明(前)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "半透明(后)"
	CALLF MAKE_INT(V_NAME, 0)
ENDSELECT


@EXIST_下半身内衣_ずらし不可2
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身内衣_ずらし不可2(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "Ｃ字袴")
CASE "装备部位"
	CALLF MAKE_STR(V_NAME, "「下半身内衣１」")
;旧装备处理との照会用、下半身内衣１の2
CASE "半透明(前)"
	CALLF MAKE_INT(V_NAME, 0)
CASE "半透明(后)"
	CALLF MAKE_INT(V_NAME, 0)
ENDSELECT


@EXIST_下半身内衣_ずらし不可3
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身内衣_ずらし不可3(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "创口贴")
CASE "装备部位"
	CALLF MAKE_STR(V_NAME, "「下半身内衣１」")
;旧装备处理との照会用、下半身内衣１の3
CASE "半透明(前)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "半透明(后)"
	CALLF MAKE_INT(V_NAME, 0)
ENDSELECT


@EXIST_下半身内衣_ずらし不可4
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身内衣_ずらし不可4(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "安全裤")
CASE "装备部位"
	CALLF MAKE_STR(V_NAME, "「下半身内衣１」")
;旧装备处理との照会用、下半身内衣１の4
CASE "半透明(前)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "半透明(后)"
	CALLF MAKE_INT(V_NAME, 1)
ENDSELECT


@EXIST_下半身内衣_ずらし不可5
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身内衣_ずらし不可5(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "贞操带")
CASE "装备部位"
	CALLF MAKE_STR(V_NAME, "「下半身内衣１」")
;旧装备处理との照会用、下半身内衣１の5
CASE "半透明(前)"
	CALLF MAKE_INT(V_NAME, 0)
CASE "半透明(后)"
	CALLF MAKE_INT(V_NAME, 0)
ENDSELECT


@EXIST_下半身内衣_ずらし不可6
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身内衣_ずらし不可6(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "阴唇环")
CASE "装备部位"
	CALLF MAKE_STR(V_NAME, "「下半身内衣１」")
;旧装备处理との照会用、下半身内衣１の6
CASE "半透明(前)"
	CALLF MAKE_INT(V_NAME, 0)
CASE "半透明(后)"
	CALLF MAKE_INT(V_NAME, 0)
ENDSELECT


@EXIST_下半身内衣_ずらし不可7
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身内衣_ずらし不可7(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "阴蒂环")
CASE "装备部位"
	CALLF MAKE_STR(V_NAME, "「下半身内衣１」")
;旧装备处理との照会用、下半身内衣１の7
CASE "半透明(前)"
	CALLF MAKE_INT(V_NAME, 0)
CASE "半透明(后)"
	CALLF MAKE_INT(V_NAME, 0)
ENDSELECT



