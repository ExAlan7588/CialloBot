;读书
@EXIST_FreeAct6
#LOCALSIZE 1
#LOCALSSIZE 1
;オブジェクト本体
@FreeAct6(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
#DIM 经过时间
经过时间 = Activity_Duration:TARGET
SELECTCASE O_DATA
CASE "名称"
	CALLF MAKE_STR(V_NAME, "询问阅读感想")
CASE "CHAIN"
;他角色に连锁する场合コメントアウト外すこと
;	CALLF MAKE_INT(V_NAME, 1)
CASE "经过时间"
;陪同行动指令での经过时间
;未入力だと最大30分
	IF Activity_Duration:TARGET > 30
		CALLF MAKE_INT(V_NAME, 10)
	ELSE
		CALLF MAKE_INT(V_NAME, 0)
	ENDIF
ENDSELECT

@自由行动内容_6(CHARA, ARGS, 时间系数, CHAIN)
#DIM CHARA
#DIM CHAIN
#DIM 现在地
#DIM メッセージ表示
#DIM 时间系数
#DIM 经过时间
现在地 = CFLAG:CHARA:当前位置
メッセージ表示 = 现在地 == CFLAG:MASTER:当前位置 ? 1 # 0
SELECTCASE ARGS
	CASE "发生"
		;教养1以上の角色が读书できる场所または自室にいる时
		SIF ABL:CHARA:教养 < 1
			RETURN 0
		SIF BASE:CHARA:气力 <= 500
			RETURN 0
		SIF !IS_LIBRARY(现在地, 1) && 现在地 != CFLAG:CHARA:神社在住
			RETURN 0
		SIF メッセージ表示
			PRINTFORMW %NAME_OUTPUT_TRANSLATION("CALLNAME", CHARA)%开始看书了。
		RETURN 60 + RAND:60
	CASE "行动中"
			IF 充填率(TARGET,1) >= 10 || 充填率(TARGET,2) >= 10
				SIF メッセージ表示
					PRINTFORMW %NAME_OUTPUT_TRANSLATION("CALLNAME", CHARA)%满脸通红地看着书。
			ELSE
				SIF メッセージ表示
					PRINTFORMW %NAME_OUTPUT_TRANSLATION("CALLNAME", CHARA)%在默默地看着书。
			ENDIF
	CASE "完了"
		EXP:CHARA:学习经验 += 1
		IF 充填率(CHARA,1) >= 10 || 充填率(CHARA,2) >= 10
			SIF メッセージ表示
				PRINTFORMW %NAME_OUTPUT_TRANSLATION("CALLNAME", CHARA)%看完书后吐着甜美的喘息。
		ELSE
			SIF メッセージ表示
				PRINTFORMW %NAME_OUTPUT_TRANSLATION("CALLNAME", CHARA)%似乎看完书了。
		ENDIF	
	CASE "中断"
		EXP:CHARA:学习经验 += 1
	CASE "交手"
		PRINTFORML %NAME_OUTPUT_TRANSLATION("CALLNAME", PLAYER)%向%NAME_OUTPUT_TRANSLATION("CALLNAME", TARGET)%询问了书的读后感
		IF TFLAG:193 < 0
			IF 充填率(CHARA,1) >= 150 || 充填率(CHARA,2) >= 150
				PRINTFORML %NAME_OUTPUT_TRANSLATION("CALLNAME", CHARA)%捂着嘴忍耐着体内\@ TALENT:MASTER:浓厚精液? 浓厚的 # 的\@精子的爱抚…香汗淋漓…
				PRINTFORML 脸涨得通红并且夹杂着甜美喘息…并没有在看书…
				PRINTFORML 捂着小腹忍耐着❤书都拿反了…
			ELSEIF 充填率(CHARA,1) >= 100 || 充填率(CHARA,2) >= 100
				PRINTFORML %NAME_OUTPUT_TRANSLATION("CALLNAME", CHARA)%正享受着体内满满\@ TALENT:MASTER:浓厚精液? 浓厚的 # 的\@精子的刺激轻轻娇喘……
				PRINTFORML 夹杂着甜美气息的脸涨得通红…眼神似乎没有聚焦在书上…
			ELSEIF 充填率(CHARA,1) >= 70 || 充填率(CHARA,2) >= 70
				PRINTFORML %NAME_OUTPUT_TRANSLATION("CALLNAME", CHARA)%正感受着体内大量\@ TALENT:MASTER:浓厚精液? 浓厚的 # 的\@精子的刺激……
				PRINTFORML 似乎还沉浸在书中无法回话的样子…喘息中似乎夹杂着甜美的气息…
			ELSEIF 充填率(CHARA,1) >= 40 || 充填率(CHARA,2) >= 40
				PRINTFORML %NAME_OUTPUT_TRANSLATION("CALLNAME", CHARA)%正被体内许多\@ TALENT:MASTER:浓厚精液? 浓厚的 # 的\@精子刺激着……
				PRINTFORML 似乎沉浸书中无法回话的样子…脸红红的…
			ELSEIF 充填率(CHARA,1) >= 10 || 充填率(CHARA,2) >= 10
				PRINTFORML %NAME_OUTPUT_TRANSLATION("CALLNAME", CHARA)%正被体内残存\@ TALENT:MASTER:浓厚精液? 浓厚的 # 的\@精子刺激着……
				PRINTFORML 似乎还沉浸在书中无法回话的样子…
			ELSE 
				PRINTFORML 似乎还沉浸在书中无法回话的样子…
			ENDIF	
		ELSE
			IF 充填率(CHARA,1) >= 150 || 充填率(CHARA,2) >= 150
				PRINTFORML %NAME_OUTPUT_TRANSLATION("CALLNAME", CHARA)%捂着嘴忍耐着体内\@ TALENT:MASTER:浓厚精液? 浓厚的 # 的\@精子的爱抚…香汗淋漓…
				PRINTFORML 脸涨得通红并且夹杂着甜美喘息摆着手、
				PRINTFORML 看来暂时没有余力好好讨论呢❤真可爱
			ELSEIF 充填率(CHARA,1) >= 100 || 充填率(CHARA,2) >= 100
				PRINTFORML %NAME_OUTPUT_TRANSLATION("CALLNAME", CHARA)%正享受着体内满满\@ TALENT:MASTER:浓厚精液? 浓厚的 # 的\@精子的刺激轻轻娇喘……
				PRINTFORML 夹杂着甜美娇喘的讨论格外热烈…但其实根本没注意互相说了什么…
			ELSEIF 充填率(CHARA,1) >= 70 || 充填率(CHARA,2) >= 70
				PRINTFORML %NAME_OUTPUT_TRANSLATION("CALLNAME", CHARA)%正感受着体内大量\@ TALENT:MASTER:浓厚精液? 浓厚的 # 的\@精子的刺激……
				PRINTFORML 和红着脸的%NAME_OUTPUT_TRANSLATION("CALLNAME", TARGET)%热烈地讨论起了书本的内容…回话夹杂着甜美的娇喘…
			ELSEIF 充填率(CHARA,1) >= 40 || 充填率(CHARA,2) >= 40
				PRINTFORML %NAME_OUTPUT_TRANSLATION("CALLNAME", CHARA)%正被体内许多\@ TALENT:MASTER:浓厚精液? 浓厚的 # 的\@精子刺激着……
				PRINTFORML 和红着脸的%NAME_OUTPUT_TRANSLATION("CALLNAME", TARGET)%热烈地讨论起了书本的内容…眼神有些飘忽…
			ELSEIF 充填率(CHARA,1) >= 10 || 充填率(CHARA,2) >= 10
				PRINTFORML %NAME_OUTPUT_TRANSLATION("CALLNAME", CHARA)%正被体内残存\@ TALENT:MASTER:浓厚精液? 浓厚的 # 的\@精子刺激着……
				PRINTFORML 和红着脸的%NAME_OUTPUT_TRANSLATION("CALLNAME", TARGET)%热烈地讨论起了书本的内容…
			ELSE
				PRINTFORML 和%NAME_OUTPUT_TRANSLATION("CALLNAME", TARGET)%热烈地讨论起了书本的内容…
			ENDIF		
			
		ENDIF
	CASE "ソース"
		;读み终わる直前まで闻けない
		IF Activity_Duration:TARGET > 30
			TFLAG:193 = -1
			DOWNBASE:MASTER:气力 += 40 * 时间系数
			SOURCE:反感 = 50
		ELSE
			DOWNBASE:MASTER:气力 += 20 * 时间系数
			DOWNBASE:TARGET:气力 += 20 * 时间系数
			SOURCE:欢乐 = (100 + ((ABL:亲密 + ABL:MASTER:教养) * 50)) * 时间系数
			SOURCE:被动 = (100 + ((ABL:顺从 + ABL:MASTER:教养) * 50)) * 时间系数
			TFLAG:98 = 1
			EXP:MASTER:会话经验 ++
			EXP:MASTER:学习经验 ++
		ENDIF
ENDSELECT