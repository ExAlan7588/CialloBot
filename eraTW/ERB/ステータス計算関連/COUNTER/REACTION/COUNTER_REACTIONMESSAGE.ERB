;被插入膣性交
@EVENT_COUNTE_MESSAGE5001
SELECTCASE TCVAR:MASTER:21
	CASE 1
		PRINTFORMW %NAME_OUTPUT_TRANSLATION("CALLNAME", MASTER)%的肉褶紧缠着%NAME_OUTPUT_TRANSLATION("CALLNAME", TARGET)%的%TINKO(TARGET)%、为了渴望的%SAMEN_DESCRIPTION(TARGET)%不停收缩…
	;だいしゅきホールド
	CASE 2
		PRINTFORMW %NAME_OUTPUT_TRANSLATION("CALLNAME", MASTER)%抱住%NAME_OUTPUT_TRANSLATION("CALLNAME", TARGET)%同时用两腿紧紧缠住了腰…
	;性交见せつけ
	CASE 3
		PRINTFORMW %NAME_OUTPUT_TRANSLATION("CALLNAME", MASTER)%抬起上身向%NAME_OUTPUT_TRANSLATION("CALLNAME", TARGET)%展示着结合部位并捋起了%TINKO(TARGET)%…
	CASE 4
		PRINTFORMW %NAME_OUTPUT_TRANSLATION("CALLNAME", MASTER)%那渴求孕育生命的子宫颤抖着降下来了❤…为了榨取%NAME_OUTPUT_TRANSLATION("CALLNAME", TARGET)%的精子，不受控制的拼命吮吸着
	CASE 5
		PRINTFORMW %NAME_OUTPUT_TRANSLATION("CALLNAME", MASTER)%无意识地仰起腰、迎合着%NAME_OUTPUT_TRANSLATION("CALLNAME", TARGET)%使其轻松地插入到了深处…
	CASE 6
		PRINTFORMW %NAME_OUTPUT_TRANSLATION("CALLNAME", MASTER)%的子宫口翕动着一张一合、渴求着%NAME_OUTPUT_TRANSLATION("CALLNAME", TARGET)%射精的冲击…
	CASE 7
		PRINTFORMW %NAME_OUTPUT_TRANSLATION("CALLNAME", MASTER)%五指交缠地握住了%NAME_OUTPUT_TRANSLATION("CALLNAME", TARGET)%的手…蜜穴收缩的力度越来越紧…
ENDSELECT
RETURN 1
;被插入肛门性交
@EVENT_COUNTE_MESSAGE5011
SELECTCASE TCVAR:MASTER:21
	CASE 1
		PRINTFORMW 装满热乎乎的粘液的%NAME_OUTPUT_TRANSLATION("CALLNAME", MASTER)%的肠内、肉壁紧紧缠住了%TINKO(TARGET)%…
	CASE 2
		PRINTFORMW %NAME_OUTPUT_TRANSLATION("CALLNAME", MASTER)%五指交缠地握住了%NAME_OUTPUT_TRANSLATION("CALLNAME", TARGET)%的手…菊穴收缩的力度越来越紧…
ENDSELECT
RETURN 1
;给对方手淫
@EVENT_COUNTE_MESSAGE5021
;现状许可を贳うには童贞丧失必须
;IF NOWEX:TARGET:射精 && TALENT:TARGET:非童贞 <= 0 && !TALENT:无知 && CFLAG:允许无套 == 2
SELECTCASE TCVAR:MASTER:21
	CASE 1
		IF NOWEX:TARGET:射精 && !TALENT:无知
			IF !RAND:2
				;PRINTFORML %NAME_OUTPUT_TRANSLATION("CALLNAME", MASTER)%跨坐在%NAME_OUTPUT_TRANSLATION("CALLNAME", TARGET)%身上、把膣穴压在%TINKO(TARGET)%之上，然后提高速度一口气把%TINKO(TARGET)%吞进了最深处…
				;这句怪怪的，不是用手吗，不知道原文是怎么回事，稍微改了一下
				PRINTFORML %NAME_OUTPUT_TRANSLATION("CALLNAME", MASTER)%跨坐在%NAME_OUTPUT_TRANSLATION("CALLNAME", TARGET)%身上、把膣穴压在%TINKO(TARGET)%之上，然后提高手上的速度专注地刺激%NAME_OUTPUT_TRANSLATION("CALLNAME", TARGET)%的%TINKO(TARGET)%的最前端…
			ELSE
		;ELSEIF NOWEX:TARGET:射精
				PRINTFORML %NAME_OUTPUT_TRANSLATION("CALLNAME", MASTER)%将溢出的前列腺液涂在整个%TINKO(TARGET)%上并一口气加快速度捋起了%TINKO(TARGET)%…
			ENDIF
		ELSE
			PRINTFORML %NAME_OUTPUT_TRANSLATION("CALLNAME", MASTER)%握住%NAME_OUTPUT_TRANSLATION("CALLNAME", TARGET)%的%TINKO(TARGET)%温柔的上下撸动起来…
		ENDIF
	CASE 2
		; IF SOURCE:施虐
		; 	PRINTFORMW %NAME_OUTPUT_TRANSLATION("CALLNAME", TARGET)%用手伸向%NAME_OUTPUT_TRANSLATION("CALLNAME", PLAYER)%的阴嚢揉捏起来…
		; ELSE
		PRINTFORMW %NAME_OUTPUT_TRANSLATION("CALLNAME", MASTER)%用手伸向%NAME_OUTPUT_TRANSLATION("CALLNAME", TARGET)%的阴嚢温柔的揉捏起来…
		; ENDIF
	CASE 3
		PRINTFORMW %NAME_OUTPUT_TRANSLATION("CALLNAME", MASTER)%在用手撸动%NAME_OUTPUT_TRANSLATION("CALLNAME", TARGET)%的%TINKO(TARGET)%的同时，用嘴温柔的把阴嚢含在嘴里咕噜咕噜地滚动起了睾丸
ENDSELECT
RETURN 1

;给对方口交
@EVENT_COUNTE_MESSAGE5031
SELECTCASE TCVAR:MASTER:21
	CASE 1
		PRINTFORMW %NAME_OUTPUT_TRANSLATION("CALLNAME", MASTER)%含住%NAME_OUTPUT_TRANSLATION("CALLNAME", TARGET)%的%TINKO(TARGET)%、用嘴唇撸弄着龟头…舌头不断旋转缠绕…
	CASE 2
		PRINTFORMW %NAME_OUTPUT_TRANSLATION("CALLNAME", MASTER)%、用舌头舔舐肉棒后亲吻睾丸、不断吮吸侍奉着%NAME_OUTPUT_TRANSLATION("CALLNAME", TARGET)%性器的每一处…
	;ディープスロート
	CASE 3
		PRINTFORMW %NAME_OUTPUT_TRANSLATION("CALLNAME", MASTER)%将勃起到极限的%NAME_OUTPUT_TRANSLATION("CALLNAME", TARGET)%的%TINKO(TARGET)%连根吞进了喉咙
		PRINTFORMW 用力吮吸的同时，用舌头和喉咙缠住爱抚着…
	CASE 4
		PRINTFORMW %NAME_OUTPUT_TRANSLATION("CALLNAME", MASTER)%在持续侍奉着%NAME_OUTPUT_TRANSLATION("CALLNAME", TARGET)%的同时、不停玩弄着自己的私处…
ENDSELECT
RETURN 1

;给对方乳交
@EVENT_COUNTE_MESSAGE5041
SELECTCASE TCVAR:MASTER:21
	CASE 1
		IF TALENT:MASTER:胸围 > 0
			PRINTFORMW %NAME_OUTPUT_TRANSLATION("CALLNAME", MASTER)%用胸部挟住了%NAME_OUTPUT_TRANSLATION("CALLNAME", TARGET)%的%TINKO(TARGET)%…
		ELSE
			PRINTFORMW %NAME_OUTPUT_TRANSLATION("CALLNAME", MASTER)%用乳头咯哩咯哩地摩擦着%TINKO(TARGET)%的环状沟…
		ENDIF
	;纵乳交,乳ファック
	CASE 2
		PRINTFORMW %NAME_OUTPUT_TRANSLATION("CALLNAME", TARGET)%将%TINKO(TARGET)%突入%NAME_OUTPUT_TRANSLATION("CALLNAME", MASTER)%丰满胸部的间隙后晃起了腰部…
	;谷间を作る
	CASE 3
		IF TALENT:MASTER:胸围 < 0
			PRINTFORMW %NAME_OUTPUT_TRANSLATION("CALLNAME", MASTER)%拼命贴上平平的胸部想法设法来挟住%TINKO(TARGET)%…
		ELSE
			PRINTFORMW %NAME_OUTPUT_TRANSLATION("CALLNAME", MASTER)%用挤压胸部做出来的乳沟挟住%TINKO(TARGET)%并激烈地动起了上半身…
		ENDIF
	;乳夹口交
	CASE 4
		PRINTFORMW 另外%NAME_OUTPUT_TRANSLATION("CALLNAME", MASTER)%还用舌头旋转缠绕并吮吸着露出来的龟头…
ENDSELECT
RETURN 1
;以上是MASTER不为男性时使用，以下是男性时使用
;--------------------------------------------------------------------------------------------------------------------------------
;膣性交
@EVENT_COUNTE_MESSAGE500
IF TCVAR:MASTER:21 > 0
	LOCAL = TCVAR:MASTER:21
ELSE
	LOCAL = TCVAR:21
ENDIF
SELECTCASE LOCAL
	CASE 1
		PRINTFORMW %NAME_OUTPUT_TRANSLATION("CALLNAME", TARGET)%的肉褶紧缠着%NAME_OUTPUT_TRANSLATION("CALLNAME", MASTER)%的%TINKO(PLAYER)%、为了渴望的%SAMEN_DESCRIPTION(PLAYER)%不停收缩…
	;だいしゅきホールド
	CASE 2
		PRINTFORMW %NAME_OUTPUT_TRANSLATION("CALLNAME", TARGET)%抱住%NAME_OUTPUT_TRANSLATION("CALLNAME", PLAYER)%同时用两腿紧紧缠住了腰…
	;性交见せつけ
	CASE 3
		PRINTFORMW %NAME_OUTPUT_TRANSLATION("CALLNAME", TARGET)%抬起上身向%NAME_OUTPUT_TRANSLATION("CALLNAME", PLAYER)%展示着结合部位并捋起了%TINKO(PLAYER)%…
	CASE 4
		PRINTFORMW %NAME_OUTPUT_TRANSLATION("CALLNAME", TARGET)%那渴求孕育生命的子宫颤抖着降下来了❤…为了榨取%NAME_OUTPUT_TRANSLATION("CALLNAME", PLAYER)%的精子，不受控制的拼命吮吸着
	CASE 5
		PRINTFORMW %NAME_OUTPUT_TRANSLATION("CALLNAME", TARGET)%无意识地仰起腰、迎合着%NAME_OUTPUT_TRANSLATION("CALLNAME", MASTER)%使其轻松地插入到了深处…
	CASE 6
		PRINTFORMW %NAME_OUTPUT_TRANSLATION("CALLNAME", TARGET)%的子宫口翕动着一张一合、渴求着%NAME_OUTPUT_TRANSLATION("CALLNAME", PLAYER)%射精的冲击…
	CASE 7
		PRINTFORMW %NAME_OUTPUT_TRANSLATION("CALLNAME", TARGET)%五指交缠地握住了%NAME_OUTPUT_TRANSLATION("CALLNAME", MASTER)%的手…蜜穴收缩的力度越来越紧…
ENDSELECT
RETURN 1
;肛门性交
@EVENT_COUNTE_MESSAGE501
IF TCVAR:MASTER:21 > 0
	LOCAL = TCVAR:MASTER:21
ELSE
	LOCAL = TCVAR:21
ENDIF
SELECTCASE LOCAL
	CASE 1
		PRINTFORMW 装满热乎乎的粘液的%NAME_OUTPUT_TRANSLATION("CALLNAME", TARGET)%的肠内、肉壁紧紧缠住了%TINKO(PLAYER)%…
	CASE 2
		PRINTFORMW %NAME_OUTPUT_TRANSLATION("CALLNAME", TARGET)%五指交缠地握住了%NAME_OUTPUT_TRANSLATION("CALLNAME", MASTER)%的手…菊穴收缩的力度越来越紧…
ENDSELECT
RETURN 1
;手淫
@EVENT_COUNTE_MESSAGE502
;现状许可を贳うには童贞丧失必须
;IF NOWEX:PLAYER:射精 && TALENT:PLAYER:非童贞 <= 0 && !TALENT:无知 && CFLAG:允许无套 == 2
IF TCVAR:MASTER:21 > 0
	LOCAL = TCVAR:MASTER:21
ELSE
	LOCAL = TCVAR:21
ENDIF
SELECTCASE LOCAL
	CASE 1
		IF NOWEX:PLAYER:射精 && !TALENT:无知 && CFLAG:允许无套 == 2
			;PRINTFORML %NAME_OUTPUT_TRANSLATION("CALLNAME", TARGET)%跨坐在%NAME_OUTPUT_TRANSLATION("CALLNAME", MASTER)%身上、把膣穴压在%TINKO(PLAYER)%之上然后提高速度一口气把%TINKO(PLAYER)%吞进了最深处…
			;这句怪怪的，不是用手吗，不知道原文是怎么回事，稍微改了一下
			PRINTFORML %NAME_OUTPUT_TRANSLATION("CALLNAME", TARGET)%跨坐在%NAME_OUTPUT_TRANSLATION("CALLNAME", MASTER)%身上、把膣穴压在%TINKO(MASTER)%之上，然后提高手上的速度专注地刺激%NAME_OUTPUT_TRANSLATION("CALLNAME", MASTER)%的%TINKO(MASTER)%的最前端…
		ELSEIF NOWEX:PLAYER:射精
			PRINTFORML %NAME_OUTPUT_TRANSLATION("CALLNAME", TARGET)%将溢出的前列腺液涂在整个%TINKO(PLAYER)%上并一口气加快速度捋起了%TINKO(PLAYER)%…
		ELSE
			PRINTFORML %NAME_OUTPUT_TRANSLATION("CALLNAME", TARGET)%握住%NAME_OUTPUT_TRANSLATION("CALLNAME", MASTER)%的%TINKO(PLAYER)%温柔的上下撸动起来…
		ENDIF
	CASE 2
		IF SOURCE:施虐
			PRINTFORMW %NAME_OUTPUT_TRANSLATION("CALLNAME", TARGET)%用手伸向%NAME_OUTPUT_TRANSLATION("CALLNAME", PLAYER)%的阴嚢揉捏起来…
		ELSE
			PRINTFORMW %NAME_OUTPUT_TRANSLATION("CALLNAME", TARGET)%用手伸向%NAME_OUTPUT_TRANSLATION("CALLNAME", PLAYER)%的阴嚢温柔的揉捏起来…
		ENDIF
	CASE 3
		PRINTFORMW %NAME_OUTPUT_TRANSLATION("CALLNAME", TARGET)%在用手撸动%NAME_OUTPUT_TRANSLATION("CALLNAME", MASTER)%的%TINKO(MASTER)%的同时，用嘴温柔的把阴嚢含在嘴里咕噜咕噜地滚动起了睾丸
ENDSELECT
RETURN 1

;口交
@EVENT_COUNTE_MESSAGE503
IF TCVAR:MASTER:21 > 0
	LOCAL = TCVAR:MASTER:21
ELSE
	LOCAL = TCVAR:21
ENDIF
SELECTCASE LOCAL
	CASE 1
		PRINTFORMW %NAME_OUTPUT_TRANSLATION("CALLNAME", TARGET)%含住%NAME_OUTPUT_TRANSLATION("CALLNAME", MASTER)%的%TINKO(PLAYER)%、用嘴唇撸弄着龟头…舌头不断旋转缠绕…
	CASE 2
		PRINTFORMW %NAME_OUTPUT_TRANSLATION("CALLNAME", TARGET)%、用舌头舔舐肉棒后亲吻睾丸、不断吮吸侍奉着%NAME_OUTPUT_TRANSLATION("CALLNAME", MASTER)%性器的每一处…
	;ディープスロート
	CASE 3
		PRINTFORMW %NAME_OUTPUT_TRANSLATION("CALLNAME", TARGET)%将勃起到极限的%NAME_OUTPUT_TRANSLATION("CALLNAME", MASTER)%的%TINKO(PLAYER)%连根吞进了喉咙
		PRINTFORMW 用力吮吸的同时，用舌头和喉咙缠住爱抚着…
	CASE 4
		PRINTFORMW %NAME_OUTPUT_TRANSLATION("CALLNAME", TARGET)%在持续侍奉着%NAME_OUTPUT_TRANSLATION("CALLNAME", MASTER)%的同时、不停玩弄着自己的私处…
ENDSELECT
RETURN 1

;乳交
@EVENT_COUNTE_MESSAGE504
IF TCVAR:MASTER:21 > 0
	LOCAL = TCVAR:MASTER:21
ELSE
	LOCAL = TCVAR:21
ENDIF
SELECTCASE LOCAL
	CASE 1
		IF TALENT:TARGET:胸围 > 0
			PRINTFORMW %NAME_OUTPUT_TRANSLATION("CALLNAME", TARGET)%用胸部挟住了%NAME_OUTPUT_TRANSLATION("CALLNAME", MASTER)%的%TINKO(PLAYER)%…
		ELSE
			PRINTFORMW %NAME_OUTPUT_TRANSLATION("CALLNAME", TARGET)%用乳头咯哩咯哩地摩擦着%TINKO(PLAYER)%的环状沟…
		ENDIF
	;纵乳交,乳ファック
	CASE 2
		PRINTFORMW %NAME_OUTPUT_TRANSLATION("CALLNAME", PLAYER)%将%TINKO(PLAYER)%突入%NAME_OUTPUT_TRANSLATION("CALLNAME", TARGET)%丰满胸部的间隙后晃起了腰部…
	;谷间を作る
	CASE 3
		IF TALENT:TARGET:胸围 < 0
			PRINTFORMW %NAME_OUTPUT_TRANSLATION("CALLNAME", TARGET)%拼命贴上平平的胸部想法设法来挟住%TINKO(PLAYER)%…努力侍奉的样子真是可爱极了…
		ELSE
			PRINTFORMW %NAME_OUTPUT_TRANSLATION("CALLNAME", TARGET)%用挤压胸部做出来的乳沟挟住%TINKO(PLAYER)%并激烈地动起了上半身…
		ENDIF
	;乳夹口交
	CASE 4
		PRINTFORMW 另外%NAME_OUTPUT_TRANSLATION("CALLNAME", TARGET)%还用舌头旋转缠绕并吮吸着露出来的龟头…
ENDSELECT
RETURN 1

;素股
@EVENT_COUNTE_MESSAGE505
IF TCVAR:MASTER:21 > 0
	LOCAL = TCVAR:MASTER:21
ELSE
	LOCAL = TCVAR:21
ENDIF
SELECTCASE LOCAL
	CASE 1
		PRINTFORMW %NAME_OUTPUT_TRANSLATION("CALLNAME", TARGET)%用大腿钳住%NAME_OUTPUT_TRANSLATION("CALLNAME", MASTER)%的%TINKO(PLAYER)%并动起了腰身…
	;手淫追加
	CASE 2
		PRINTFORMW %NAME_OUTPUT_TRANSLATION("CALLNAME", TARGET)%用大腿钳住%NAME_OUTPUT_TRANSLATION("CALLNAME", MASTER)%的%TINKO(PLAYER)%并用手指刺激起了露出来的龟头…
ENDSELECT
RETURN 1

;强行膣性交
@EVENT_COUNTE_MESSAGE506
IF TCVAR:MASTER:21 > 0
	LOCAL = TCVAR:MASTER:21
ELSE
	LOCAL = TCVAR:21
ENDIF
SELECTCASE LOCAL
	CASE 1
		PRINTFORMW %NAME_OUTPUT_TRANSLATION("CALLNAME", TARGET)%正努力从%NAME_OUTPUT_TRANSLATION("CALLNAME", PLAYER)%身边逃开般扭动着身体…
	CASE 2
		PRINTFORMW %NAME_OUTPUT_TRANSLATION("CALLNAME", TARGET)%含着眼泪瞪了过来…
ENDSELECT
RETURN 1


;被玩弄乳头
@EVENT_COUNTE_MESSAGE510
PRINTFORM %NAME_OUTPUT_TRANSLATION("CALLNAME", TARGET)%
IF TCVAR:MASTER:21 > 0
	LOCAL = TCVAR:MASTER:21
ELSE
	LOCAL = TCVAR:21
ENDIF
SELECTCASE LOCAL
	CASE 1
		PRINTFORMW 用手指捏住了%NAME_OUTPUT_TRANSLATION("CALLNAME", PLAYER)%的乳头、转动着给予刺激…
	CASE 2
		PRINTFORMW 用舌头舔舐着%NAME_OUTPUT_TRANSLATION("CALLNAME", PLAYER)%的乳头…
ENDSELECT

RETURN 1

;玉揉み
; @EVENT_COUNTE_MESSAGE600
; IF TCVAR:21 == 1
; 	IF SOURCE:施虐
; 		PRINTFORMW %NAME_OUTPUT_TRANSLATION("CALLNAME", TARGET)%用手伸向%NAME_OUTPUT_TRANSLATION("CALLNAME", PLAYER)%的阴嚢揉捏起来…
; 	ELSE
; 		PRINTFORMW %NAME_OUTPUT_TRANSLATION("CALLNAME", TARGET)%用手伸向%NAME_OUTPUT_TRANSLATION("CALLNAME", PLAYER)%的阴嚢温柔的揉捏起来…
; 	ENDIF
; ELSE
; 	PRINTFORMW %NAME_OUTPUT_TRANSLATION("CALLNAME", TARGET)%把阴嚢含在嘴里咕噜咕噜地滚动起了睾丸
; ENDIF
; RETURN 1

;肛门爱抚
@EVENT_COUNTE_MESSAGE601
IF ABL:PLAYER:Ａ感觉 < 2
	PRINTFORMW %NAME_OUTPUT_TRANSLATION("CALLNAME", TARGET)%用手指刺激起了%NAME_OUTPUT_TRANSLATION("CALLNAME", PLAYER)%的肛门…
ELSE
	PRINTFORMW %NAME_OUTPUT_TRANSLATION("CALLNAME", TARGET)%将手指插进%NAME_OUTPUT_TRANSLATION("CALLNAME", PLAYER)%的肛门直到直肠并来回搅动着…
ENDIF
RETURN 1