;-------------------------------------------------
;娘の性格倾向を决める函数
;eramegatenのEVENT_K3501「あなたの娘」の「@MUSUME_SEIKAKU_KETTEI」をベースにさせていただいております
;作者样に感谢を
;处理の顺番の关系上、男の子でも出てしまう
;-------------------------------------------------
@K139_PERSONALITY
PRINTFORML  %CALLNAME:MASTER%和典的孩子平安诞生了和典的孩子平安诞生了
PRINTL 女儿的性格是…
CALL ASK_M("使用预设", 1,"随机决定", 1,"随机决定（根据您的素質影响）", 1)
SELECTCASE RESULT
CASE 0
	PRINTL 这是预设的口上
	PRINTL 性格将会是「普通」
	CFLAG:1983 = 1
CASE 1
	PRINTL 将以随机方式做出决定…
	SELECTCASE RAND:4
	CASE 0
		PRINTL 性格将会是「普通」
		CFLAG:1983 = 1
	CASE 1
		PRINTL 性格会是「天真无邪」
		CFLAG:1983 = 2
	CASE 2
		PRINTL 性格会是「安静」
		CFLAG:1983 = 3
	CASE 3
		PRINTL 性格会是「调皮」
		CFLAG:1983 = 4
	ENDSELECT
CASE 2
	PRINTFORML 基于%CALLNAME:MASTER%的素質、以随机方式决定性格
	;性格：普通
	;お母さんと一绪。エッチは好きだけど割とノーマル
	;────────────────
	SIF TALENT:MASTER:态度 == -1
	LOCAL:1 += 1

	SIF TALENT:MASTER:性的兴趣 == 1
	LOCAL:1 += 2

	SIF TALENT:MASTER:贞操 == -1
	LOCAL:1 += 1

	SIF TALENT:MASTER:痛觉 == 1
	LOCAL:1 += 1

	SIF TALENT:MASTER:习得速度 == 1
	LOCAL:1 += 1

	SIF TALENT:MASTER:献身的
	LOCAL:1 += 2

	SIF TALENT:MASTER:小恶魔
	LOCAL:1 += 3

	SIF TALENT:MASTER:妖狐
	LOCAL:1 += 1

	SIF TALENT:MASTER:魅惑
	LOCAL:1 += 1

	SIF TALENT:MASTER:鼓舞
	LOCAL:1 += 1


	;性格：无邪气
	;子どもっぽい性格だけどエッチのときは一番耻じらう。甘々寄り
	;────────────────
	SIF TALENT:MASTER:态度 == -1
	LOCAL:2 += 3

	SIF TALENT:MASTER:回应 == 1
	LOCAL:2 += 1

	SIF TALENT:MASTER:开朗／阴郁 == 1
	LOCAL:2 += 2

	SIF TALENT:MASTER:无知
	LOCAL:2 += 2

	SIF TALENT:MASTER:贞操 == -1
	LOCAL:2 += 1

	SIF TALENT:MASTER:羞耻心 == 1
	LOCAL:2 += 1

	SIF TALENT:MASTER:鼓舞
	LOCAL:2 += 1

	SIF TALENT:MASTER:幼稚
	LOCAL:2 += 1

	;性格：大人しい
	;心优しいけどムッツリスケベ。エッチなことなら何でも好き
	;────────────────
	SIF TALENT:MASTER:胆量 == -1
	LOCAL:3 += 1

	SIF TALENT:MASTER:回应 == -1
	LOCAL:3 += 1

	SIF TALENT:MASTER:性的兴趣 == 1
	LOCAL:3 += 3

	SIF TALENT:MASTER:开朗／阴郁 == -1
	LOCAL:3 += 1

	SIF TALENT:MASTER:自己爱 == -1
	LOCAL:3 += 2

	SIF TALENT:MASTER:羞耻心 == -1
	LOCAL:3 += 1

	SIF TALENT:MASTER:献身的
	LOCAL:3 += 1

	SIF TALENT:MASTER:容易中毒
	LOCAL:3 += 2

	;性格：メスガキ
	;分からせなきゃ…（使命感）一见口は恶いですがパパ大好き
	;────────────────
	SIF TALENT:MASTER:态度 == -1
	LOCAL:4 += 1

	SIF TALENT:MASTER:回应 == -1
	LOCAL:4 += 2

	SIF TALENT:MASTER:傲娇
	LOCAL:4 += 3

	SIF TALENT:MASTER:性情不定
	LOCAL:4 += 1

	SIF TALENT:MASTER:自己爱 == 1
	LOCAL:4 += 1

	SIF TALENT:MASTER:献身的
	LOCAL:4 += 1

	SIF TALENT:MASTER:倒错的
	LOCAL:4 += 1

	SIF TALENT:MASTER:小恶魔
	LOCAL:4 += 2

	;繰り返し1～4
	FOR LOCAL, 1, 5
		;おっきければ
		IF LOCAL:5 < LOCAL:LOCAL
			;それに合わせます
			LOCAL:5 = LOCAL:LOCAL
			;暂定一位
			CFLAG:1983 = LOCAL
			;以上ではなくより大きいなので1から优先されるはず　はず
		ENDIF
	NEXT

	;どのLOCALも合计が3に达しなければ性格が1になる
	;LOCAL:5は1～4の中での最大值
	SIF LOCAL:5 < 2
		CFLAG:1983 = 1
ENDSELECT

	PRINT 这孩子的将来、
	SELECTCASE CFLAG:1983
	CASE 1
		PRINT 感觉会很坦率但会跟典一样好色
	CASE 2
		PRINT 感觉会充满活力
	CASE 3
		PRINT 感觉会很善良但内向
	CASE 4
		PRINTFORM 感觉会讲着　杂鱼%UNICODE(0x2665) *1%　杂鱼%UNICODE(0x2665) *1%　的骂着父亲
	ENDSELECT
	PRINTFORML ……感觉大概会变成这样呢
