@M_KOJO_假酒自制_IRAIEXIST_K66(IRAI_NO)
#DIM  IRAI_NO   ;抑制させる一般依赖ナンバー
;动きを抑制させたい场合は、”RETURN 0”に变更すること
SELECTCASE IRAI_NO
	;扫除を手传って（发生条件：TARGETがメインマップ在住）
	CASE 1
		RETURN 1
	;お花に水をあげて（发生条件：TARGETがMASTER侵入可能の自宅住まいかつ、メインマップ在住かつ、亲密２以上）
	CASE 2
		RETURN 1
	;いっしょにあそぼ（发生条件：TARGETがTALENT:幼稚持ち）
	CASE 3
		RETURN 1
	;料理上手になりたい（发生条件：TARGETの料理技能がMASTER未满かつ、MASTERの料理技能が２以上かつ、亲密が３以上）
	CASE 4
		RETURN 1
	;特训につきあって（发生条件：TARGETの战斗能力がMASTER以下かつ、亲密が２以上かつ人里以外）
	CASE 5
		RETURN 1
	;谁か教えて！（发生条件：TARGETの教养がMASTER以下）
	CASE 6
		RETURN 1
	;大扫除が终わらない（发生条件：TARGETがメインマップ在住かつ、冬の15日から31日までかつ、亲密が３以上）
	CASE 7
		RETURN 1
	;手纸を届けて（发生条件：TARGETの信赖度が30以上かつ、TARGETに文通相手が设定されている（FUNC_IRAI.ERBの965目以降に存在）
	CASE 8
		RETURN 1
	;谁かの落とし物（发生条件：TARGETの信赖度が30以上）
	CASE 9
		RETURN 1
	;どうしてもみつからない(そのまま落ちてる)（发生条件：TARGETの信赖度が30以上）
	CASE 10
		RETURN 1
	;どうしてもみつからない(谁かが拾ってる)（发生条件：TARGETの信赖度が30以上）
	CASE 11
		RETURN 1
	;惩らしめて！（发生条件：TARGETのCSVに战斗能力が１以上ある场合）
	CASE 12
		RETURN 1
	;うまい鱼が食べたいなぁ（发生条件：わかさぎ姫以外）
	CASE 13
		RETURN 1
	;薪を切らしてる（未实装）
	CASE 14
		RETURN 1
	;あなたに抱かれたい（发生条件：炮友or爱欲かつ积攒度700以上 or ヤリマン）
	CASE 15
		RETURN 1
	;お恼み相谈（发生条件：MASTERの话术技能が1以上）
	CASE 16
		RETURN 1
	[SKIPSTART]
	;现状无し
	CASEELSE
		RETURN 1
	[SKIPEND]
ENDSELECT


;==================================================
;依赖口上・各依赖で共通の口上
;==================================================
@M_KOJO_假酒自制_IRAI_K66(ROLE, SCENE, IRAI_ID)
#DIMS ROLE     ;キャラの立场、依赖者かそうでないか
#DIMS SCENE    ;セリフの表示されるタイミング
#DIM  IRAI_ID  ;依赖自体のID
#DIM  CLIENT_ID;依赖者IDだけ抜き出したもの
#DIM  CHK_ID   ;依赖IDだけ抜き出したもの
#DIMS IRAI_NAME;依赖名
CHK_ID    = IRAI_ID % 1000
CLIENT_ID = IRAI_ID_TO_CLIENT(IRAI_ID)
IRAI_NAME = %STR_DATA_IRAI(IRAI_ID, "依赖名", CLIENT_ID)%
RESULT = 0
IF CHK_ID > 100
	TRYCALLFORM M_KOJO_假酒自制_IRAI_%IRAI_NAME%_K66(ROLE, SCENE, CLIENT_ID)
ELSE
	TRYCALLFORM M_KOJO_假酒自制_IRAI_{CHK_ID}_K66(ROLE, SCENE, CLIENT_ID)
ENDIF
SIF RESULT
	RETURN 1
;--------------------------------------------------
;ここにあるのは各依赖で共通の口上です
;依赖ごとの口上を表示した场合は共通の口上は表示されません
;【】で囲ってあるのは口上前后の地の文や发生する处理です
;WAITをするか等の目安にしてください（L,WはそれぞれPRINTFORML,PRINTFORMWの略）
;--------------------------------------------------
SELECTCASE SCENE
CASE "依赖提示时"
;●依赖を持ちかけられる时
;【W どうやら［彼女］は［あなた］に赖み事があるようだ】
;【◆口上】
;【依赖データ表示】
;【INPUT（受托するかY/N）】
	PRINTFORML 「%CALLNAME:MASTER%看起来不太忙呢～」
	PRINTFORMW 「要不帮我个忙吧～」
	
CASE "依赖非受托时"
;●内容だけ见て受托しなかった时
;【L ［あなた］は丁重にお断りした】
;【◆口上】
;【W ［彼女］は残念そうな颜をしている】
	PRINTFORMW 「这样嘛～真是可惜～」
CASE "依赖受托时"
;●依赖を受托した时
;【W ［あなた］は［彼女］の依赖を受けた】
;【◆口上】
	IF TALENT:恋慕 || TALENT:恋人
		PRINTFORML 「果然还是%CALLNAME:MASTER%最好了～」
		PRINTFORMW 「完成之后帮你捏捏肩膀哦♪～」
	ELSEIF TALENT:思慕
		PRINTFORML 「非常感谢呢～」
		PRINTFORMW 「那么就拜托%CALLNAME:MASTER%了～」
	ELSE
		PRINTFORMW 「非常感谢～」
	ENDIF
CASE "依赖确认时"
;●[366]で依赖内容を依赖人に确认した时
;【◆口上】
;【依赖データ表示】
;【INPUT（破弃するかY/N）】
	IF TALENT:恋慕 || TALENT:恋人
		PRINTFORML 「是没有把委托放在心上吗～」
		PRINTFORMW 「下次可不要这样哦～」
	ELSEIF TALENT:思慕
		PRINTFORMW 「答应别人的事情可不能随随便便就忘了啊～」
	ELSE
		PRINTFORML 「唉？已经完全忘记了吗？」
		PRINTFORMW 「这么不靠谱的话可是会被人嫌弃的哦～」
	ENDIF
CASE "依赖破弃时"
;●受托した依赖を破弃する时
;【◆口上】
;【W 依赖を破弃しました。】
	IF TALENT:恋慕 || TALENT:恋人
		PRINTFORML 「失败了吗，太抱歉了…」
		PRINTFORMW 「如果太困难的话拒绝掉也是可以的呢～」
	ELSEIF TALENT:思慕
		PRINTFORMW 「啊，完全没有考虑到这个问题呢……」
	ELSE
		PRINTFORML 「看来对于你来说还是太困难了～」
	ENDIF
CASE "依赖实行直前"
;●[367]で依赖を实行する时、实行处理の前に呼び出される
;※依赖によって状况が大きく异なるので共通口上は非推奖
;【◆口上】
;【实行时の地の文・ソース等】
;【▼下の口上に续く】
	IF ROLE == "CLIENT"
	;（共通）CLIENT：依赖实行の对象と依赖主（口上主）が同一の场合（主に特训系ですが一绪に捜索等をした场合も含まれます）
		
	ELSEIF ROLE == "TARGET"
	;（共通）TARGET：口上主が依赖实行の对象である场合（主に配达系ですが捜索等をした际、TARGETにしていた场合も含まれます）
		
	ENDIF
CASE "依赖实行直后"
;●[367]で依赖を实行する时、实行处理の后に呼び出される
;※依赖によって状况が大きく异なるので共通口上は非推奖
;【▲上の口上から续き】
;【◆口上】
;【达成时・CLIENT：▼下の口上に续く】
;【达成时・TARGET：忘れずに［依赖主］に报告しよう】
	IF ROLE == "CLIENT"
	;（共通）CLIENT：依赖实行の对象と依赖主（口上主）が同一の场合（主に特训系ですが一绪に捜索等をした场合も含まれます）
		SELECTCASE CFLAG:CLIENT_ID:依赖状况
		CASE 2
		;实行后未解决
			
		CASE 3
		;实行后达成
			
		CASE 6
		;实行后失败（现在「06_谁か教えて！」のみ）
			
		ENDSELECT
	ELSEIF ROLE == "TARGET"
	;（共通）TARGET：口上主が依赖实行の对象である场合（主に配达系ですが捜索等をした际、TARGETにしていた场合も含まれます）
		SELECTCASE CFLAG:CLIENT_ID:依赖状况
		CASE 2
		;实行后未解决
			
		CASE 3
		;实行后达成
			
		CASE 6
		;实行后失败（现在「06_谁か教えて！」のみ）
			
		ENDSELECT
	ENDIF
CASE "成功报告时"
;●报告时（成功）
;【依赖实行と报告が同时の时：▲上の口上から续き】
;【◆口上】
;【ソースの入手・道具等の报酬获得】
	
CASE "失败报告时"
;○直接失败报告は现在「06_谁か教えて！」のみ
;●报告忘れ・期限切れになった际、遭遇时の最后に呼び出される
;（遭遇时）【挨拶等】→【机嫌の良し恶し】→【色欲察觉】→【祭日イベント】→【◆ココ！】（尚、依赖失败判定があるとデートのお诱いは发生しません）
;【◆口上】
;○机嫌の变化や追加のペナルティ等は口上侧で处理してください
	
CASE "依赖报告不要"
;○一般依赖では现在「02_お花に水をあげて」「09_谁かの落とし物」のみ
;●起床时、报告不要依赖を达成していた场合に呼ばれる口上
;（起床时）【起床メッセージ】→【X时间寝た等】→【◆ココ！】→【神签・补充等】→【一日开始】
;○地の文はありません
;○口上主がその场にいなくても表示されます（MASTERが寝る时のイベント口上と同じタイプ）
	
ENDSELECT
RETURN 1

;==================================================
;依赖口上・一般依赖01：扫除を手传って
;==================================================
@M_KOJO_假酒自制_IRAI_1_K66(ROLE, SCENE, CLIENT_ID)
#DIMS ROLE     ;キャラの立场、依赖者かそうでないか
#DIMS SCENE    ;セリフの表示されるタイミング
#DIM  CLIENT_ID;依赖者IDだけ抜き出したもの
;--------------------------------------------------
;RETURN 0 の部分は共通の口上が表示されます
;依赖ごとの口上を表示する部分は RETURN 0 を RETURN 1 にしてください
;--------------------------------------------------
SELECTCASE SCENE
CASE "依赖提示时"
	
	RETURN 0
CASE "依赖非受托时"
	
	RETURN 0
CASE "依赖受托时"
	
	RETURN 0
CASE "依赖确认时"
	
	RETURN 0
CASE "依赖破弃时"
	
	RETURN 0
CASE "依赖实行直前"
;○この依赖は依赖主がTARGETです
	IF YOGORE:(GET_CHARAHOME(TARGET)) < 1000
	;先に扫除してしまっていた场合
	;【◆口上】
	;【W ［彼女］の房间は十分にきれいだ】
	;【▼そのまま下の口上へ】
		
		RETURN 0
	ELSE
	;一绪に扫除する场合
	;【◆口上】
	;【W ［あなた］は［彼女］と协力して房间の扫除を行なった】
	;【▼そのまま下の口上へ】
		
		RETURN 0
	ENDIF
CASE "依赖实行直后"
;○この依赖は必ず实行后达成します
;【▲上の口上から续き】
;【◆口上】
;【▼そのまま下の口上へ】
	
	RETURN 0
CASE "成功报告时"
;报酬：低级素材
;【▲上の口上から续き】
;【◆口上】
;【报酬获得】
	
	RETURN 0
CASE "失败报告时"
	
	RETURN 0
;CASE "依赖报告不要"
;＜この依赖にはありません＞
;	RETURN 0
ENDSELECT

;==================================================
;依赖口上・一般依赖02：お花に水をあげて
;==================================================
@M_KOJO_假酒自制_IRAI_2_K66(ROLE, SCENE, CLIENT_ID)
#DIMS ROLE     ;キャラの立场、依赖者かそうでないか
#DIMS SCENE    ;セリフの表示されるタイミング
#DIM  CLIENT_ID;依赖者IDだけ抜き出したもの
;--------------------------------------------------
;RETURN 0 の部分は共通の口上が表示されます
;依赖ごとの口上を表示する部分は RETURN 0 を RETURN 1 にしてください
;--------------------------------------------------
SELECTCASE SCENE
CASE "依赖提示时"
	
	RETURN 0
CASE "依赖非受托时"
	
	RETURN 0
CASE "依赖受托时"
	
	RETURN 0
CASE "依赖确认时"
	
	RETURN 0
CASE "依赖破弃时"
	
	RETURN 0
CASE "依赖实行直前"
;【◆口上】
;【W ［あなた］は［依赖主］的家の鉢植えや植木に水をあげた】
;【▼そのまま下の口上へ】
	IF ROLE == "CLIENT"
	;水やりした时、依赖主である口上主がTARGETの场合
		
		RETURN 0
	ELSEIF ROLE == "TARGET"
	;水やりした时、依赖主ではない口上主がTARGETの场合
		
		RETURN 0
	ENDIF
CASE "依赖实行直后"
;○この依赖は必ず实行后达成します
;【▲上の口上から续き】
;【◆口上】
	IF ROLE == "CLIENT"
	;水やりした时、依赖主である口上主がTARGETの场合
	;この场合はそのまま下の口上へ续きます
		
		RETURN 0
	ELSEIF ROLE == "TARGET"
	;水やりした时、依赖主ではない口上主がTARGETの场合
		
		RETURN 0
	ENDIF
CASE "成功报告时"
;报酬：\500
	
	RETURN 0
CASE "失败报告时"
	
	RETURN 0
CASE "依赖报告不要"
	
	RETURN 0
ENDSELECT

;==================================================
;依赖口上・一般依赖03：いっしょにあそぼ
;※この依赖は[幼稚]を持っているキャラのみ发生します
;==================================================
@M_KOJO_假酒自制_IRAI_3_K66(ROLE, SCENE, CLIENT_ID)
#DIMS ROLE     ;キャラの立场、依赖者かそうでないか
#DIMS SCENE    ;セリフの表示されるタイミング
#DIM  CLIENT_ID;依赖者IDだけ抜き出したもの
;--------------------------------------------------
;RETURN 0 の部分は共通の口上が表示されます
;依赖ごとの口上を表示する部分は RETURN 0 を RETURN 1 にしてください
;--------------------------------------------------
SELECTCASE SCENE
CASE "依赖提示时"
	
	RETURN 0
CASE "依赖非受托时"
	
	RETURN 0
CASE "依赖受托时"
	
	RETURN 0
CASE "依赖确认时"
	
	RETURN 0
CASE "依赖破弃时"
	
	RETURN 0
CASE "依赖实行直前"
;○この依赖は依赖主がTARGETです
;【◆口上】
;【W ［あなた］は［彼女］と游んだ】
;【▼そのまま下の口上へ】
	
	RETURN 0
CASE "依赖实行直后"
;○この依赖は必ず实行后达成します
;【▲上の口上から续き】
;【◆口上】
;【▼そのまま下の口上へ】
	
	RETURN 0
CASE "成功报告时"
;报酬：好感度と信赖度のみ（机嫌が良くなる）
;【▲上の口上から续き】
;【◆口上】
	
	RETURN 0
CASE "失败报告时"
	
	RETURN 0
;CASE "依赖报告不要"
;＜この依赖にはありません＞
;	RETURN 0
ENDSELECT

;==================================================
;依赖口上・一般依赖04：料理上手になりたい
;==================================================
@M_KOJO_假酒自制_IRAI_4_K66(ROLE, SCENE, CLIENT_ID)
#DIMS ROLE     ;キャラの立场、依赖者かそうでないか
#DIMS SCENE    ;セリフの表示されるタイミング
#DIM  CLIENT_ID;依赖者IDだけ抜き出したもの
;--------------------------------------------------
;RETURN 0 の部分は共通の口上が表示されます
;依赖ごとの口上を表示する部分は RETURN 0 を RETURN 1 にしてください
;--------------------------------------------------
SELECTCASE SCENE
CASE "依赖提示时"
	
	RETURN 0
CASE "依赖非受托时"
	
	RETURN 0
CASE "依赖受托时"
	
	RETURN 0
CASE "依赖确认时"
	
	RETURN 0
CASE "依赖破弃时"
	
	RETURN 0
CASE "依赖实行直前"
;○この依赖は依赖主がTARGETです
;【◆口上】
;【L ［あなた］は［彼女］と一绪に料理をした】
;【W ［料理名］を作ります】
;【▼そのまま下の口上へ】
	
	RETURN 0
CASE "依赖实行直后"
;○この依赖は必ず实行后达成します
;【▲上の口上から续き】
;【◆口上】
;【▼そのまま下の口上へ】
	
	RETURN 0
CASE "成功报告时"
;报酬：\500
;【▲上の口上から续き】
;【◆口上】
;【报酬获得】
;【L できた料理は二人でいっしょに顶いた】
;【食事效果（料理采点）】
	
	RETURN 0
CASE "失败报告时"
	
	RETURN 0
;CASE "依赖报告不要"
;＜この依赖にはありません＞
;	RETURN 0
ENDSELECT

;==================================================
;依赖口上・一般依赖05：特训につきあって
;==================================================
@M_KOJO_假酒自制_IRAI_5_K66(ROLE, SCENE, CLIENT_ID)
#DIMS ROLE     ;キャラの立场、依赖者かそうでないか
#DIMS SCENE    ;セリフの表示されるタイミング
#DIM  CLIENT_ID;依赖者IDだけ抜き出したもの
;--------------------------------------------------
;RETURN 0 の部分は共通の口上が表示されます
;依赖ごとの口上を表示する部分は RETURN 0 を RETURN 1 にしてください
;--------------------------------------------------
SELECTCASE SCENE
CASE "依赖提示时"
	
	RETURN 0
CASE "依赖非受托时"
	
	RETURN 0
CASE "依赖受托时"
	
	RETURN 0
CASE "依赖确认时"
	
	RETURN 0
CASE "依赖破弃时"
	
	RETURN 0
CASE "依赖实行直前"
;○この依赖は依赖主がTARGETです
;【◆口上】
;【W ［あなた］は［彼女］と弹幕ごっこの特训をした】
;【▼そのまま下の口上へ】
	
	RETURN 0
CASE "依赖实行直后"
;○この依赖は必ず实行后达成します
;【▲上の口上から续き】
;【◆口上】
;【▼そのまま下の口上へ】
	
	RETURN 0
CASE "成功报告时"
;报酬：\500
;【▲上の口上から续き】
;【◆口上】
;【报酬获得】
	
	RETURN 0
CASE "失败报告时"
	
	RETURN 0
;CASE "依赖报告不要"
;＜この依赖にはありません＞
;	RETURN 0
ENDSELECT

;==================================================
;依赖口上・一般依赖06：谁か教えて！
;==================================================
@M_KOJO_假酒自制_IRAI_6_K66(ROLE, SCENE, CLIENT_ID)
#DIMS ROLE     ;キャラの立场、依赖者かそうでないか
#DIMS SCENE    ;セリフの表示されるタイミング
#DIM  CLIENT_ID;依赖者IDだけ抜き出したもの
;--------------------------------------------------
;RETURN 0 の部分は共通の口上が表示されます
;依赖ごとの口上を表示する部分は RETURN 0 を RETURN 1 にしてください
;--------------------------------------------------
SELECTCASE SCENE
CASE "依赖提示时"
	
	RETURN 0
CASE "依赖非受托时"
	
	RETURN 0
CASE "依赖受托时"
	
	RETURN 0
CASE "依赖确认时"
	
	RETURN 0
CASE "依赖破弃时"
	
	RETURN 0
CASE "依赖实行直前"
	IF IS_LIBRARY(CFLAG:MASTER:当前位置) && TARGET == CLIENT_ID
	;［しらべもの］と［教えてあげる］が重なっている场合
	;【◆口上】
	;【L ［あなた］は［彼女］の疑问に答えるために［现在地］を访れた】
	;【L ここなら役に立つ资料が见つかりそうだ】
	;【INPUT（しらべもの/教えてあげる）】
	;【しらべもの＞［あなた］と［彼女］は［现在地］で调べ物をした】
	;【▼そのまま下の口上へ】
	;【教えてあげる＞［教えてあげる］へジャンプ】
		
		RETURN 0
	ELSEIF IS_LIBRARY(CFLAG:MASTER:当前位置)
	;［しらべもの］をした时、依赖主ではない口上主がTARGETの场合
	;【◆口上】
	;【［あなた］は［现在地］で调べ物をした】
	;【▼そのまま下の口上へ】
		
		RETURN 0
	ELSE
	;［教えてあげる］时（依赖主がTARGET）
	;【◆口上】
	;【L ［あなた］は［彼女］の疑问に答えた】
	;【成功：［彼女］の疑问は冰解した！】
	;【失败：［彼女］は纳得できなかったようだ……】
	;【▼そのまま下の口上へ】
		
		RETURN 0
	ENDIF
CASE "依赖实行直后"
	;【▲上の口上から续き】
	;【◆口上】
	SELECTCASE CFLAG:CLIENT_ID:依赖状况
	CASE 2
	;［しらべもの］をした时
		IF ROLE == "CLIENT"
		;一绪に［しらべもの］をした时
			
			RETURN 0
		ELSEIF ROLE == "TARGET"
		;依赖主ではない口上主の前で［しらべもの］をした时
			
			RETURN 0
		ENDIF
	CASE 3
	;成功した时
	;【▼そのまま下の口上へ】
		
		RETURN 0
	CASE 6
	;失败した时
	;【▼そのまま失败报告时の口上へ】
		
		RETURN 0
	ENDSELECT
CASE "成功报告时"
;报酬：\500
	
	RETURN 0
CASE "失败报告时"
	
	RETURN 0
;CASE "依赖报告不要"
;＜この依赖にはありません＞
;	RETURN 0
ENDSELECT

;==================================================
;依赖口上・一般依赖07：大扫除が终わらない
;==================================================
@M_KOJO_假酒自制_IRAI_7_K66(ROLE, SCENE, CLIENT_ID)
#DIMS ROLE     ;キャラの立场、依赖者かそうでないか
#DIMS SCENE    ;セリフの表示されるタイミング
#DIM  CLIENT_ID;依赖者IDだけ抜き出したもの
;--------------------------------------------------
;RETURN 0 の部分は共通の口上が表示されます
;依赖ごとの口上を表示する部分は RETURN 0 を RETURN 1 にしてください
;--------------------------------------------------
SELECTCASE SCENE
CASE "依赖提示时"
	
	RETURN 0
CASE "依赖非受托时"
	
	RETURN 0
CASE "依赖受托时"
	
	RETURN 0
CASE "依赖确认时"
	
	RETURN 0
CASE "依赖破弃时"
	
	RETURN 0
CASE "依赖实行直前"
;○この依赖は依赖主がTARGETです
	IF YOGORE:(GET_CHARAHOME(TARGET)) < 1000
	;先に扫除してしまっていた场合
	;【◆口上】
	;【W ［彼女］の房间は十分にきれいだ】
	;【▼そのまま下の口上へ】
		
		RETURN 0
	ELSE
	;一绪に扫除する场合
	;【◆口上】
	;【W ［あなた］は［彼女］と协力して彻底的に房间を扫除した】
	;【▼そのまま下の口上へ】
		
		RETURN 0
	ENDIF
CASE "依赖实行直后"
;○この依赖は必ず实行后达成します
;【▲上の口上から续き】
;【◆口上】
;【▼そのまま下の口上へ】
	
	RETURN 0
CASE "成功报告时"
;报酬：\1500
;【▲上の口上から续き】
;【◆口上】
;【报酬获得】
	
	RETURN 0
CASE "失败报告时"
	
	RETURN 0
;CASE "依赖报告不要"
;＜この依赖にはありません＞
;	RETURN 0
ENDSELECT

;==================================================
;依赖口上・一般依赖08：手纸を届けて
;※この依赖はキャラによっては发生しません
;==================================================
@M_KOJO_假酒自制_IRAI_8_K66(ROLE, SCENE, CLIENT_ID)
#DIMS ROLE     ;キャラの立场、依赖者かそうでないか
#DIMS SCENE    ;セリフの表示されるタイミング
#DIM  CLIENT_ID;依赖者IDだけ抜き出したもの
;--------------------------------------------------
;RETURN 0 の部分は共通の口上が表示されます
;依赖ごとの口上を表示する部分は RETURN 0 を RETURN 1 にしてください
;--------------------------------------------------
SELECTCASE SCENE
CASE "依赖提示时"
	
	RETURN 0
CASE "依赖非受托时"
	
	RETURN 0
CASE "依赖受托时"
	
	RETURN 0
CASE "依赖确认时"
	
	RETURN 0
CASE "依赖破弃时"
	
	RETURN 0
CASE "依赖实行直前"
;○この依赖では手纸を渡される侧の口上です
;【◆口上】
;【W ［あなた］は［彼女］に预かっていた手纸を渡した】
;【▼そのまま下の口上へ】
	
	RETURN 0
CASE "依赖实行直后"
;○この依赖は必ず实行后达成します
;【▲上の口上から续き】
;【◆口上】
	
	RETURN 0
CASE "成功报告时"
;报酬：素材4つ
	
	RETURN 0
CASE "失败报告时"
	
	RETURN 0
;CASE "依赖报告不要"
;＜この依赖にはありません＞
;	RETURN 0
ENDSELECT

;==================================================
;依赖口上・一般依赖09：谁かの落とし物
;==================================================
@M_KOJO_假酒自制_IRAI_9_K66(ROLE, SCENE, CLIENT_ID)
#DIMS ROLE     ;キャラの立场、依赖者かそうでないか
#DIMS SCENE    ;セリフの表示されるタイミング
#DIM  CLIENT_ID;依赖者IDだけ抜き出したもの
;--------------------------------------------------
;RETURN 0 の部分は共通の口上が表示されます
;依赖ごとの口上を表示する部分は RETURN 0 を RETURN 1 にしてください
;--------------------------------------------------
SELECTCASE SCENE
CASE "依赖提示时"
	
	RETURN 0
CASE "依赖非受托时"
	
	RETURN 0
CASE "依赖受托时"
	
	RETURN 0
CASE "依赖确认时"
	
	RETURN 0
CASE "依赖破弃时"
	
	RETURN 0
CASE "依赖实行直前"
;○この依赖では落とし物を渡される侧の口上です
;【◆口上】
;【W ［あなた］は［彼女］に预かっていた落とし物を返した】
;【▼そのまま下の口上へ】
	
	RETURN 0
CASE "依赖实行直后"
;○この依赖は必ず实行后达成します
;【▲上の口上から续き】
;【◆口上】
	
	RETURN 0
CASE "成功报告时"
;报酬：上级素材
	
	RETURN 0
CASE "失败报告时"
	
	RETURN 0
CASE "依赖报告不要"
	
	RETURN 0
ENDSELECT

;==================================================
;依赖口上・一般依赖10：どうしてもみつからない（场所探しパターン）
;==================================================
@M_KOJO_假酒自制_IRAI_10_K66(ROLE, SCENE, CLIENT_ID)
#DIMS ROLE     ;キャラの立场、依赖者かそうでないか
#DIMS SCENE    ;セリフの表示されるタイミング
#DIM  CLIENT_ID;依赖者IDだけ抜き出したもの
;--------------------------------------------------
;RETURN 0 の部分は共通の口上が表示されます
;依赖ごとの口上を表示する部分は RETURN 0 を RETURN 1 にしてください
;--------------------------------------------------
SELECTCASE SCENE
CASE "依赖提示时"
	
	RETURN 0
CASE "依赖非受托时"
	
	RETURN 0
CASE "依赖受托时"
	
	RETURN 0
CASE "依赖确认时"
	
	RETURN 0
CASE "依赖破弃时"
	
	RETURN 0
CASE "依赖实行直前"
;【◆口上】
;【W あたりを探していた［あなた］はついに探していた落とし物を发见した】
;【▼そのまま下の口上へ】
	IF ROLE == "CLIENT"
	;依赖主である口上主と一绪に发见した场合
		
		RETURN 0
	ELSEIF ROLE == "TARGET"
	;发见した时、依赖主ではない口上主がTARGETの场合
		
		RETURN 0
	ENDIF
CASE "依赖实行直后"
;○この依赖は必ず实行后达成します
;【▲上の口上から续き】
;【◆口上】
	IF ROLE == "CLIENT"
	;依赖主である口上主と一绪に发见した场合
		
		RETURN 0
	ELSEIF ROLE == "TARGET"
	;发见した时、依赖主ではない口上主がTARGETの场合
		
		RETURN 0
	ENDIF
CASE "成功报告时"
;报酬：上级素材2つ
	
	RETURN 0
CASE "失败报告时"
	
	RETURN 0
;CASE "依赖报告不要"
;＜この依赖にはありません＞
;	RETURN 0
ENDSELECT

;==================================================
;依赖口上・一般依赖11：どうしてもみつからない（人物探しパターン）
;==================================================
@M_KOJO_假酒自制_IRAI_11_K66(ROLE, SCENE, CLIENT_ID)
#DIMS ROLE     ;キャラの立场、依赖者かそうでないか
#DIMS SCENE    ;セリフの表示されるタイミング
#DIM  CLIENT_ID;依赖者IDだけ抜き出したもの
;--------------------------------------------------
;RETURN 0 の部分は共通の口上が表示されます
;依赖ごとの口上を表示する部分は RETURN 0 を RETURN 1 にしてください
;--------------------------------------------------
SELECTCASE SCENE
CASE "依赖提示时"
	
	RETURN 0
CASE "依赖非受托时"
	
	RETURN 0
CASE "依赖受托时"
	
	RETURN 0
CASE "依赖确认时"
	
	RETURN 0
CASE "依赖破弃时"
	
	RETURN 0
CASE "依赖实行直前"
;○この依赖では失くしもの持っていた侧の口上です
;【◆口上】
;【W 闻き込みを续けていた［あなた］はついに探していた落とし物を发见した】
;【▼そのまま下の口上へ】
	
	RETURN 0
CASE "依赖实行直后"
;○この依赖は必ず实行后达成します
;【▲上の口上から续き】
;【◆口上】
	
	RETURN 0
CASE "成功报告时"
;报酬：\3200
	
	RETURN 0
CASE "失败报告时"
	
	RETURN 0
;CASE "依赖报告不要"
;＜この依赖にはありません＞
;	RETURN 0
ENDSELECT

;==================================================
;依赖口上・一般依赖12：惩らしめて！
;==================================================
@M_KOJO_假酒自制_IRAI_12_K66(ROLE, SCENE, CLIENT_ID)
#DIMS ROLE     ;キャラの立场、依赖者かそうでないか
#DIMS SCENE    ;セリフの表示されるタイミング
#DIM  CLIENT_ID;依赖者IDだけ抜き出したもの
;--------------------------------------------------
;RETURN 0 の部分は共通の口上が表示されます
;依赖ごとの口上を表示する部分は RETURN 0 を RETURN 1 にしてください
;--------------------------------------------------
SELECTCASE SCENE
CASE "依赖提示时"
	
	RETURN 0
CASE "依赖非受托时"
	
	RETURN 0
CASE "依赖受托时"
	
	RETURN 0
CASE "依赖确认时"
	
	RETURN 0
CASE "依赖破弃时"
	
	RETURN 0
CASE "依赖实行直前"
;○この依赖では讨伐される侧の口上です
	IF CANNOT_DANMAKU() == 1
	;屋内のとき（胜负できない）
	;【◆口上】
	;【W 屋外で挑もう…】
		
		RETURN 0
	ELSEIF CANNOT_DANMAKU() == 2
	;仕事中（胜负できない）
	;【◆口上】
	;【W お取り込み中だ…】
		
		RETURN 0
	ELSEIF CANNOT_DANMAKU() == 3
	;あなたが疲劳中（胜负できない）
	;【◆口上】
	;【W 今は疲れている…】
		
		RETURN 0
	ELSEIF CANNOT_DANMAKU() == 4
	;口上主が疲劳竜（胜负できない）
	;【◆口上】
	;【W 今挑むのはフェアじゃない…】
		
		RETURN 0
	ELSEIF CANNOT_DANMAKU() == 5
	;既に今日一度弹幕胜负をしている（胜负できない）
	;【◆口上】
	;【W 今日はもう挑めない…】
		
		RETURN 0
	ELSE
	;【◆口上】
	;【W 胜负开始！！】
	;【以降はそのまま弹幕口上が呼ばれます】
		
		RETURN 0
	ENDIF
	;【▼そのまま下の口上へ】
CASE "依赖实行直后"
;○この依赖では讨伐される侧の口上です
	IF CFLAG:CLIENT_ID:依赖状况 == 2
		IF CANNOT_DANMAKU() == 0
			;败犬
			;【▲上の口上から续き】
			;【L ［彼女］の讨伐に失败した……】
			;【W ……こうなったら后日出直すしかない】
			;【◆口上】
			RETURN 0
		ELSE
			;挑めなかった场合
			;【▲上の口上から续き】
			;【◆口上】
			RETURN 0
		ENDIF
	ELSEIF CFLAG:CLIENT_ID:依赖状况 == 3
	;胜利
	;【▲上の口上から续き】
	;【W ［彼女］を惩らしめた！】
	;【◆口上】
		
		RETURN 0
	ENDIF
CASE "成功报告时"
;报酬：赏金（时价）
	
	RETURN 0
CASE "失败报告时"
	
	RETURN 0
;CASE "依赖报告不要"
;＜この依赖にはありません＞
;	RETURN 0
ENDSELECT

;==================================================
;依赖口上・一般依赖13：うまい鱼が食べたいなぁ
;==================================================
@M_KOJO_假酒自制_IRAI_13_K66(ROLE, SCENE, CLIENT_ID)
#DIMS ROLE     ;キャラの立场、依赖者かそうでないか
#DIMS SCENE    ;セリフの表示されるタイミング
#DIM  CLIENT_ID;依赖者IDだけ抜き出したもの
;--------------------------------------------------
;RETURN 0 の部分は共通の口上が表示されます
;依赖ごとの口上を表示する部分は RETURN 0 を RETURN 1 にしてください
;--------------------------------------------------
SELECTCASE SCENE
CASE "依赖提示时"
	
	RETURN 0
CASE "依赖非受托时"
	
	RETURN 0
CASE "依赖受托时"
	
	RETURN 0
CASE "依赖确认时"
	
	RETURN 0
CASE "依赖破弃时"
	
	RETURN 0
CASE "依赖实行直前"
;○この依赖は依赖主がTARGETです
;【◆口上】
;【L ［あなた］は［彼女］にうまい鱼を渡した】
;【L 何だか生きる希望がわいてきたよ。】
;【▼そのまま下の口上へ】
	
	RETURN 0
CASE "依赖实行直后"
;○この依赖は必ず实行后达成します
;【▲上の口上から续き】
;【◆口上】
;【▼そのまま下の口上へ】
	
	RETURN 0
CASE "成功报告时"
;报酬：白米３个
	
	RETURN 0
CASE "失败报告时"
	
	RETURN 0
;CASE "依赖报告不要"
;＜この依赖にはありません＞
;	RETURN 0
ENDSELECT

;==================================================
;依赖口上・一般依赖16：お恼み相谈
;==================================================
@M_KOJO_假酒自制_IRAI_16_K66(ROLE, SCENE, CLIENT_ID)
#DIMS ROLE     ;キャラの立场、依赖者かそうでないか
#DIMS SCENE    ;セリフの表示されるタイミング
#DIM  CLIENT_ID;依赖者IDだけ抜き出したもの
;--------------------------------------------------
;RETURN 0 の部分は共通の口上が表示されます
;依赖ごとの口上を表示する部分は RETURN 0 を RETURN 1 にしてください
;--------------------------------------------------
SELECTCASE SCENE
CASE "依赖提示时"
	
	RETURN 0
CASE "依赖非受托时"
	
	RETURN 0
CASE "依赖受托时"
	
	RETURN 0
CASE "依赖确认时"
	
	RETURN 0
CASE "依赖破弃时"
	
	RETURN 0
CASE "依赖实行直前"
	
	RETURN 0
CASE "依赖实行直后"
	IF TFLAG:193 == 1
		;正解の选择肢
	ELSEIF TFLAG:193 == 2
		;ハズレの选择肢だったが逆に正解
	ELSEIF TFLAG:193 == -1
		;ハズレの选择肢
	ELSEIF TFLAG:193 == -2
		;正解の选择肢だったが逆にハズレ
	ENDIF
	RETURN 0
CASE "成功报告时"
;报酬：野菜か果物
	
	RETURN 0
CASE "失败报告时"
	
	RETURN 0
;CASE "依赖报告不要"
;＜この依赖にはありません＞
;	RETURN 0
CASE "口上委托率"
;＜この依赖特有の分岐です。(RESULT:100の值)％の概率で"口上専用"が发生します＞
	RESULT:100 = 0

;＜RESULT:YYに0以外の数值を入れると、该当のお恼みを发生させなくできます（詳しくは一般依赖16_お恼み相谈参照）＞
;1,2:弹幕ごっこの恼み 3,4:晩酌の恼み 5,6:本の恼み 7,8:香料の恼み
;9,10:音乐の恼み 11,12:服の恼み 13:发型の恼み 14:参拝の恼み 15:ダイエットの恼み
;16,17:菜园の恼み 18:休日の恼み 19:人气の恼み 20:集中力の恼み 21:御三家の恼み 22:进化の恼み
	RESULT:1 = 0
	RESULT:2 = 0
	RETURN 1
CASE "口上専用"
;＜この依赖特有の分岐です。(口上委托率)％の概率で发生します＞
;16,お恼み相谈 の场合
;RESULT        发生判定,0だと发生しません
;RESULT:1      正解の选择肢（ただしRESULT:2が0だと、一定概率で逆にハズレる）
;RESULT:2      逆张り正解／ハズレ判定の消去,1だと逆张りが无くなります
;RESULTS:0     お恼み文章（自动で『』で囲まれます）
;RESULTS:1～3  选择肢１～３
;何も发生しない状态になると无限ループになるので注意してください
;●追记メモ
;RESULT:1を4～6にするとそれぞれ1～3がハズレ（残り2つが正解）になります
;RESULT:1を7にするとどれも正解になります
;RESULT:1を8にするとどれもハズレになります
;1～3以外の场合は逆张り判定が自动で无くなります
	RESULT = 0

;▼サンプル
[SKIPSTART]
	SELECTCASE RAND:5
	CASE 0
		;このお恼みは、恋慕で发生し、亲密によって正解が变わります
		SIF TALENT:恋慕
			RESULT = 1
		RESULTS:0 = 赤ちゃん作るなら何人がいいかな？
		RESULTS:1 = ５人でバスケチームを作ろう
		RESULTS:2 = ９人で野球チームを作ろう
		RESULTS:3 = １１人でサッカーチームを作ろう
		IF ABL:亲密 >= 100
			RESULT:1 = 3
		ELSEIF ABL:亲密 >= 20
			RESULT:1 = 2
		ELSE
			RESULT:1 = 1
		ENDIF
	CASE 1
		;このお恼みは、无条件で发生し、正解は绝对に2番で固定化されます
		SIF 1
			RESULT = 1
		RESULTS:0 = どのジョブがいいかな？
		RESULTS:1 = かっこいしナイト
		RESULTS:2 = シーフが便利だよ
		RESULTS:3 = 青魔道士最强！
		RESULT:1 = 2
		RESULT:2 = 1
	CASE 2
		;このお恼みは、夏の月に发生し、正解がランダムで变わります
		SIF DAY:2 == 2
			RESULT = 1
		RESULTS:0 = どの水着を买おうかな？
		RESULTS:1 = かわいい连衣裙
		RESULTS:2 = クールなセパレート
		RESULTS:3 = 色っぽいビキニ
		RESULT:1 = RAND:3 + 1
	CASE 3
		;このお恼みは、无条件で发生し、1番と3番が正解になります（5 - 3 = 2番がハズレになります）
		SIF 1
			RESULT = 1
		RESULTS:0 = ワンちゃんの名前は何にしよう？
		RESULTS:1 = ボロンゴ
		RESULTS:2 = ゲレゲレ
		RESULTS:3 = プックル
		RESULT:1 = 5
	CASE 4
		;このお恼みは、无条件で发生し、どれを选んでも正解になります
		SIF 1
			RESULT = 1
		RESULTS:0 = 夕饭は何がいい？
		RESULTS:1 = 肉じゃが
		RESULTS:2 = コロッケ
		RESULTS:3 = 烧き鱼
		RESULT:1 = 7
	ENDSELECT
[SKIPEND]

	SIF !RESULT
		RESTART
	RETURN 1
ENDSELECT

;==================================================
;依赖口上・固有依赖101：
;固有依赖の仕样はキャラによって异なりますが依赖番号は101～で共通です
;"依赖名"部分に依赖名を入れて下さい
;==================================================
@M_KOJO_假酒自制_IRAI_依赖名_K66(ROLE, SCENE, CLIENT_ID)
#DIMS ROLE     ;キャラの立场、依赖者かそうでないか
#DIMS SCENE    ;セリフの表示されるタイミング
#DIM  CLIENT_ID;依赖者IDだけ抜き出したもの
;--------------------------------------------------
;RETURN 0 の部分は共通の口上が表示されます
;依赖ごとの口上を表示する部分は RETURN 0 を RETURN 1 にしてください
;--------------------------------------------------
SELECTCASE SCENE
CASE "依赖提示时"
	
	RETURN 0
CASE "依赖非受托时"
	
	RETURN 0
CASE "依赖受托时"
	
	RETURN 0
CASE "依赖确认时"
	
	RETURN 0
CASE "依赖破弃时"
	
	RETURN 0
CASE "依赖实行直前"
	
	RETURN 0
CASE "依赖实行直后"
	
	RETURN 0
CASE "成功报告时"
;报酬：
	
	RETURN 0
CASE "失败报告时"
	
	RETURN 0
CASE "依赖报告不要"
	
	RETURN 0
ENDSELECT

[SKIPSTART]
;＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝
;依赖口上テンプレのテンプレ
;==================================================
;依赖口上・一般依赖Y：
;==================================================
@M_KOJO_假酒自制_IRAI_Y_K66(ROLE, SCENE, CLIENT_ID)
#DIMS ROLE     ;キャラの立场、依赖者かそうでないか
#DIMS SCENE    ;セリフの表示されるタイミング
#DIM  CLIENT_ID;依赖者IDだけ抜き出したもの
;--------------------------------------------------
;RETURN 0 の部分は共通の口上が表示されます
;依赖ごとの口上を表示する部分は RETURN 0 を RETURN 1 にしてください
;--------------------------------------------------
SELECTCASE SCENE
CASE "依赖提示时"
	
	RETURN 0
CASE "依赖非受托时"
	
	RETURN 0
CASE "依赖受托时"
	
	RETURN 0
CASE "依赖确认时"
	
	RETURN 0
CASE "依赖破弃时"
	
	RETURN 0
CASE "依赖实行直前"
	
	RETURN 0
CASE "依赖实行直后"
	
	RETURN 0
CASE "成功报告时"
;报酬：
	
	RETURN 0
CASE "失败报告时"
	
	RETURN 0
;CASE "依赖报告不要"
;＜この依赖にはありません＞
;	RETURN 0
ENDSELECT
[SKIPEND]
