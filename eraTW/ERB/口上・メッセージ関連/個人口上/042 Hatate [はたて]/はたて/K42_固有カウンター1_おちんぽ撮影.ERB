;许可が出たのでサンプル。
;

;[[果]]→[[果]]

;固有Counter可否
@UNIQUE_COUNTER1_ABLE_K42
SIF TFLAG:62
	RETURN 0
SIF TEQUIP:[[果]]:50 >= 0 || TEQUIP:[[果]]:51 >= 0
	RETURN 0
SIF ABL:[[果]]:欲望 < 5
	RETURN 0
RETURN 1

;固有Counterの种别と频度
@UNIQUE_COUNTER1_FREQUENCY_K42
;ソフト/ベリーソフト/コミュ/着衣/脱衣爱抚/脱衣强迫/抱き着き/性交/责め/おねだりの中なら选ぶべし
;TYPEによって发生しない状况があるので注意
RESULTS = 脱衣爱抚
;发生率、基本值(10)にこの数值を足す
;通常のCounterはプラ​舞​5以下なので、テストプレイ以外であんまり极端な数字を入れない方がいい
RETURN 10

;文本
@UNIQUE_COUNTER1_MESSAGE_K42
PRINTFORML %CALLNAME:TARGET%将照相机对准%CALLNAME:MASTER%的性器，不停地按动快门……

;固有Counterのソース等
@UNIQUE_COUNTER1_SOURCE_K42

	;体势の变更
	;=============================
	CALL TOUCH_SET(SET_FROM_YUBI,1,[[果]])
	TFLAG:MASTERのＣ使用中 ++

	;ソース计算および内部处理系统
	;=============================
	IF TEQUIP:PLAYER:0
		IF TEQUIP:PLAYER:0 & 1
			CALL DATUI_BOTTOM([[果]],1)
		ELSEIF TEQUIP:PLAYER:0 & 8
			CALL DATUI_BOTTOM([[果]],2)
		ENDIF
	ENDIF

	SOURCE:[[果]]:性行动 += 150
	SOURCE:[[果]]:侮辱 += 500
	SOURCE:[[果]]:施虐 += 100
	SOURCE:[[果]]:征服 += 100

	TCVAR:[[果]]:嗜虐フラグ = 1
	CALL EVENT_COUNTER_POSE_69([[果]],2)
	
