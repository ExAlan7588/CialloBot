;==================================================
;依赖口上・各依赖で共通の口上
;==================================================
@M_KOJO_IRAI_KX(ROLE, SCENE, IRAI_ID)
#DIMS ROLE     ;キャラの立场、依赖者かそうでないか
#DIMS SCENE    ;セリフの表示されるタイミング
#DIM  IRAI_ID  ;依赖自体のID
#DIM  CLIENT_ID;依赖者IDだけ抜き出したもの
#DIM  CHK_ID   ;依赖IDだけ抜き出したもの
#DIMS IRAI_NAME;依赖名
CHK_ID    = IRAI_ID % 1000
CLIENT_ID = IRAI_ID_TO_CLIENT(IRAI_ID)
IRAI_NAME = %STR_DATA_IRAI(IRAI_ID, "依赖名", CLIENT_ID)%
RESULT = 0
IF CHK_ID > 100
	TRYCALLFORM M_KOJO_IRAI_%IRAI_NAME%_KX(ROLE, SCENE, CLIENT_ID)
ELSE
	TRYCALLFORM M_KOJO_IRAI_{CHK_ID}_KX(ROLE, SCENE, CLIENT_ID)
ENDIF
SIF RESULT
	RETURN 1
;--------------------------------------------------
;ここにあるのは各依赖で共通の口上です
;依赖ごとの口上を表示した场合は共通の口上は表示されません
;【】で囲ってあるのは口上前后の地の文や发生する处理です
;WAITをするか等の目安にしてください（L,WはそれぞれPRINTFORML,PRINTFORMWの略）
;--------------------------------------------------
SELECTCASE SCENE
CASE "依赖提示时"
;●依赖を持ちかけられる时
;【W どうやら［彼女］は［あなた］に赖み事があるようだ】
;【◆口上】
;【依赖データ表示】
;【INPUT（受託するかY/N）】
	
CASE "依赖非受託时"
;●内容だけ見て受託しなかった时
;【L ［あなた］は丁重にお断りした】
;【◆口上】
;【W ［彼女］は残念そうな颜をしている】
	
CASE "依赖受託时"
;●依赖を受託した时
;【W ［あなた］は［彼女］の依赖を受けた】
;【◆口上】
	
CASE "依赖确认时"
;●[366]で依赖内容を依赖人に确认した时
;【◆口上】
;【依赖データ表示】
;【INPUT（破棄するかY/N）】
	
CASE "依赖破棄时"
;●受託した依赖を破棄する时
;【◆口上】
;【W 依赖を破棄しました。】
	
CASE "依赖実行直前"
;●[367]で依赖を実行する时、実行处理の前に呼び出される
;※依赖によって状况が大きく异なるので共通口上は非推奨
;【◆口上】
;【実行时の地の文・ソース等】
;【▼下の口上に続く】
	IF ROLE == "CLIENT"
	;（共通）CLIENT：依赖実行の対象と依赖主（口上主）が同一の场合（主に特訓系ですが一绪に捜索等をした场合も含まれます）
		
	ELSEIF ROLE == "TARGET"
	;（共通）TARGET：口上主が依赖実行の対象である场合（主に配達系ですが捜索等をした際、TARGETにしていた场合も含まれます）
		
	ENDIF
CASE "依赖実行直后"
;●[367]で依赖を実行する时、実行处理の后に呼び出される
;※依赖によって状况が大きく异なるので共通口上は非推奨
;【▲上の口上から続き】
;【◆口上】
;【达成时・CLIENT：▼下の口上に続く】
;【达成时・TARGET：忘れずに［依赖主］に報告しよう】
	IF ROLE == "CLIENT"
	;（共通）CLIENT：依赖実行の対象と依赖主（口上主）が同一の场合（主に特訓系ですが一绪に捜索等をした场合も含まれます）
		SELECTCASE CFLAG:CLIENT_ID:依赖状况
		CASE 2
		;実行后未解決
			
		CASE 3
		;実行后达成
			
		CASE 6
		;実行后失敗（現在「06_誰か教えて！」のみ）
			
		ENDSELECT
	ELSEIF ROLE == "TARGET"
	;（共通）TARGET：口上主が依赖実行の対象である场合（主に配達系ですが捜索等をした際、TARGETにしていた场合も含まれます）
		SELECTCASE CFLAG:CLIENT_ID:依赖状况
		CASE 2
		;実行后未解決
			
		CASE 3
		;実行后达成
			
		CASE 6
		;実行后失敗（現在「06_誰か教えて！」のみ）
			
		ENDSELECT
	ENDIF
CASE "成功報告时"
;●報告时（成功）
;【依赖実行と報告が同时の时：▲上の口上から続き】
;【◆口上】
;【ソースの入手・道具等の報酬获得】
	
CASE "失敗報告时"
;○直接失敗報告は現在「06_誰か教えて！」のみ
;●報告忘れ・期限切れになった際、遭遇时の最后に呼び出される
;（遭遇时）【挨拶等】→【机嫌の良し悪し】→【劣情察觉】→【祭日イベント】→【◆ココ！】（尚、依赖失敗判定があるとデートのお诱いは发生しません）
;【◆口上】
;○机嫌の変化や追加のペナルティ等は口上側で处理してください
	
CASE "依赖報告不要"
;○一般依赖では現在「02_お花に水をあげて」「09_誰かの落とし物」のみ
;●起床时、報告不要依赖を达成していた场合に呼ばれる口上
;（起床时）【起床メッセージ】→【X时间寝た等】→【◆ココ！】→【神签・补充等】→【一日开始】
;○地の文はありません
;○口上主がその场にいなくても表示されます（MASTERが寝る时のイベント口上と同じタイプ）
	
ENDSELECT
RETURN 1

;==================================================
;依赖口上・一般依赖01：扫除を手伝って
;==================================================
@M_KOJO_IRAI_1_KX(ROLE, SCENE, CLIENT_ID)
#DIMS ROLE     ;キャラの立场、依赖者かそうでないか
#DIMS SCENE    ;セリフの表示されるタイミング
#DIM  CLIENT_ID;依赖者IDだけ抜き出したもの
;--------------------------------------------------
;RETURN 0 の部分は共通の口上が表示されます
;依赖ごとの口上を表示する部分は RETURN 0 を RETURN 1 にしてください
;--------------------------------------------------
SELECTCASE SCENE
CASE "依赖提示时"
	
	RETURN 0
CASE "依赖非受託时"
	
	RETURN 0
CASE "依赖受託时"
	
	RETURN 0
CASE "依赖确认时"
	
	RETURN 0
CASE "依赖破棄时"
	
	RETURN 0
CASE "依赖実行直前"
;○この依赖は依赖主がTARGETです
	IF YOGORE:(GET_CHARAHOME(TARGET)) < 1000
	;先に扫除してしまっていた场合
	;【◆口上】
	;【W ［彼女］の房间は十分にきれいだ】
	;【▼そのまま下の口上へ】
		
		RETURN 0
	ELSE
	;一绪に扫除する场合
	;【◆口上】
	;【W ［あなた］は［彼女］と協力して房间の扫除を行なった】
	;【▼そのまま下の口上へ】
		
		RETURN 0
	ENDIF
CASE "依赖実行直后"
;○この依赖は必ず実行后达成します
;【▲上の口上から続き】
;【◆口上】
;【▼そのまま下の口上へ】
	
	RETURN 0
CASE "成功報告时"
;報酬：低级素材
;【▲上の口上から続き】
;【◆口上】
;【報酬获得】
	
	RETURN 0
CASE "失敗報告时"
	
	RETURN 0
;CASE "依赖報告不要"
;＜この依赖にはありません＞
;	RETURN 0
ENDSELECT

;==================================================
;依赖口上・一般依赖02：お花に水をあげて
;==================================================
@M_KOJO_IRAI_2_KX(ROLE, SCENE, CLIENT_ID)
#DIMS ROLE     ;キャラの立场、依赖者かそうでないか
#DIMS SCENE    ;セリフの表示されるタイミング
#DIM  CLIENT_ID;依赖者IDだけ抜き出したもの
;--------------------------------------------------
;RETURN 0 の部分は共通の口上が表示されます
;依赖ごとの口上を表示する部分は RETURN 0 を RETURN 1 にしてください
;--------------------------------------------------
SELECTCASE SCENE
CASE "依赖提示时"
	
	RETURN 0
CASE "依赖非受託时"
	
	RETURN 0
CASE "依赖受託时"
	
	RETURN 0
CASE "依赖确认时"
	
	RETURN 0
CASE "依赖破棄时"
	
	RETURN 0
CASE "依赖実行直前"
;【◆口上】
;【W ［あなた］は［依赖主］の家の鉢植えや植木に水をあげた】
;【▼そのまま下の口上へ】
	IF ROLE == "CLIENT"
	;水やりした时、依赖主である口上主がTARGETの场合
		
		RETURN 0
	ELSEIF ROLE == "TARGET"
	;水やりした时、依赖主ではない口上主がTARGETの场合
		
		RETURN 0
	ENDIF
CASE "依赖実行直后"
;○この依赖は必ず実行后达成します
;【▲上の口上から続き】
;【◆口上】
	IF ROLE == "CLIENT"
	;水やりした时、依赖主である口上主がTARGETの场合
	;この场合はそのまま下の口上へ続きます
		
		RETURN 0
	ELSEIF ROLE == "TARGET"
	;水やりした时、依赖主ではない口上主がTARGETの场合
		
		RETURN 0
	ENDIF
CASE "成功報告时"
;報酬：\500
	
	RETURN 0
CASE "失敗報告时"
	
	RETURN 0
CASE "依赖報告不要"
	
	RETURN 0
ENDSELECT

;==================================================
;依赖口上・一般依赖03：いっしょにあそぼ
;※この依赖は[幼稚]を持っているキャラのみ发生します
;==================================================
@M_KOJO_IRAI_3_KX(ROLE, SCENE, CLIENT_ID)
#DIMS ROLE     ;キャラの立场、依赖者かそうでないか
#DIMS SCENE    ;セリフの表示されるタイミング
#DIM  CLIENT_ID;依赖者IDだけ抜き出したもの
;--------------------------------------------------
;RETURN 0 の部分は共通の口上が表示されます
;依赖ごとの口上を表示する部分は RETURN 0 を RETURN 1 にしてください
;--------------------------------------------------
SELECTCASE SCENE
CASE "依赖提示时"
	
	RETURN 0
CASE "依赖非受託时"
	
	RETURN 0
CASE "依赖受託时"
	
	RETURN 0
CASE "依赖确认时"
	
	RETURN 0
CASE "依赖破棄时"
	
	RETURN 0
CASE "依赖実行直前"
;○この依赖は依赖主がTARGETです
;【◆口上】
;【W ［あなた］は［彼女］と遊んだ】
;【▼そのまま下の口上へ】
	
	RETURN 0
CASE "依赖実行直后"
;○この依赖は必ず実行后达成します
;【▲上の口上から続き】
;【◆口上】
;【▼そのまま下の口上へ】
	
	RETURN 0
CASE "成功報告时"
;報酬：好感度と信赖度のみ（机嫌が良くなる）
;【▲上の口上から続き】
;【◆口上】
	
	RETURN 0
CASE "失敗報告时"
	
	RETURN 0
;CASE "依赖報告不要"
;＜この依赖にはありません＞
;	RETURN 0
ENDSELECT

;==================================================
;依赖口上・一般依赖04：料理上手になりたい
;==================================================
@M_KOJO_IRAI_4_KX(ROLE, SCENE, CLIENT_ID)
#DIMS ROLE     ;キャラの立场、依赖者かそうでないか
#DIMS SCENE    ;セリフの表示されるタイミング
#DIM  CLIENT_ID;依赖者IDだけ抜き出したもの
;--------------------------------------------------
;RETURN 0 の部分は共通の口上が表示されます
;依赖ごとの口上を表示する部分は RETURN 0 を RETURN 1 にしてください
;--------------------------------------------------
SELECTCASE SCENE
CASE "依赖提示时"
	
	RETURN 0
CASE "依赖非受託时"
	
	RETURN 0
CASE "依赖受託时"
	
	RETURN 0
CASE "依赖确认时"
	
	RETURN 0
CASE "依赖破棄时"
	
	RETURN 0
CASE "依赖実行直前"
;○この依赖は依赖主がTARGETです
;【◆口上】
;【L ［あなた］は［彼女］と一绪に料理をした】
;【W ［料理名］を作ります】
;【▼そのまま下の口上へ】
	
	RETURN 0
CASE "依赖実行直后"
;○この依赖は必ず実行后达成します
;【▲上の口上から続き】
;【◆口上】
;【▼そのまま下の口上へ】
	
	RETURN 0
CASE "成功報告时"
;報酬：\500
;【▲上の口上から続き】
;【◆口上】
;【報酬获得】
;【L できた料理は二人でいっしょに頂いた】
;【食事效果（料理采点）】
	
	RETURN 0
CASE "失敗報告时"
	
	RETURN 0
;CASE "依赖報告不要"
;＜この依赖にはありません＞
;	RETURN 0
ENDSELECT

;==================================================
;依赖口上・一般依赖05：特訓につきあって
;==================================================
@M_KOJO_IRAI_5_KX(ROLE, SCENE, CLIENT_ID)
#DIMS ROLE     ;キャラの立场、依赖者かそうでないか
#DIMS SCENE    ;セリフの表示されるタイミング
#DIM  CLIENT_ID;依赖者IDだけ抜き出したもの
;--------------------------------------------------
;RETURN 0 の部分は共通の口上が表示されます
;依赖ごとの口上を表示する部分は RETURN 0 を RETURN 1 にしてください
;--------------------------------------------------
SELECTCASE SCENE
CASE "依赖提示时"
	
	RETURN 0
CASE "依赖非受託时"
	
	RETURN 0
CASE "依赖受託时"
	
	RETURN 0
CASE "依赖确认时"
	
	RETURN 0
CASE "依赖破棄时"
	
	RETURN 0
CASE "依赖実行直前"
;○この依赖は依赖主がTARGETです
;【◆口上】
;【W ［あなた］は［彼女］と弾幕ごっこの特訓をした】
;【▼そのまま下の口上へ】
	
	RETURN 0
CASE "依赖実行直后"
;○この依赖は必ず実行后达成します
;【▲上の口上から続き】
;【◆口上】
;【▼そのまま下の口上へ】
	
	RETURN 0
CASE "成功報告时"
;報酬：\500
;【▲上の口上から続き】
;【◆口上】
;【報酬获得】
	
	RETURN 0
CASE "失敗報告时"
	
	RETURN 0
;CASE "依赖報告不要"
;＜この依赖にはありません＞
;	RETURN 0
ENDSELECT

;==================================================
;依赖口上・一般依赖06：誰か教えて！
;==================================================
@M_KOJO_IRAI_6_KX(ROLE, SCENE, CLIENT_ID)
#DIMS ROLE     ;キャラの立场、依赖者かそうでないか
#DIMS SCENE    ;セリフの表示されるタイミング
#DIM  CLIENT_ID;依赖者IDだけ抜き出したもの
;--------------------------------------------------
;RETURN 0 の部分は共通の口上が表示されます
;依赖ごとの口上を表示する部分は RETURN 0 を RETURN 1 にしてください
;--------------------------------------------------
SELECTCASE SCENE
CASE "依赖提示时"
	
	RETURN 0
CASE "依赖非受託时"
	
	RETURN 0
CASE "依赖受託时"
	
	RETURN 0
CASE "依赖确认时"
	
	RETURN 0
CASE "依赖破棄时"
	
	RETURN 0
CASE "依赖実行直前"
	IF IS_LIBRARY(CFLAG:MASTER:当前位置) && TARGET == CLIENT_ID
	;［しらべもの］と［教えてあげる］が重なっている场合
	;【◆口上】
	;【L ［あなた］は［彼女］の疑問に答えるために［現在地］を访れた】
	;【L ここなら役に立つ資料が見つかりそうだ】
	;【INPUT（しらべもの/教えてあげる）】
	;【しらべもの＞［あなた］と［彼女］は［現在地］で調べ物をした】
	;【▼そのまま下の口上へ】
	;【教えてあげる＞［教えてあげる］へジャンプ】
		
		RETURN 0
	ELSEIF IS_LIBRARY(CFLAG:MASTER:当前位置)
	;［しらべもの］をした时、依赖主ではない口上主がTARGETの场合
	;【◆口上】
	;【［あなた］は［現在地］で調べ物をした】
	;【▼そのまま下の口上へ】
		
		RETURN 0
	ELSE
	;［教えてあげる］时（依赖主がTARGET）
	;【◆口上】
	;【L ［あなた］は［彼女］の疑問に答えた】
	;【成功：［彼女］の疑問は冰解した！】
	;【失敗：［彼女］は納得できなかったようだ……】
	;【▼そのまま下の口上へ】
		
		RETURN 0
	ENDIF
CASE "依赖実行直后"
	;【▲上の口上から続き】
	;【◆口上】
	SELECTCASE CFLAG:CLIENT_ID:依赖状况
	CASE 2
	;［しらべもの］をした时
		IF ROLE == "CLIENT"
		;一绪に［しらべもの］をした时
			
			RETURN 0
		ELSEIF ROLE == "TARGET"
		;依赖主ではない口上主の前で［しらべもの］をした时
			
			RETURN 0
		ENDIF
	CASE 3
	;成功した时
	;【▼そのまま下の口上へ】
		
		RETURN 0
	CASE 6
	;失敗した时
	;【▼そのまま失敗報告时の口上へ】
		
		RETURN 0
	ENDSELECT
CASE "成功報告时"
;報酬：\500
	
	RETURN 0
CASE "失敗報告时"
	
	RETURN 0
;CASE "依赖報告不要"
;＜この依赖にはありません＞
;	RETURN 0
ENDSELECT

;==================================================
;依赖口上・一般依赖07：大扫除が終わらない
;==================================================
@M_KOJO_IRAI_7_KX(ROLE, SCENE, CLIENT_ID)
#DIMS ROLE     ;キャラの立场、依赖者かそうでないか
#DIMS SCENE    ;セリフの表示されるタイミング
#DIM  CLIENT_ID;依赖者IDだけ抜き出したもの
;--------------------------------------------------
;RETURN 0 の部分は共通の口上が表示されます
;依赖ごとの口上を表示する部分は RETURN 0 を RETURN 1 にしてください
;--------------------------------------------------
SELECTCASE SCENE
CASE "依赖提示时"
	
	RETURN 0
CASE "依赖非受託时"
	
	RETURN 0
CASE "依赖受託时"
	
	RETURN 0
CASE "依赖确认时"
	
	RETURN 0
CASE "依赖破棄时"
	
	RETURN 0
CASE "依赖実行直前"
;○この依赖は依赖主がTARGETです
	IF YOGORE:(GET_CHARAHOME(TARGET)) < 1000
	;先に扫除してしまっていた场合
	;【◆口上】
	;【W ［彼女］の房间は十分にきれいだ】
	;【▼そのまま下の口上へ】
		
		RETURN 0
	ELSE
	;一绪に扫除する场合
	;【◆口上】
	;【W ［あなた］は［彼女］と協力して徹底的に房间を扫除した】
	;【▼そのまま下の口上へ】
		
		RETURN 0
	ENDIF
CASE "依赖実行直后"
;○この依赖は必ず実行后达成します
;【▲上の口上から続き】
;【◆口上】
;【▼そのまま下の口上へ】
	
	RETURN 0
CASE "成功報告时"
;報酬：\1500
;【▲上の口上から続き】
;【◆口上】
;【報酬获得】
	
	RETURN 0
CASE "失敗報告时"
	
	RETURN 0
;CASE "依赖報告不要"
;＜この依赖にはありません＞
;	RETURN 0
ENDSELECT

;==================================================
;依赖口上・一般依赖08：手纸を届けて
;※この依赖はキャラによっては发生しません
;==================================================
@M_KOJO_IRAI_8_KX(ROLE, SCENE, CLIENT_ID)
#DIMS ROLE     ;キャラの立场、依赖者かそうでないか
#DIMS SCENE    ;セリフの表示されるタイミング
#DIM  CLIENT_ID;依赖者IDだけ抜き出したもの
;--------------------------------------------------
;RETURN 0 の部分は共通の口上が表示されます
;依赖ごとの口上を表示する部分は RETURN 0 を RETURN 1 にしてください
;--------------------------------------------------
SELECTCASE SCENE
CASE "依赖提示时"
	
	RETURN 0
CASE "依赖非受託时"
	
	RETURN 0
CASE "依赖受託时"
	
	RETURN 0
CASE "依赖确认时"
	
	RETURN 0
CASE "依赖破棄时"
	
	RETURN 0
CASE "依赖実行直前"
;○この依赖では手纸を渡される側の口上です
;【◆口上】
;【W ［あなた］は［彼女］に預かっていた手纸を渡した】
;【▼そのまま下の口上へ】
	
	RETURN 0
CASE "依赖実行直后"
;○この依赖は必ず実行后达成します
;【▲上の口上から続き】
;【◆口上】
	
	RETURN 0
CASE "成功報告时"
;報酬：素材4つ
	
	RETURN 0
CASE "失敗報告时"
	
	RETURN 0
;CASE "依赖報告不要"
;＜この依赖にはありません＞
;	RETURN 0
ENDSELECT

;==================================================
;依赖口上・一般依赖09：誰かの落とし物
;==================================================
@M_KOJO_IRAI_9_KX(ROLE, SCENE, CLIENT_ID)
#DIMS ROLE     ;キャラの立场、依赖者かそうでないか
#DIMS SCENE    ;セリフの表示されるタイミング
#DIM  CLIENT_ID;依赖者IDだけ抜き出したもの
;--------------------------------------------------
;RETURN 0 の部分は共通の口上が表示されます
;依赖ごとの口上を表示する部分は RETURN 0 を RETURN 1 にしてください
;--------------------------------------------------
SELECTCASE SCENE
CASE "依赖提示时"
	
	RETURN 0
CASE "依赖非受託时"
	
	RETURN 0
CASE "依赖受託时"
	
	RETURN 0
CASE "依赖确认时"
	
	RETURN 0
CASE "依赖破棄时"
	
	RETURN 0
CASE "依赖実行直前"
;○この依赖では落とし物を渡される側の口上です
;【◆口上】
;【W ［あなた］は［彼女］に預かっていた落とし物を返した】
;【▼そのまま下の口上へ】
	
	RETURN 0
CASE "依赖実行直后"
;○この依赖は必ず実行后达成します
;【▲上の口上から続き】
;【◆口上】
	
	RETURN 0
CASE "成功報告时"
;報酬：上级素材
	
	RETURN 0
CASE "失敗報告时"
	
	RETURN 0
CASE "依赖報告不要"
	
	RETURN 0
ENDSELECT

;==================================================
;依赖口上・一般依赖10：どうしてもみつからない（场所探しパターン）
;==================================================
@M_KOJO_IRAI_10_KX(ROLE, SCENE, CLIENT_ID)
#DIMS ROLE     ;キャラの立场、依赖者かそうでないか
#DIMS SCENE    ;セリフの表示されるタイミング
#DIM  CLIENT_ID;依赖者IDだけ抜き出したもの
;--------------------------------------------------
;RETURN 0 の部分は共通の口上が表示されます
;依赖ごとの口上を表示する部分は RETURN 0 を RETURN 1 にしてください
;--------------------------------------------------
SELECTCASE SCENE
CASE "依赖提示时"
	
	RETURN 0
CASE "依赖非受託时"
	
	RETURN 0
CASE "依赖受託时"
	
	RETURN 0
CASE "依赖确认时"
	
	RETURN 0
CASE "依赖破棄时"
	
	RETURN 0
CASE "依赖実行直前"
;【◆口上】
;【W あたりを探していた［あなた］はついに探していた落とし物を发見した】
;【▼そのまま下の口上へ】
	IF ROLE == "CLIENT"
	;依赖主である口上主と一绪に发見した场合
		
		RETURN 0
	ELSEIF ROLE == "TARGET"
	;发見した时、依赖主ではない口上主がTARGETの场合
		
		RETURN 0
	ENDIF
CASE "依赖実行直后"
;○この依赖は必ず実行后达成します
;【▲上の口上から続き】
;【◆口上】
	IF ROLE == "CLIENT"
	;依赖主である口上主と一绪に发見した场合
		
		RETURN 0
	ELSEIF ROLE == "TARGET"
	;发見した时、依赖主ではない口上主がTARGETの场合
		
		RETURN 0
	ENDIF
CASE "成功報告时"
;報酬：上级素材2つ
	
	RETURN 0
CASE "失敗報告时"
	
	RETURN 0
;CASE "依赖報告不要"
;＜この依赖にはありません＞
;	RETURN 0
ENDSELECT

;==================================================
;依赖口上・一般依赖11：どうしてもみつからない（人物探しパターン）
;==================================================
@M_KOJO_IRAI_11_KX(ROLE, SCENE, CLIENT_ID)
#DIMS ROLE     ;キャラの立场、依赖者かそうでないか
#DIMS SCENE    ;セリフの表示されるタイミング
#DIM  CLIENT_ID;依赖者IDだけ抜き出したもの
;--------------------------------------------------
;RETURN 0 の部分は共通の口上が表示されます
;依赖ごとの口上を表示する部分は RETURN 0 を RETURN 1 にしてください
;--------------------------------------------------
SELECTCASE SCENE
CASE "依赖提示时"
	
	RETURN 0
CASE "依赖非受託时"
	
	RETURN 0
CASE "依赖受託时"
	
	RETURN 0
CASE "依赖确认时"
	
	RETURN 0
CASE "依赖破棄时"
	
	RETURN 0
CASE "依赖実行直前"
;○この依赖では失くしもの持っていた側の口上です
;【◆口上】
;【W 聞き込みを続けていた［あなた］はついに探していた落とし物を发見した】
;【▼そのまま下の口上へ】
	
	RETURN 0
CASE "依赖実行直后"
;○この依赖は必ず実行后达成します
;【▲上の口上から続き】
;【◆口上】
	
	RETURN 0
CASE "成功報告时"
;報酬：\3200
	
	RETURN 0
CASE "失敗報告时"
	
	RETURN 0
;CASE "依赖報告不要"
;＜この依赖にはありません＞
;	RETURN 0
ENDSELECT

;==================================================
;依赖口上・一般依赖12：懲らしめて！
;==================================================
@M_KOJO_IRAI_12_KX(ROLE, SCENE, CLIENT_ID)
#DIMS ROLE     ;キャラの立场、依赖者かそうでないか
#DIMS SCENE    ;セリフの表示されるタイミング
#DIM  CLIENT_ID;依赖者IDだけ抜き出したもの
;--------------------------------------------------
;RETURN 0 の部分は共通の口上が表示されます
;依赖ごとの口上を表示する部分は RETURN 0 を RETURN 1 にしてください
;--------------------------------------------------
SELECTCASE SCENE
CASE "依赖提示时"
	
	RETURN 0
CASE "依赖非受託时"
	
	RETURN 0
CASE "依赖受託时"
	
	RETURN 0
CASE "依赖确认时"
	
	RETURN 0
CASE "依赖破棄时"
	
	RETURN 0
CASE "依赖実行直前"
;○この依赖では討伐される側の口上です
	IF CANNOT_DANMAKU() == 1
	;屋内のとき（勝負できない）
	;【◆口上】
	;【W 屋外で挑もう…】
		
		RETURN 0
	ELSEIF CANNOT_DANMAKU() == 2
	;仕事中（勝負できない）
	;【◆口上】
	;【W お取り込み中だ…】
		
		RETURN 0
	ELSEIF CANNOT_DANMAKU() == 3
	;あなたが疲労中（勝負できない）
	;【◆口上】
	;【W 今は疲れている…】
		
		RETURN 0
	ELSEIF CANNOT_DANMAKU() == 4
	;口上主が疲労竜（勝負できない）
	;【◆口上】
	;【W 今挑むのはフェアじゃない…】
		
		RETURN 0
	ELSEIF CANNOT_DANMAKU() == 5
	;既に今日一度弹幕胜负をしている（勝負できない）
	;【◆口上】
	;【W 今日はもう挑めない…】
		
		RETURN 0
	ELSE
	;【◆口上】
	;【W 勝負开始！！】
	;【以降はそのまま弾幕口上が呼ばれます】
		
		RETURN 0
	ENDIF
	;【▼そのまま下の口上へ】
CASE "依赖実行直后"
;○この依赖では討伐される側の口上です
	IF CFLAG:CLIENT_ID:依赖状况 == 2
		IF CANNOT_DANMAKU() == 0
			;败犬
			;【▲上の口上から続き】
			;【L ［彼女］の討伐に失敗した……】
			;【W ……こうなったら后日出直すしかない】
			;【◆口上】
			RETURN 0
		ELSE
			;挑めなかった场合
			;【▲上の口上から続き】
			;【◆口上】
			RETURN 0
		ENDIF
	ELSEIF CFLAG:CLIENT_ID:依赖状况 == 3
	;勝利
	;【▲上の口上から続き】
	;【W ［彼女］を懲らしめた！】
	;【◆口上】
		
		RETURN 0
	ENDIF
CASE "成功報告时"
;報酬：賞金（时価）
	
	RETURN 0
CASE "失敗報告时"
	
	RETURN 0
;CASE "依赖報告不要"
;＜この依赖にはありません＞
;	RETURN 0
ENDSELECT

;==================================================
;依赖口上・一般依赖13：うまい鱼が食べたいなぁ
;==================================================
@M_KOJO_IRAI_13_KX(ROLE, SCENE, CLIENT_ID)
#DIMS ROLE     ;キャラの立场、依赖者かそうでないか
#DIMS SCENE    ;セリフの表示されるタイミング
#DIM  CLIENT_ID;依赖者IDだけ抜き出したもの
;--------------------------------------------------
;RETURN 0 の部分は共通の口上が表示されます
;依赖ごとの口上を表示する部分は RETURN 0 を RETURN 1 にしてください
;--------------------------------------------------
SELECTCASE SCENE
CASE "依赖提示时"
	
	RETURN 0
CASE "依赖非受託时"
	
	RETURN 0
CASE "依赖受託时"
	
	RETURN 0
CASE "依赖确认时"
	
	RETURN 0
CASE "依赖破棄时"
	
	RETURN 0
CASE "依赖実行直前"
;○この依赖は依赖主がTARGETです
;【◆口上】
;【L ［あなた］は［彼女］にうまい鱼を渡した】
;【L 何だか生きる希望がわいてきたよ。】
;【▼そのまま下の口上へ】
	
	RETURN 0
CASE "依赖実行直后"
;○この依赖は必ず実行后达成します
;【▲上の口上から続き】
;【◆口上】
;【▼そのまま下の口上へ】
	
	RETURN 0
CASE "成功報告时"
;報酬：白米３個
	
	RETURN 0
CASE "失敗報告时"
	
	RETURN 0
;CASE "依赖報告不要"
;＜この依赖にはありません＞
;	RETURN 0
ENDSELECT

;==================================================
;依赖口上・一般依赖16：お悩み相談
;==================================================
@M_KOJO_IRAI_16_KX(ROLE, SCENE, CLIENT_ID)
#DIMS ROLE     ;キャラの立场、依赖者かそうでないか
#DIMS SCENE    ;セリフの表示されるタイミング
#DIM  CLIENT_ID;依赖者IDだけ抜き出したもの
;--------------------------------------------------
;RETURN 0 の部分は共通の口上が表示されます
;依赖ごとの口上を表示する部分は RETURN 0 を RETURN 1 にしてください
;--------------------------------------------------
SELECTCASE SCENE
CASE "依赖提示时"
	
	RETURN 0
CASE "依赖非受託时"
	
	RETURN 0
CASE "依赖受託时"
	
	RETURN 0
CASE "依赖确认时"
	
	RETURN 0
CASE "依赖破棄时"
	
	RETURN 0
CASE "依赖実行直前"
	
	RETURN 0
CASE "依赖実行直后"
	IF TFLAG:193 == 1
		;正解の选择肢
	ELSEIF TFLAG:193 == 2
		;ハズレの选择肢だったが逆に正解
	ELSEIF TFLAG:193 == -1
		;ハズレの选择肢
	ELSEIF TFLAG:193 == -2
		;正解の选择肢だったが逆にハズレ
	ENDIF
	RETURN 0
CASE "成功報告时"
;報酬：野菜か果物
	
	RETURN 0
CASE "失敗報告时"
	
	RETURN 0
;CASE "依赖報告不要"
;＜この依赖にはありません＞
;	RETURN 0
CASE "口上委託率"
;＜この依赖特有の分岐です。(RESULT:100の值)％の確率で"口上専用"が发生します＞
	RESULT:100 = 0

;＜RESULT:YYに0以外の数值を入れると、該当のお悩みを发生させなくできます（詳しくは一般依赖16_お悩み相談参照）＞
;1,2:弾幕ごっこの悩み 3,4:晩酌の悩み 5,6:本の悩み 7,8:香料の悩み
;9,10:音乐の悩み 11,12:服の悩み 13:髪型の悩み 14:参拝の悩み 15:ダイエットの悩み
;16,17:菜园の悩み 18:休日の悩み 19:人气の悩み 20:集中力の悩み 21:御三家の悩み 22:進化の悩み
	RESULT:1 = 0
	RESULT:2 = 0
	RETURN 1
CASE "口上専用"
;＜この依赖特有の分岐です。(口上委託率)％の確率で发生します＞
;16,お悩み相談 の场合
;RESULT        发生判定,0だと发生しません
;RESULT:1      正解の选择肢（ただしRESULT:2が0だと、一定確率で逆にハズレる）
;RESULT:2      逆张り正解／ハズレ判定の消去,1だと逆张りが无くなります
;RESULTS:0     お悩み文章（自动で『』で囲まれます）
;RESULTS:1～3  选择肢１～３
;何も发生しない状态になると无限ループになるので注意してください
;●追记メモ
;RESULT:1を4～6にするとそれぞれ1～3がハズレ（残り2つが正解）になります
;RESULT:1を7にするとどれも正解になります
;RESULT:1を8にするとどれもハズレになります
;1～3以外の场合は逆张り判定が自动で无くなります
	RESULT = 0

;▼サンプル
[SKIPSTART]
	SELECTCASE RAND:5
	CASE 0
		;このお悩みは、恋慕で发生し、亲密によって正解が変わります
		SIF TALENT:恋慕
			RESULT = 1
		RESULTS:0 = 赤ちゃん作るなら何人がいいかな？
		RESULTS:1 = ５人でバスケチームを作ろう
		RESULTS:2 = ９人で野球チームを作ろう
		RESULTS:3 = １１人でサッカーチームを作ろう
		IF ABL:亲密 >= 100
			RESULT:1 = 3
		ELSEIF ABL:亲密 >= 20
			RESULT:1 = 2
		ELSE
			RESULT:1 = 1
		ENDIF
	CASE 1
		;このお悩みは、无条件で发生し、正解は絶対に2番で固定化されます
		SIF 1
			RESULT = 1
		RESULTS:0 = どのジョブがいいかな？
		RESULTS:1 = かっこいしナイト
		RESULTS:2 = シーフが便利だよ
		RESULTS:3 = 青魔道士最强！
		RESULT:1 = 2
		RESULT:2 = 1
	CASE 2
		;このお悩みは、夏の月に发生し、正解がランダムで変わります
		SIF DAY:2 == 2
			RESULT = 1
		RESULTS:0 = どの水着を買おうかな？
		RESULTS:1 = かわいい连衣裙
		RESULTS:2 = クールなセパレート
		RESULTS:3 = 色っぽいビキニ
		RESULT:1 = RAND:3 + 1
	CASE 3
		;このお悩みは、无条件で发生し、1番と3番が正解になります（5 - 3 = 2番がハズレになります）
		SIF 1
			RESULT = 1
		RESULTS:0 = ワンちゃんの名前は何にしよう？
		RESULTS:1 = ボロンゴ
		RESULTS:2 = ゲレゲレ
		RESULTS:3 = プックル
		RESULT:1 = 5
	CASE 4
		;このお悩みは、无条件で发生し、どれを选んでも正解になります
		SIF 1
			RESULT = 1
		RESULTS:0 = 夕饭は何がいい？
		RESULTS:1 = 肉じゃが
		RESULTS:2 = コロッケ
		RESULTS:3 = 焼き鱼
		RESULT:1 = 7
	ENDSELECT
[SKIPEND]

	SIF !RESULT
		RESTART
	RETURN 1
ENDSELECT

;==================================================
;依赖口上・固有依赖101：
;固有依赖の仕様はキャラによって异なりますが依赖番号は101～で共通です
;"依赖名"部分に依赖名を入れて下さい
;==================================================
@M_KOJO_IRAI_依赖名_KX(ROLE, SCENE, CLIENT_ID)
#DIMS ROLE     ;キャラの立场、依赖者かそうでないか
#DIMS SCENE    ;セリフの表示されるタイミング
#DIM  CLIENT_ID;依赖者IDだけ抜き出したもの
;--------------------------------------------------
;RETURN 0 の部分は共通の口上が表示されます
;依赖ごとの口上を表示する部分は RETURN 0 を RETURN 1 にしてください
;--------------------------------------------------
SELECTCASE SCENE
CASE "依赖提示时"
	
	RETURN 0
CASE "依赖非受託时"
	
	RETURN 0
CASE "依赖受託时"
	
	RETURN 0
CASE "依赖确认时"
	
	RETURN 0
CASE "依赖破棄时"
	
	RETURN 0
CASE "依赖実行直前"
	
	RETURN 0
CASE "依赖実行直后"
	
	RETURN 0
CASE "成功報告时"
;報酬：
	
	RETURN 0
CASE "失敗報告时"
	
	RETURN 0
CASE "依赖報告不要"
	
	RETURN 0
ENDSELECT

[SKIPSTART]
;＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝
;依赖口上テンプレのテンプレ
;==================================================
;依赖口上・一般依赖Y：
;==================================================
@M_KOJO_IRAI_Y_KX(ROLE, SCENE, CLIENT_ID)
#DIMS ROLE     ;キャラの立场、依赖者かそうでないか
#DIMS SCENE    ;セリフの表示されるタイミング
#DIM  CLIENT_ID;依赖者IDだけ抜き出したもの
;--------------------------------------------------
;RETURN 0 の部分は共通の口上が表示されます
;依赖ごとの口上を表示する部分は RETURN 0 を RETURN 1 にしてください
;--------------------------------------------------
SELECTCASE SCENE
CASE "依赖提示时"
	
	RETURN 0
CASE "依赖非受託时"
	
	RETURN 0
CASE "依赖受託时"
	
	RETURN 0
CASE "依赖确认时"
	
	RETURN 0
CASE "依赖破棄时"
	
	RETURN 0
CASE "依赖実行直前"
	
	RETURN 0
CASE "依赖実行直后"
	
	RETURN 0
CASE "成功報告时"
;報酬：
	
	RETURN 0
CASE "失敗報告时"
	
	RETURN 0
;CASE "依赖報告不要"
;＜この依赖にはありません＞
;	RETURN 0
ENDSELECT
[SKIPEND]
