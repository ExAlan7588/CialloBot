;XXは置换で空いている番号（30まで）に置き换えてください。

;--------------------------------------------
;基本条件いろいろ
;--------------------------------------------
@IRAI_SETTING_XX

;期限、当日中なら0
SETTING_IRAI_DEADLINE:XX =
;依赖主
SETTING_IRAI_CLIENT:XX = 
;対象 依赖主と同じでもいい
SETTING_IRAI_TARGET:XX = 
;依赖実行时のコマンド名
SETTING_IRAI_COMMANDNAME:XX = 


;--------------------------------------------
;依赖发生时のメッセージ
;--------------------------------------------
@IRAI_INITIALMESSAGE_XX

PRINTFORML 「依赖を持ちかけた时のセリフ」
PRINTFORML 依赖内容：
PRINTFORML 報酬　　：

;--------------------------------------------
;依赖実行时のメッセージやソースなどの处理
;--------------------------------------------
@IRAI_CARRYOUT_XX
;ここでRETURN -1した场合、実行されず戻る

;PRINTFORML %CALLNAME:MASTER%は言われたことをやった

;--------------------------------------------
;依赖失敗时のメッセージやソースなどの处理
;--------------------------------------------
@IRAI_FAILED_XX
;依赖を期限切れさせてから会ったときに呼び出される

;システムメッセージつき好感度変化
;最后の引数だけ好感度が変化
;CALL CHANGE_CFLAG(2,TARGET,-5)

;システムメッセージつき信赖度変化
;最后の引数だけ信赖度が変化
;CALL CHANGE_CFLAG(4,TARGET,-3)

;机嫌を悪化させる处理を入れてもいい
;TALENT:机嫌 = LIMIT(TALENT:机嫌 -1, -1,1)

;--------------------------------------------
;依赖达成时のメッセージやソースなどの处理
;--------------------------------------------
@IRAI_SUCCESS_XX
;PRINTFORML 「钓りはいらん、取っておけ」
;MONEY += 10

;システムメッセージつき好感度変化
;最后の引数だけ好感度が変化
;CALL CHANGE_CFLAG(2,TARGET,30)

;システムメッセージつき信赖度変化
;最后の引数だけ信赖度が変化
;CALL CHANGE_CFLAG(4,TARGET,10)

;机嫌をよくする处理を入れてもいい
;TALENT:机嫌 = LIMIT(TALENT:机嫌 + 1, -1, 1)
;--------------------------------------------
;依赖実行条件
;--------------------------------------------
@IRAI_ACTABLE_XX
;これはTARGETに意识があり、ウフフ中でないという条件
;TARGETがSETTING_IRAI_TARGET:XXと一致しない场合ハネられる处理が外部にあるので、TARGETを指定する必要はない

SIF SHIRAHU(TARGET) == 1
	RETURN 1

;--------------------------------------------
;依赖发生条件
;--------------------------------------------
@IRAI_HAPPEN_XX
;1/15になってるけど適当に調整してください
;一行目コメントアウトして无条件でRETURN 1するようにすれば確実に发生するのでテストプレイ时にどうぞ
SIF !RAND:15
	RETURN 1


;--------------------------------------------
;依赖消滅条件
;--------------------------------------------
@IRAI_VANISH_XX
;1/3になってるけど適当に調整してください
;一行目コメントアウトして无条件でRETURN 1するようにすれば（ｒｙ
SIF !RAND:3
	RETURN 1
