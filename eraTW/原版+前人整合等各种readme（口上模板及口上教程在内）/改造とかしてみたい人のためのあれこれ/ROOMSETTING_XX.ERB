@ROOMSETTING_XX
VARSET ROOMDATA
FOR LOCAL,1,100
	SIF GROUPMATCH(LOCAL,)
		ROOMDATA:LOCAL |= 场所_布団
	SIF GROUPMATCH(LOCAL,)
		ROOMDATA:LOCAL |= 场所_屋内
	SIF GROUPMATCH(LOCAL,)
		ROOMDATA:LOCAL |= 场所_澡堂
	SIF GROUPMATCH(LOCAL,)
		ROOMDATA:LOCAL |= 场所_厨房
	SIF GROUPMATCH(LOCAL,)
		ROOMDATA:LOCAL |= 场所_训练
	SIF GROUPMATCH(LOCAL,)
		ROOMDATA:LOCAL |= 场所_勉强
	SIF GROUPMATCH(LOCAL,)
		ROOMDATA:LOCAL |= 场所_休憩
	SIF GROUPMATCH(LOCAL,)
		ROOMDATA:LOCAL |= 场所_トイレ
	SIF GROUPMATCH(LOCAL,)
		ROOMDATA:LOCAL |= 场所_青天井
	SIF GROUPMATCH(LOCAL,)
		ROOMDATA:LOCAL |= 场所_月見
	SIF GROUPMATCH(LOCAL,)
		ROOMDATA:LOCAL |= 场所_天の声
	SIF GROUPMATCH(LOCAL,)
		ROOMDATA:LOCAL |= 场所_菜园
	SIF GROUPMATCH(LOCAL,)
		ROOMDATA:LOCAL |= 场所_水汲み
	SIF GROUPMATCH(LOCAL,)
		ROOMDATA:LOCAL |= 场所_見通し
	SIF GROUPMATCH(LOCAL,)
		ROOMDATA:LOCAL |= 场所_狭い
	SIF GROUPMATCH(LOCAL,)
		ROOMDATA:LOCAL |= 场所_うたた寝_可能
	SIF GROUPMATCH(LOCAL,)
		ROOMDATA:LOCAL |= 场所_身体测定
NEXT

MAXROOM = 
ENTRANCE = 
避難先 = 
デフォルト初始位置 = 
据点_大家 = 

;以下二つはよく変化するので個别に设定

@KICKOUT_X(ARG)
#FUNCTION

@BATHROOM_X()
#FUNCTION